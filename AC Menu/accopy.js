const raw = {
  "il2cpp_init": "LOnFi_cCHFZ",
  "il2cpp_init_utf16": "ZpsQccDQITV",
  "il2cpp_shutdown": "vmyoEMyOAyD",
  "il2cpp_set_config_dir": "DcOJiOinGjs",
  "il2cpp_set_data_dir": "PVIvxIOwHgK",
  "il2cpp_set_temp_dir": "OHOaHiGmhvx",
  "il2cpp_set_commandline_arguments": "OBaD_WoCVQ_",
  "il2cpp_set_commandline_arguments_utf16": "ryYfpAIQdFU",
  "il2cpp_set_config_utf16": "xFlUJaARRaC",
  "il2cpp_set_config": "oaapFusFVKM",
  "il2cpp_set_memory_callbacks": "nvWMXDPdpLd",
  "il2cpp_memory_pool_set_region_size": "YYnGhrpdPZJ",
  "il2cpp_memory_pool_get_region_size": "qvpcbr_KLgH",
  "il2cpp_get_corlib": "GdlZuDTNQpp",
  "il2cpp_add_internal_call": "I_hWtrioFhm",
  "il2cpp_resolve_icall": "kHibNPCUwvM",
  "il2cpp_alloc": "GKqqHnriYOH",
  "il2cpp_free": "yUnRFgzbi_r",
  "il2cpp_array_class_get": "KQBKfRewdSp",
  "il2cpp_array_length": "tulfmNDzoUG",
  "il2cpp_array_get_byte_length": "ClXpUemcUrl",
  "il2cpp_array_new": "gLKhfXwdlI_",
  "il2cpp_array_new_specific": "KyBCHwjfSyV",
  "il2cpp_array_new_full": "LrZpEZdLBNl",
  "il2cpp_bounded_array_class_get": "nQzkQuL_can",
  "il2cpp_array_element_size": "gxDwgDGKgXS",
  "il2cpp_assembly_get_image": "xXVXljANVIZ",
  "il2cpp_class_enum_basetype": "SsSSPWkjTEQ",
  "il2cpp_class_from_system_type": "yqfMkAGPijT",
  "il2cpp_class_is_inited": "rzWxrKNIbBN",
  "il2cpp_class_is_generic": "wdgEyO_wnBG",
  "il2cpp_class_is_inflated": "evVTaLMmUAC",
  "il2cpp_class_is_assignable_from": "COZrPUWlCNn",
  "il2cpp_class_is_subclass_of": "rfJijagNoYb",
  "il2cpp_class_has_parent": "PJAbCKzlDsp",
  "il2cpp_class_from_il2cpp_type": "npcErVsagoE",
  "il2cpp_class_from_name": "EOXvpahofqI",
  "il2cpp_class_get_element_class": "HaOrKUOZTzf",
  "il2cpp_class_get_events": "bkUbcuphCAw",
  "il2cpp_class_get_fields": "hlFOObCxLNr",
  "il2cpp_class_get_nested_types": "cjSlUMxIX_s",
  "il2cpp_class_get_interfaces": "jIcjhCenpWK",
  "il2cpp_class_get_properties": "HtMuuInFFIg",
  "il2cpp_class_get_property_from_name": "XdPuvHsNILh",
  "il2cpp_class_get_field_from_name": "sUpKbJpRJet",
  "il2cpp_class_get_methods": "VNacg_BYqum",
  "il2cpp_class_get_method_from_name": "svKvcEfHaVV",
  "il2cpp_class_get_name": "QQOa_vUelDi",
  "il2cpp_class_get_namespace": "vdpQ_pLfyli",
  "il2cpp_class_get_parent": "GUkDRIavEIV",
  "il2cpp_class_get_declaring_type": "fJwbHyParGA",
  "il2cpp_class_instance_size": "fCVNbcmCRSx",
  "il2cpp_class_num_fields": "LBcxbXe_VbC",
  "il2cpp_class_is_valuetype": "dSktnoqsJV_",
  "il2cpp_class_is_blittable": "KXvoZIvxelR",
  "il2cpp_class_value_size": "mMjpDPBopQk",
  "il2cpp_class_get_flags": "gPbOlxktgYQ",
  "il2cpp_class_is_abstract": "WATGmhPIWar",
  "il2cpp_class_is_interface": "NMIrbICjdQw",
  "il2cpp_class_array_element_size": "fiBOoHXOjzy",
  "il2cpp_class_from_type": "eqJdZUROfxe",
  "il2cpp_class_get_type": "XTWeaGDqCDb",
  "il2cpp_class_get_type_token": "bYtZSvHbelV",
  "il2cpp_class_has_attribute": "EKgxZrubnbZ",
  "il2cpp_class_has_references": "cGJHPEuVKMW",
  "il2cpp_class_is_enum": "OBrPWMTHSLv",
  "il2cpp_class_get_image": "BHtWjOdDdkU",
  "il2cpp_class_get_assemblyname": "olOElzYvPOM",
  "il2cpp_class_get_rank": "wbsysyTytgY",
  "il2cpp_class_get_data_size": "bTbZVOExlnl",
  "il2cpp_class_get_static_field_data": "TvGpYtVlkUs",
  "il2cpp_stats_dump_to_file": "WHFrfWMWIch",
  "il2cpp_stats_get_value": "zBSLzWqciEY",
  "il2cpp_domain_get": "aHdZiwOCZvx",
  "il2cpp_domain_assembly_open": "iwKYOzMiINV",
  "il2cpp_domain_get_assemblies": "DshB_ZYKjbL",
  "il2cpp_raise_exception": "GTdJ_SwTmdV",
  "il2cpp_exception_from_name_msg": "XNNcjNJoYYT",
  "il2cpp_get_exception_argument_null": "oHJQkjtxvkl",
  "il2cpp_format_exception": "icgLAGdlghK",
  "il2cpp_format_stack_trace": "eNOCkRBPOFS",
  "il2cpp_unhandled_exception": "HOowifuhVZM",
  "il2cpp_native_stack_trace": "C_XPtGJvRTb",
  "il2cpp_field_get_name": "YjIvRdaYJJX",
  "il2cpp_field_get_flags": "bjJZnlOUpDR",
  "il2cpp_field_get_from_reflection": "_xt_zaGofTH",
  "il2cpp_field_get_parent": "rKCqBdPLqkg",
  "il2cpp_field_get_object": "XBgVREirNLl",
  "il2cpp_field_get_offset": "ykhMfBAanXz",
  "il2cpp_field_get_type": "btQorhwVuai",
  "il2cpp_field_get_value": "IzTqPNEoLQk",
  "il2cpp_field_get_value_object": "pjqCopNUKFu",
  "il2cpp_field_has_attribute": "tQrURUVKsTw",
  "il2cpp_field_set_value": "ZcqGduYlCEQ",
  "il2cpp_field_set_value_object": "ZHVdlteiKMW",
  "il2cpp_field_static_get_value": "GzjoEkikslf",
  "il2cpp_field_static_set_value": "xyerPqExZGl",
  "il2cpp_field_is_literal": "V_ScUCwdpqE",
  "il2cpp_gc_collect": "oCtpfBkJiuV",
  "il2cpp_gc_collect_a_little": "ovoclvSxgGQ",
  "il2cpp_gc_start_incremental_collection": "NlEpkPWGiPY",
  "il2cpp_gc_enable": "_DRUgexbbyf",
  "il2cpp_gc_disable": "ozQVhggSGik",
  "il2cpp_gc_is_disabled": "SQDToIllnnO",
  "il2cpp_gc_set_mode": "krjlzCxlEgf",
  "il2cpp_gc_is_incremental": "AxFaOsHKbHB",
  "il2cpp_gc_get_max_time_slice_ns": "kzaidMQyHxP",
  "il2cpp_gc_set_max_time_slice_ns": "XFOXBqhgVzb",
  "il2cpp_gc_get_used_size": "ZLKxUZqNXHl",
  "il2cpp_gc_get_heap_size": "ZLaAPprnysK",
  "il2cpp_gc_foreach_heap": "a_YbWBvtSbY",
  "il2cpp_stop_gc_world": "vMzVyKwlJZV",
  "il2cpp_start_gc_world": "lNVQOucxMQY",
  "il2cpp_gc_alloc_fixed": "plLGAEiVmDE",
  "il2cpp_gc_free_fixed": "pDSKFZLmWfu",
  "il2cpp_gchandle_new": "YWcQGHBlEPM",
  "il2cpp_gchandle_new_weakref": "dZwFeDiq_MJ",
  "il2cpp_gchandle_get_target": "ysTrVSBkzTi",
  "il2cpp_gchandle_foreach_get_target": "GNXcvOApUlD",
  "il2cpp_gc_wbarrier_set_field": "ieHQpFhBiSF",
  "il2cpp_gc_has_strict_wbarriers": "ryNLRlkR_Pl",
  "il2cpp_gc_set_external_allocation_tracker": "nAE_RgVaqWS",
  "il2cpp_gc_set_external_wbarrier_tracker": "gtCGXbYMQhl",
  "il2cpp_gchandle_free": "BZCHiNQbwuZ",
  "il2cpp_object_header_size": "IwzyQLOTyXn",
  "il2cpp_array_object_header_size": "tALqcCccliC",
  "il2cpp_offset_of_array_length_in_array_object_header": "AWKOUQQPVqc",
  "il2cpp_offset_of_array_bounds_in_array_object_header": "OPe_ldKekYo",
  "il2cpp_allocation_granularity": "qYaouBrhAvw",
  "il2cpp_unity_liveness_allocate_struct": "bvsHUUiAt_u",
  "il2cpp_unity_liveness_calculation_from_root": "InsVouSxaHY",
  "il2cpp_unity_liveness_calculation_from_statics": "UYimSJxubpy",
  "il2cpp_unity_liveness_finalize": "hkeJGH_i_xV",
  "il2cpp_unity_liveness_free_struct": "hSKsleovlZk",
  "il2cpp_method_get_return_type": "XpAfDeZxNNQ",
  "il2cpp_method_get_from_reflection": "KNqbEifAexh",
  "il2cpp_method_get_object": "clXJBtUTTZl",
  "il2cpp_method_get_name": "zZSghTNUYAK",
  "il2cpp_method_is_generic": "WwiDvosNkbX",
  "il2cpp_method_is_inflated": "dQoScrzRzka",
  "il2cpp_method_is_instance": "nRnDVqbNqth",
  "il2cpp_method_get_param_count": "TBMeeqhQqaU",
  "il2cpp_method_get_param": "NNYTUWoqoHP",
  "il2cpp_method_get_class": "BsHtpKGsqTQ",
  "il2cpp_method_has_attribute": "syFVWSUrdIR",
  "il2cpp_method_get_declaring_type": "jfopoHsUqR_",
  "il2cpp_method_get_flags": "BVoXVFxsTMW",
  "il2cpp_method_get_token": "u_hwlhLQFYB",
  "il2cpp_method_get_param_name": "ephcDjBzTTh",
  "il2cpp_profiler_install": "ZkJGRsVNTGO",
  "il2cpp_profiler_set_events": "hlHeYhXnkdL",
  "il2cpp_profiler_install_enter_leave": "dSPlMlkzUex",
  "il2cpp_profiler_install_allocation": "xZvmMCFBBIO",
  "il2cpp_profiler_install_gc": "_qdpU_AKUmh",
  "il2cpp_profiler_install_fileio": "hToYRrhhakZ",
  "il2cpp_profiler_install_thread": "xJODdGHheCl",
  "il2cpp_property_get_name": "miGTvEHybNn",
  "il2cpp_property_get_get_method": "FklDlKFSucC",
  "il2cpp_property_get_set_method": "BAUGioZzXkA",
  "il2cpp_property_get_parent": "nDBEmNKlZjX",
  "il2cpp_property_get_flags": "CL_xDXxBTkQ",
  "il2cpp_object_get_class": "Xo_WLcnXYrP",
  "il2cpp_object_get_size": "GwINctDosov",
  "il2cpp_object_get_virtual_method": "GZTQqAUKphP",
  "il2cpp_object_new": "hbOcyjdQqdU",
  "il2cpp_object_unbox": "mJLCUsRikuF",
  "il2cpp_value_box": "QU_jdcv_CdU",
  "il2cpp_monitor_enter": "aLHRk_jVcFL",
  "il2cpp_monitor_try_enter": "SCgpOgkFuRw",
  "il2cpp_monitor_exit": "IokHqPSCbWI",
  "il2cpp_monitor_pulse": "ZyrgntjulEj",
  "il2cpp_monitor_pulse_all": "bOeJEOyxoqo",
  "il2cpp_monitor_wait": "GwoVFsCHAPo",
  "il2cpp_monitor_try_wait": "dHctoJwaWWi",
  "il2cpp_runtime_invoke_convert_args": "lvzqRptUKSy",
  "il2cpp_runtime_invoke": "uexuDtkfdOK",
  "il2cpp_runtime_class_init": "AwmmxUYGoSE",
  "il2cpp_runtime_object_init": "BVXBQSsxCUk",
  "il2cpp_runtime_object_init_exception": "hPIXXMsDyId",
  "il2cpp_runtime_unhandled_exception_policy_set": "crHvwUrAIuA",
  "il2cpp_string_length": "ofEOijrnuLR",
  "il2cpp_string_chars": "EogvXHzAJya",
  "il2cpp_string_new": "qrjbQTRQyMr",
  "il2cpp_string_new_wrapper": "fMNiJgKilBk",
  "il2cpp_string_new_utf16": "qoaFnIv_lzr",
  "il2cpp_string_new_len": "bpvPzxpdryK",
  "il2cpp_string_intern": "amFqjvJPyLg",
  "il2cpp_string_is_interned": "otzMiUDyZcW",
  "il2cpp_thread_current": "ibwFxxRrBhO",
  "il2cpp_thread_attach": "zCgFjfxPmGj",
  "il2cpp_thread_detach": "IhdCri_dBnr",
  "il2cpp_is_vm_thread": "PZXxzlXmxZe",
  "il2cpp_current_thread_walk_frame_stack": "hpWLbUxjQjU",
  "il2cpp_thread_walk_frame_stack": "hLWKIWelVFy",
  "il2cpp_current_thread_get_top_frame": "wEXdobhpzgl",
  "il2cpp_thread_get_top_frame": "DUbXlQpEvZP",
  "il2cpp_current_thread_get_frame_at": "Eoiim_AZKxg",
  "il2cpp_thread_get_frame_at": "iyj_cSOXlNO",
  "il2cpp_current_thread_get_stack_depth": "ztUXXGKZwwD",
  "il2cpp_thread_get_stack_depth": "gRTnAajVHXL",
  "il2cpp_set_default_thread_affinity": "_tYgdxgprvE",
  "il2cpp_override_stack_backtrace": "ZKMIoGYxQpI",
  "il2cpp_type_get_object": "ycMCUIuWYUj",
  "il2cpp_type_get_type": "IthlcwwLZnn",
  "il2cpp_type_get_class_or_element_class": "KWbslDwLTTS",
  "il2cpp_type_get_name": "nYihKtykBkg",
  "il2cpp_type_get_assembly_qualified_name": "kfEGstMnMEk",
  "il2cpp_type_get_reflection_name": "wuKLiRbdQor",
  "il2cpp_type_is_byref": "jzBRMtEq_LH",
  "il2cpp_type_get_attrs": "IcjKObGwwjY",
  "il2cpp_type_equals": "jbuPOjmDxNl",
  "il2cpp_type_is_static": "VSvlDJyHZpm",
  "il2cpp_type_is_pointer_type": "GuONFYeCm_S",
  "il2cpp_image_get_assembly": "rUQQNmEgNVd",
  "il2cpp_image_get_name": "wtUSUdaHkCi",
  "il2cpp_image_get_filename": "nHAklgjVGZW",
  "il2cpp_image_get_entry_point": "wHSFNzIbMjg",
  "il2cpp_image_get_class_count": "VtOvPLgTjhv",
  "il2cpp_image_get_class": "tVmEXnlULmw",
  "il2cpp_capture_memory_snapshot": "QoLobDClDIR",
  "il2cpp_free_captured_memory_snapshot": "gVTYPXdShvj",
  "il2cpp_set_find_plugin_callback": "jDEWdnXpqIq",
  "il2cpp_register_log_callback": "zrvhyqSZOVj",
  "il2cpp_debugger_set_agent_options": "oDoVfntklIK",
  "il2cpp_is_debugger_attached": "yCch_WWxiAj",
  "il2cpp_register_debugger_agent_transport": "hVgCaXWvpQz",
  "il2cpp_debug_foreach_method": "A_pHBBIsNRQ",
  "il2cpp_debug_get_method_info": "iIMgaSFnmim",
  "il2cpp_unity_install_unitytls_interface": "os_KcuLfmky",
  "il2cpp_custom_attrs_from_class": "jdGtWGyIId_",
  "il2cpp_custom_attrs_from_method": "gxgAPIdRXew",
  "il2cpp_custom_attrs_from_field": "FJAPkZtaPFM",
  "il2cpp_custom_attrs_has_attr": "quITzbnLXrV",
  "il2cpp_custom_attrs_get_attr": "aJRYRrzVHEQ",
  "il2cpp_custom_attrs_construct": "AZFbczzoNtd",
  "il2cpp_custom_attrs_free": "LLhUbOtwlYW",
  "il2cpp_type_get_name_chunked": "BaLveM_VvJl",
  "il2cpp_class_set_userdata": "AXkgbcQxalk",
  "il2cpp_class_get_userdata_offset": "rZazEijkHgZ",
  "il2cpp_class_for_each": "mqTvgMdHJed"
};
Il2Cpp.$config.exports = {
	il2cpp_init: () => Il2Cpp.module.findExportByName("LOnFi_cCHFZ"),
	il2cpp_init_utf16: () => Il2Cpp.module.findExportByName("ZpsQccDQITV"),
	il2cpp_shutdown: () => Il2Cpp.module.findExportByName("vmyoEMyOAyD"),
	il2cpp_set_config_dir: () => Il2Cpp.module.findExportByName("DcOJiOinGjs"),
	il2cpp_set_data_dir: () => Il2Cpp.module.findExportByName("PVIvxIOwHgK"),
	il2cpp_set_temp_dir: () => Il2Cpp.module.findExportByName("OHOaHiGmhvx"),
	il2cpp_set_commandline_arguments: () => Il2Cpp.module.findExportByName("OBaD_WoCVQ_"),
	il2cpp_set_commandline_arguments_utf16: () => Il2Cpp.module.findExportByName("ryYfpAIQdFU"),
	il2cpp_set_config_utf16: () => Il2Cpp.module.findExportByName("xFlUJaARRaC"),
	il2cpp_set_config: () => Il2Cpp.module.findExportByName("oaapFusFVKM"),
	il2cpp_set_memory_callbacks: () => Il2Cpp.module.findExportByName("nvWMXDPdpLd"),
	il2cpp_memory_pool_set_region_size: () => Il2Cpp.module.findExportByName("YYnGhrpdPZJ"),
	il2cpp_memory_pool_get_region_size: () => Il2Cpp.module.findExportByName("qvpcbr_KLgH"),
	il2cpp_get_corlib: () => Il2Cpp.module.findExportByName("GdlZuDTNQpp"),
	il2cpp_add_internal_call: () => Il2Cpp.module.findExportByName("I_hWtrioFhm"),
	il2cpp_resolve_icall: () => Il2Cpp.module.findExportByName("kHibNPCUwvM"),
	il2cpp_alloc: () => Il2Cpp.module.findExportByName("GKqqHnriYOH"),
	il2cpp_free: () => Il2Cpp.module.findExportByName("yUnRFgzbi_r"),
	il2cpp_array_class_get: () => Il2Cpp.module.findExportByName("KQBKfRewdSp"),
	il2cpp_array_length: () => Il2Cpp.module.findExportByName("tulfmNDzoUG"),
	il2cpp_array_get_byte_length: () => Il2Cpp.module.findExportByName("ClXpUemcUrl"),
	il2cpp_array_new: () => Il2Cpp.module.findExportByName("gLKhfXwdlI_"),
	il2cpp_array_new_specific: () => Il2Cpp.module.findExportByName("KyBCHwjfSyV"),
	il2cpp_array_new_full: () => Il2Cpp.module.findExportByName("LrZpEZdLBNl"),
	il2cpp_bounded_array_class_get: () => Il2Cpp.module.findExportByName("nQzkQuL_can"),
	il2cpp_array_element_size: () => Il2Cpp.module.findExportByName("gxDwgDGKgXS"),
	il2cpp_assembly_get_image: () => Il2Cpp.module.findExportByName("xXVXljANVIZ"),
	il2cpp_class_enum_basetype: () => Il2Cpp.module.findExportByName("SsSSPWkjTEQ"),
	il2cpp_class_from_system_type: () => Il2Cpp.module.findExportByName("yqfMkAGPijT"),
	il2cpp_class_is_inited: () => Il2Cpp.module.findExportByName("rzWxrKNIbBN"),
	il2cpp_class_is_generic: () => Il2Cpp.module.findExportByName("wdgEyO_wnBG"),
	il2cpp_class_is_inflated: () => Il2Cpp.module.findExportByName("evVTaLMmUAC"),
	il2cpp_class_is_assignable_from: () => Il2Cpp.module.findExportByName("COZrPUWlCNn"),
	il2cpp_class_is_subclass_of: () => Il2Cpp.module.findExportByName("rfJijagNoYb"),
	il2cpp_class_has_parent: () => Il2Cpp.module.findExportByName("PJAbCKzlDsp"),
	il2cpp_class_from_il2cpp_type: () => Il2Cpp.module.findExportByName("npcErVsagoE"),
	il2cpp_class_from_name: () => Il2Cpp.module.findExportByName("EOXvpahofqI"),
	il2cpp_class_get_element_class: () => Il2Cpp.module.findExportByName("HaOrKUOZTzf"),
	il2cpp_class_get_events: () => Il2Cpp.module.findExportByName("bkUbcuphCAw"),
	il2cpp_class_get_fields: () => Il2Cpp.module.findExportByName("hlFOObCxLNr"),
	il2cpp_class_get_nested_types: () => Il2Cpp.module.findExportByName("cjSlUMxIX_s"),
	il2cpp_class_get_interfaces: () => Il2Cpp.module.findExportByName("jIcjhCenpWK"),
	il2cpp_class_get_properties: () => Il2Cpp.module.findExportByName("HtMuuInFFIg"),
	il2cpp_class_get_property_from_name: () => Il2Cpp.module.findExportByName("XdPuvHsNILh"),
	il2cpp_class_get_field_from_name: () => Il2Cpp.module.findExportByName("sUpKbJpRJet"),
	il2cpp_class_get_methods: () => Il2Cpp.module.findExportByName("VNacg_BYqum"),
	il2cpp_class_get_method_from_name: () => Il2Cpp.module.findExportByName("svKvcEfHaVV"),
	il2cpp_class_get_name: () => Il2Cpp.module.findExportByName("QQOa_vUelDi"),
	il2cpp_class_get_namespace: () => Il2Cpp.module.findExportByName("vdpQ_pLfyli"),
	il2cpp_class_get_parent: () => Il2Cpp.module.findExportByName("GUkDRIavEIV"),
	il2cpp_class_get_declaring_type: () => Il2Cpp.module.findExportByName("fJwbHyParGA"),
	il2cpp_class_instance_size: () => Il2Cpp.module.findExportByName("fCVNbcmCRSx"),
	il2cpp_class_num_fields: () => Il2Cpp.module.findExportByName("LBcxbXe_VbC"),
	il2cpp_class_is_valuetype: () => Il2Cpp.module.findExportByName("dSktnoqsJV_"),
	il2cpp_class_is_blittable: () => Il2Cpp.module.findExportByName("KXvoZIvxelR"),
	il2cpp_class_value_size: () => Il2Cpp.module.findExportByName("mMjpDPBopQk"),
	il2cpp_class_get_flags: () => Il2Cpp.module.findExportByName("gPbOlxktgYQ"),
	il2cpp_class_is_abstract: () => Il2Cpp.module.findExportByName("WATGmhPIWar"),
	il2cpp_class_is_interface: () => Il2Cpp.module.findExportByName("NMIrbICjdQw"),
	il2cpp_class_array_element_size: () => Il2Cpp.module.findExportByName("fiBOoHXOjzy"),
	il2cpp_class_from_type: () => Il2Cpp.module.findExportByName("eqJdZUROfxe"),
	il2cpp_class_get_type: () => Il2Cpp.module.findExportByName("XTWeaGDqCDb"),
	il2cpp_class_get_type_token: () => Il2Cpp.module.findExportByName("bYtZSvHbelV"),
	il2cpp_class_has_attribute: () => Il2Cpp.module.findExportByName("EKgxZrubnbZ"),
	il2cpp_class_has_references: () => Il2Cpp.module.findExportByName("cGJHPEuVKMW"),
	il2cpp_class_is_enum: () => Il2Cpp.module.findExportByName("OBrPWMTHSLv"),
	il2cpp_class_get_image: () => Il2Cpp.module.findExportByName("BHtWjOdDdkU"),
	il2cpp_class_get_assemblyname: () => Il2Cpp.module.findExportByName("olOElzYvPOM"),
	il2cpp_class_get_rank: () => Il2Cpp.module.findExportByName("wbsysyTytgY"),
	il2cpp_class_get_data_size: () => Il2Cpp.module.findExportByName("bTbZVOExlnl"),
	il2cpp_class_get_static_field_data: () => Il2Cpp.module.findExportByName("TvGpYtVlkUs"),
	il2cpp_stats_dump_to_file: () => Il2Cpp.module.findExportByName("WHFrfWMWIch"),
	il2cpp_stats_get_value: () => Il2Cpp.module.findExportByName("zBSLzWqciEY"),
	il2cpp_domain_get: () => Il2Cpp.module.findExportByName("aHdZiwOCZvx"),
	il2cpp_domain_assembly_open: () => Il2Cpp.module.findExportByName("iwKYOzMiINV"),
	il2cpp_domain_get_assemblies: () => Il2Cpp.module.findExportByName("DshB_ZYKjbL"),
	il2cpp_raise_exception: () => Il2Cpp.module.findExportByName("GTdJ_SwTmdV"),
	il2cpp_exception_from_name_msg: () => Il2Cpp.module.findExportByName("XNNcjNJoYYT"),
	il2cpp_get_exception_argument_null: () => Il2Cpp.module.findExportByName("oHJQkjtxvkl"),
	il2cpp_format_exception: () => Il2Cpp.module.findExportByName("icgLAGdlghK"),
	il2cpp_format_stack_trace: () => Il2Cpp.module.findExportByName("eNOCkRBPOFS"),
	il2cpp_unhandled_exception: () => Il2Cpp.module.findExportByName("HOowifuhVZM"),
	il2cpp_native_stack_trace: () => Il2Cpp.module.findExportByName("C_XPtGJvRTb"),
	il2cpp_field_get_name: () => Il2Cpp.module.findExportByName("YjIvRdaYJJX"),
	il2cpp_field_get_flags: () => Il2Cpp.module.findExportByName("bjJZnlOUpDR"),
	il2cpp_field_get_from_reflection: () => Il2Cpp.module.findExportByName("_xt_zaGofTH"),
	il2cpp_field_get_parent: () => Il2Cpp.module.findExportByName("rKCqBdPLqkg"),
	il2cpp_field_get_object: () => Il2Cpp.module.findExportByName("XBgVREirNLl"),
	il2cpp_field_get_offset: () => Il2Cpp.module.findExportByName("ykhMfBAanXz"),
	il2cpp_field_get_type: () => Il2Cpp.module.findExportByName("btQorhwVuai"),
	il2cpp_field_get_value: () => Il2Cpp.module.findExportByName("IzTqPNEoLQk"),
	il2cpp_field_get_value_object: () => Il2Cpp.module.findExportByName("pjqCopNUKFu"),
	il2cpp_field_has_attribute: () => Il2Cpp.module.findExportByName("tQrURUVKsTw"),
	il2cpp_field_set_value: () => Il2Cpp.module.findExportByName("ZcqGduYlCEQ"),
	il2cpp_field_set_value_object: () => Il2Cpp.module.findExportByName("ZHVdlteiKMW"),
	il2cpp_field_static_get_value: () => Il2Cpp.module.findExportByName("GzjoEkikslf"),
	il2cpp_field_static_set_value: () => Il2Cpp.module.findExportByName("xyerPqExZGl"),
	il2cpp_field_is_literal: () => Il2Cpp.module.findExportByName("V_ScUCwdpqE"),
	il2cpp_gc_collect: () => Il2Cpp.module.findExportByName("oCtpfBkJiuV"),
	il2cpp_gc_collect_a_little: () => Il2Cpp.module.findExportByName("ovoclvSxgGQ"),
	il2cpp_gc_start_incremental_collection: () => Il2Cpp.module.findExportByName("NlEpkPWGiPY"),
	il2cpp_gc_enable: () => Il2Cpp.module.findExportByName("_DRUgexbbyf"),
	il2cpp_gc_disable: () => Il2Cpp.module.findExportByName("ozQVhggSGik"),
	il2cpp_gc_is_disabled: () => Il2Cpp.module.findExportByName("SQDToIllnnO"),
	il2cpp_gc_set_mode: () => Il2Cpp.module.findExportByName("krjlzCxlEgf"),
	il2cpp_gc_is_incremental: () => Il2Cpp.module.findExportByName("AxFaOsHKbHB"),
	il2cpp_gc_get_max_time_slice_ns: () => Il2Cpp.module.findExportByName("kzaidMQyHxP"),
	il2cpp_gc_set_max_time_slice_ns: () => Il2Cpp.module.findExportByName("XFOXBqhgVzb"),
	il2cpp_gc_get_used_size: () => Il2Cpp.module.findExportByName("ZLKxUZqNXHl"),
	il2cpp_gc_get_heap_size: () => Il2Cpp.module.findExportByName("ZLaAPprnysK"),
	il2cpp_gc_foreach_heap: () => Il2Cpp.module.findExportByName("a_YbWBvtSbY"),
	il2cpp_stop_gc_world: () => Il2Cpp.module.findExportByName("vMzVyKwlJZV"),
	il2cpp_start_gc_world: () => Il2Cpp.module.findExportByName("lNVQOucxMQY"),
	il2cpp_gc_alloc_fixed: () => Il2Cpp.module.findExportByName("plLGAEiVmDE"),
	il2cpp_gc_free_fixed: () => Il2Cpp.module.findExportByName("pDSKFZLmWfu"),
	il2cpp_gchandle_new: () => Il2Cpp.module.findExportByName("YWcQGHBlEPM"),
	il2cpp_gchandle_new_weakref: () => Il2Cpp.module.findExportByName("dZwFeDiq_MJ"),
	il2cpp_gchandle_get_target: () => Il2Cpp.module.findExportByName("ysTrVSBkzTi"),
	il2cpp_gchandle_foreach_get_target: () => Il2Cpp.module.findExportByName("GNXcvOApUlD"),
	il2cpp_gc_wbarrier_set_field: () => Il2Cpp.module.findExportByName("ieHQpFhBiSF"),
	il2cpp_gc_has_strict_wbarriers: () => Il2Cpp.module.findExportByName("ryNLRlkR_Pl"),
	il2cpp_gc_set_external_allocation_tracker: () => Il2Cpp.module.findExportByName("nAE_RgVaqWS"),
	il2cpp_gc_set_external_wbarrier_tracker: () => Il2Cpp.module.findExportByName("gtCGXbYMQhl"),
	il2cpp_gchandle_free: () => Il2Cpp.module.findExportByName("BZCHiNQbwuZ"),
	il2cpp_object_header_size: () => Il2Cpp.module.findExportByName("IwzyQLOTyXn"),
	il2cpp_array_object_header_size: () => Il2Cpp.module.findExportByName("tALqcCccliC"),
	il2cpp_offset_of_array_length_in_array_object_header: () => Il2Cpp.module.findExportByName("AWKOUQQPVqc"),
	il2cpp_offset_of_array_bounds_in_array_object_header: () => Il2Cpp.module.findExportByName("OPe_ldKekYo"),
	il2cpp_allocation_granularity: () => Il2Cpp.module.findExportByName("qYaouBrhAvw"),
	il2cpp_unity_liveness_allocate_struct: () => Il2Cpp.module.findExportByName("bvsHUUiAt_u"),
	il2cpp_unity_liveness_calculation_from_root: () => Il2Cpp.module.findExportByName("InsVouSxaHY"),
	il2cpp_unity_liveness_calculation_from_statics: () => Il2Cpp.module.findExportByName("UYimSJxubpy"),
	il2cpp_unity_liveness_finalize: () => Il2Cpp.module.findExportByName("hkeJGH_i_xV"),
	il2cpp_unity_liveness_free_struct: () => Il2Cpp.module.findExportByName("hSKsleovlZk"),
	il2cpp_method_get_return_type: () => Il2Cpp.module.findExportByName("XpAfDeZxNNQ"),
	il2cpp_method_get_from_reflection: () => Il2Cpp.module.findExportByName("KNqbEifAexh"),
	il2cpp_method_get_object: () => Il2Cpp.module.findExportByName("clXJBtUTTZl"),
	il2cpp_method_get_name: () => Il2Cpp.module.findExportByName("zZSghTNUYAK"),
	il2cpp_method_is_generic: () => Il2Cpp.module.findExportByName("WwiDvosNkbX"),
	il2cpp_method_is_inflated: () => Il2Cpp.module.findExportByName("dQoScrzRzka"),
	il2cpp_method_is_instance: () => Il2Cpp.module.findExportByName("nRnDVqbNqth"),
	il2cpp_method_get_param_count: () => Il2Cpp.module.findExportByName("TBMeeqhQqaU"),
	il2cpp_method_get_param: () => Il2Cpp.module.findExportByName("NNYTUWoqoHP"),
	il2cpp_method_get_class: () => Il2Cpp.module.findExportByName("BsHtpKGsqTQ"),
	il2cpp_method_has_attribute: () => Il2Cpp.module.findExportByName("syFVWSUrdIR"),
	il2cpp_method_get_declaring_type: () => Il2Cpp.module.findExportByName("jfopoHsUqR_"),
	il2cpp_method_get_flags: () => Il2Cpp.module.findExportByName("BVoXVFxsTMW"),
	il2cpp_method_get_token: () => Il2Cpp.module.findExportByName("u_hwlhLQFYB"),
	il2cpp_method_get_param_name: () => Il2Cpp.module.findExportByName("ephcDjBzTTh"),
	il2cpp_profiler_install: () => Il2Cpp.module.findExportByName("ZkJGRsVNTGO"),
	il2cpp_profiler_set_events: () => Il2Cpp.module.findExportByName("hlHeYhXnkdL"),
	il2cpp_profiler_install_enter_leave: () => Il2Cpp.module.findExportByName("dSPlMlkzUex"),
	il2cpp_profiler_install_allocation: () => Il2Cpp.module.findExportByName("xZvmMCFBBIO"),
	il2cpp_profiler_install_gc: () => Il2Cpp.module.findExportByName("_qdpU_AKUmh"),
	il2cpp_profiler_install_fileio: () => Il2Cpp.module.findExportByName("hToYRrhhakZ"),
	il2cpp_profiler_install_thread: () => Il2Cpp.module.findExportByName("xJODdGHheCl"),
	il2cpp_property_get_name: () => Il2Cpp.module.findExportByName("miGTvEHybNn"),
	il2cpp_property_get_get_method: () => Il2Cpp.module.findExportByName("FklDlKFSucC"),
	il2cpp_property_get_set_method: () => Il2Cpp.module.findExportByName("BAUGioZzXkA"),
	il2cpp_property_get_parent: () => Il2Cpp.module.findExportByName("nDBEmNKlZjX"),
	il2cpp_property_get_flags: () => Il2Cpp.module.findExportByName("CL_xDXxBTkQ"),
	il2cpp_object_get_class: () => Il2Cpp.module.findExportByName("Xo_WLcnXYrP"),
	il2cpp_object_get_size: () => Il2Cpp.module.findExportByName("GwINctDosov"),
	il2cpp_object_get_virtual_method: () => Il2Cpp.module.findExportByName("GZTQqAUKphP"),
	il2cpp_object_new: () => Il2Cpp.module.findExportByName("hbOcyjdQqdU"),
	il2cpp_object_unbox: () => Il2Cpp.module.findExportByName("mJLCUsRikuF"),
	il2cpp_value_box: () => Il2Cpp.module.findExportByName("QU_jdcv_CdU"),
	il2cpp_monitor_enter: () => Il2Cpp.module.findExportByName("aLHRk_jVcFL"),
	il2cpp_monitor_try_enter: () => Il2Cpp.module.findExportByName("SCgpOgkFuRw"),
	il2cpp_monitor_exit: () => Il2Cpp.module.findExportByName("IokHqPSCbWI"),
	il2cpp_monitor_pulse: () => Il2Cpp.module.findExportByName("ZyrgntjulEj"),
	il2cpp_monitor_pulse_all: () => Il2Cpp.module.findExportByName("bOeJEOyxoqo"),
	il2cpp_monitor_wait: () => Il2Cpp.module.findExportByName("GwoVFsCHAPo"),
	il2cpp_monitor_try_wait: () => Il2Cpp.module.findExportByName("dHctoJwaWWi"),
	il2cpp_runtime_invoke_convert_args: () => Il2Cpp.module.findExportByName("lvzqRptUKSy"),
	il2cpp_runtime_invoke: () => Il2Cpp.module.findExportByName("uexuDtkfdOK"),
	il2cpp_runtime_class_init: () => Il2Cpp.module.findExportByName("AwmmxUYGoSE"),
	il2cpp_runtime_object_init: () => Il2Cpp.module.findExportByName("BVXBQSsxCUk"),
	il2cpp_runtime_object_init_exception: () => Il2Cpp.module.findExportByName("hPIXXMsDyId"),
	il2cpp_runtime_unhandled_exception_policy_set: () => Il2Cpp.module.findExportByName("crHvwUrAIuA"),
	il2cpp_string_length: () => Il2Cpp.module.findExportByName("ofEOijrnuLR"),
	il2cpp_string_chars: () => Il2Cpp.module.findExportByName("EogvXHzAJya"),
	il2cpp_string_new: () => Il2Cpp.module.findExportByName("qrjbQTRQyMr"),
	il2cpp_string_new_wrapper: () => Il2Cpp.module.findExportByName("fMNiJgKilBk"),
	il2cpp_string_new_utf16: () => Il2Cpp.module.findExportByName("qoaFnIv_lzr"),
	il2cpp_string_new_len: () => Il2Cpp.module.findExportByName("bpvPzxpdryK"),
	il2cpp_string_intern: () => Il2Cpp.module.findExportByName("amFqjvJPyLg"),
	il2cpp_string_is_interned: () => Il2Cpp.module.findExportByName("otzMiUDyZcW"),
	il2cpp_thread_current: () => Il2Cpp.module.findExportByName("ibwFxxRrBhO"),
	il2cpp_thread_attach: () => Il2Cpp.module.findExportByName("zCgFjfxPmGj"),
	il2cpp_thread_detach: () => Il2Cpp.module.findExportByName("IhdCri_dBnr"),
	il2cpp_is_vm_thread: () => Il2Cpp.module.findExportByName("PZXxzlXmxZe"),
	il2cpp_current_thread_walk_frame_stack: () => Il2Cpp.module.findExportByName("hpWLbUxjQjU"),
	il2cpp_thread_walk_frame_stack: () => Il2Cpp.module.findExportByName("hLWKIWelVFy"),
	il2cpp_current_thread_get_top_frame: () => Il2Cpp.module.findExportByName("wEXdobhpzgl"),
	il2cpp_thread_get_top_frame: () => Il2Cpp.module.findExportByName("DUbXlQpEvZP"),
	il2cpp_current_thread_get_frame_at: () => Il2Cpp.module.findExportByName("Eoiim_AZKxg"),
	il2cpp_thread_get_frame_at: () => Il2Cpp.module.findExportByName("iyj_cSOXlNO"),
	il2cpp_current_thread_get_stack_depth: () => Il2Cpp.module.findExportByName("ztUXXGKZwwD"),
	il2cpp_thread_get_stack_depth: () => Il2Cpp.module.findExportByName("gRTnAajVHXL"),
	il2cpp_set_default_thread_affinity: () => Il2Cpp.module.findExportByName("_tYgdxgprvE"),
	il2cpp_override_stack_backtrace: () => Il2Cpp.module.findExportByName("ZKMIoGYxQpI"),
	il2cpp_type_get_object: () => Il2Cpp.module.findExportByName("ycMCUIuWYUj"),
	il2cpp_type_get_type: () => Il2Cpp.module.findExportByName("IthlcwwLZnn"),
	il2cpp_type_get_class_or_element_class: () => Il2Cpp.module.findExportByName("KWbslDwLTTS"),
	il2cpp_type_get_name: () => Il2Cpp.module.findExportByName("nYihKtykBkg"),
	il2cpp_type_get_assembly_qualified_name: () => Il2Cpp.module.findExportByName("kfEGstMnMEk"),
	il2cpp_type_get_reflection_name: () => Il2Cpp.module.findExportByName("wuKLiRbdQor"),
	il2cpp_type_is_byref: () => Il2Cpp.module.findExportByName("jzBRMtEq_LH"),
	il2cpp_type_get_attrs: () => Il2Cpp.module.findExportByName("IcjKObGwwjY"),
	il2cpp_type_equals: () => Il2Cpp.module.findExportByName("jbuPOjmDxNl"),
	il2cpp_type_is_static: () => Il2Cpp.module.findExportByName("VSvlDJyHZpm"),
	il2cpp_type_is_pointer_type: () => Il2Cpp.module.findExportByName("GuONFYeCm_S"),
	il2cpp_image_get_assembly: () => Il2Cpp.module.findExportByName("rUQQNmEgNVd"),
	il2cpp_image_get_name: () => Il2Cpp.module.findExportByName("wtUSUdaHkCi"),
	il2cpp_image_get_filename: () => Il2Cpp.module.findExportByName("nHAklgjVGZW"),
	il2cpp_image_get_entry_point: () => Il2Cpp.module.findExportByName("wHSFNzIbMjg"),
	il2cpp_image_get_class_count: () => Il2Cpp.module.findExportByName("VtOvPLgTjhv"),
	il2cpp_image_get_class: () => Il2Cpp.module.findExportByName("tVmEXnlULmw"),
	il2cpp_capture_memory_snapshot: () => Il2Cpp.module.findExportByName("QoLobDClDIR"),
	il2cpp_free_captured_memory_snapshot: () => Il2Cpp.module.findExportByName("gVTYPXdShvj"),
	il2cpp_set_find_plugin_callback: () => Il2Cpp.module.findExportByName("jDEWdnXpqIq"),
	il2cpp_register_log_callback: () => Il2Cpp.module.findExportByName("zrvhyqSZOVj"),
	il2cpp_debugger_set_agent_options: () => Il2Cpp.module.findExportByName("oDoVfntklIK"),
	il2cpp_is_debugger_attached: () => Il2Cpp.module.findExportByName("yCch_WWxiAj"),
	il2cpp_register_debugger_agent_transport: () => Il2Cpp.module.findExportByName("hVgCaXWvpQz"),
	il2cpp_debug_foreach_method: () => Il2Cpp.module.findExportByName("A_pHBBIsNRQ"),
	il2cpp_debug_get_method_info: () => Il2Cpp.module.findExportByName("iIMgaSFnmim"),
	il2cpp_unity_install_unitytls_interface: () => Il2Cpp.module.findExportByName("os_KcuLfmky"),
	il2cpp_custom_attrs_from_class: () => Il2Cpp.module.findExportByName("jdGtWGyIId_"),
	il2cpp_custom_attrs_from_method: () => Il2Cpp.module.findExportByName("gxgAPIdRXew"),
	il2cpp_custom_attrs_from_field: () => Il2Cpp.module.findExportByName("FJAPkZtaPFM"),
	il2cpp_custom_attrs_has_attr: () => Il2Cpp.module.findExportByName("quITzbnLXrV"),
	il2cpp_custom_attrs_get_attr: () => Il2Cpp.module.findExportByName("aJRYRrzVHEQ"),
	il2cpp_custom_attrs_construct: () => Il2Cpp.module.findExportByName("AZFbczzoNtd"),
	il2cpp_custom_attrs_free: () => Il2Cpp.module.findExportByName("LLhUbOtwlYW"),
	il2cpp_type_get_name_chunked: () => Il2Cpp.module.findExportByName("BaLveM_VvJl"),
	il2cpp_class_set_userdata: () => Il2Cpp.module.findExportByName("AXkgbcQxalk"),
	il2cpp_class_get_userdata_offset: () => Il2Cpp.module.findExportByName("rZazEijkHgZ"),
	il2cpp_class_for_each: () => Il2Cpp.module.findExportByName("mqTvgMdHJed"),
};
const itemIDs = [
    "item_anti_gravity_grenade",
    "item_apple",
    "item_arena_pistol",
    "item_arena_shotgun",
    "item_arrow",
    "item_arrow_heart",
    "item_arrow_lightbulb",
    "item_backpack",
    "item_backpack_black",
    "item_backpack_green",
    "item_backpack_large_base",
    "item_backpack_large_basketball",
    "item_backpack_large_clover",
    "item_backpack_pink",
    "item_backpack_small_base",
    "item_backpack_white",
    "item_backpack_with_flashlight",
    "item_balloon",
    "item_balloon_heart",
    "item_banana",
    "item_baseball_bat",
    "item_big_cup",
    "item_boombox",
    "item_boombox_neon",
    "item_box_fan",
    "item_broccoli_grenade",
    "item_broccoli_shrink_grenade",
    "item_calculator",
    "item_cardboard_box",
    "item_clapper",
    "item_cluster_grenade",
    "item_cola",
    "item_cola_large",
    "item_company_ration",
    "item_company_ration_heal",
    "item_cracker",
    "item_crossbow",
    "item_crossbow_heart",
    "item_crowbar",
    "item_disc",
    "item_disposable_camera",
    "item_dynamite",
    "item_dynamite_cube",
    "item_egg",
    "item_flaregun",
    "item_flashbang",
    "item_flashlight",
    "item_flashlight_mega",
    "item_flashlight_red",
    "item_floppy3",
    "item_floppy5",
    "item_football",
    "item_frying_pan",
    "item_glowstick",
    "item_goldbar",
    "item_grenade",
    "item_hawaiian_drum",
    "item_heart_chunk",
    "item_heart_gun",
    "item_heartchocolatebox",
    "item_hh_key",
    "item_hookshot",
    "item_hookshot_sword",
    "item_hoverpad",
    "item_impulse_grenade",
    "item_jetpack",
    "item_keycard",
    "item_lance",
    "item_landmine",
    "item_large_banana",
    "item_mug",
    "item_nut",
    "item_nut_drop",
    "item_ogre_hands",
    "item_ore_copper_l",
    "item_ore_copper_m",
    "item_ore_copper_s",
    "item_ore_gold_l",
    "item_ore_gold_m",
    "item_ore_gold_s",
    "item_ore_hell",
    "item_ore_silver_l",
    "item_ore_silver_m",
    "item_ore_silver_s",
    "item_painting_canvas",
    "item_paperpack",
    "item_pelican_case",
    "item_pickaxe",
    "item_pickaxe_cny",
    "item_pickaxe_cube",
    "item_pinata_bat",
    "item_pipe",
    "item_plunger",
    "item_pogostick",
    "item_police_baton",
    "item_pumpkin_pie",
    "item_pumpkinjack",
    "item_pumpkinjack_small",
    "item_quiver",
    "item_quiver_heart",
    "item_radioactive_broccoli",
    "item_randombox_mobloot_big",
    "item_randombox_mobloot_medium",
    "item_randombox_mobloot_small",
    "item_randombox_mobloot_weapons",
    "item_randombox_mobloot_zombie",
    "item_revolver",
    "item_revolver_ammo",
    "item_revolver_gold",
    "item_rope",
    "item_rpg",
    "item_rpg_ammo",
    "item_rpg_ammo_egg",
    "item_rpg_ammo_spear",
    "item_rpg_cny",
    "item_rpg_easter",
    "item_rpg_spear",
    "item_rubberducky",
    "item_ruby",
    "item_saddle",
    "item_scanner",
    "item_scissors",
    "item_shield",
    "item_shield_bones",
    "item_shield_police",
    "item_shield_viking_1",
    "item_shield_viking_2",
    "item_shield_viking_3",
    "item_shield_viking_4",
    "item_shotgun",
    "item_shotgun_ammo",
    "item_shredder",
    "item_shrinking_broccoli",
    "item_snowball",
    "item_stapler",
    "item_stash_grenade",
    "item_stick_armbones",
    "item_stick_bone",
    "item_sticker_dispenser",
    "item_sticky_dynamite",
    "item_stinky_cheese",
    "item_tablet",
    "item_tapedispenser",
    "item_tele_grenade",
    "item_theremin",
    "item_timebomb",
    "item_toilet_paper",
    "item_toilet_paper_mega",
    "item_toilet_paper_roll_empty",
    "item_treestick",
    "item_tripwire_explosive",
    "item_trophy",
    "item_turkey_leg",
    "item_turkey_whole",
    "item_ukulele",
    "item_ukulele_gold",
    "item_umbrella",
    "item_umbrella_clover",
    "item_upsidedown_loot",
    "item_uranium_chunk_l",
    "item_uranium_chunk_m",
    "item_uranium_chunk_s",
    "item_viking_hammer",
    "item_viking_hammer_twilight",
    "item_whoopie",
    "item_zipline_gun",
    "item_zombie_meat",
    "item_crate",
    ];
const prefabList = [
    "InflatedBalloon",
    "InflatedHeartBalloon",
    "ChristmasBox",
    "ChristmasBoxManager",
    "RPGRocket",
    "RPGRocketEgg",
    "RPGRocketSpear",
    "item_randombox_base",
    "StickyAnchor",
    "SpawnableZipline",
    "NetLootSpawnGroup",
    "Vehicle_Buggy",
    "NetPlayer",
    "NetSpectator",
    "TeleportationManager",
    "FlareGunProjectile",
    "AutoDestroyItem_DeadBody_Poop",
    "AutoDestroyItem_Splash (0)",
    "AutoDestroyItem_Splash (1)",
    "AutoDestroyItem_Splash (2)",
    "AutoDestroyItem_Splash (3)",
    "AutoDestroyItem_Splash (4)",
    "AutoDestroyItem_Splash (5)",
    "SpaceshipTeleporter",
    "MazeManager",
    "ThunderController",
    "SlenderMonster",
    "Explosion_AntiGravity",
    "Explosion_Bomb",
    "Explosion_Bomber",
    "Explosion_Bomber_Flashbang",
    "Explosion_BroccoliGrenade",
    "Explosion_BroccoliShrinkGrenade",
    "Explosion_ClusterGrenade",
    "Explosion_Dynamite",
    "Explosion_Egg",
    "Explosion_EggCluster",
    "Explosion_EggSubCluster",
    "Explosion_Flashbang",
    "Explosion_FlyingSwarm",
    "Explosion_Grenade",
    "Explosion_Heart_Gun",
    "Explosion_ImpulseGrenade",
    "Explosion_Landmine",
    "Explosion_RPG",
    "Explosion_RPG_Spear",
    "Explosion_SellingMachine",
    "Explosion_StashGrenade",
    "Explosion_TeleGrenade",
    "Explosion_Tripwire",
    "Explosion_Vehicle",
    "ExplosiveEgg",
    "ExplosiveEggClustered",
    "ItemSellingMachineController",
    "Landmine",
    "LeaderBoardMonsterKill",
    "GiantRockObject",
    "HordeMobController",
    "HordeMobLobbyHandler",
    "LongAisleController",
    "Duplicator",
    "HH_LockedDoor",
    "HingedDoorNetworked",
    "ScaffoldTrap",
    "Pillar_Arched_Broken_01",
    "RuinTower_FloatingPlatform",
    "RuinTower_FloatingSmall",
];
const mobIDs = [
    "AnglerController",
    "AnglerMadController",
    "ArmstrongController",
    "ArmstrongMadController",
    "BansheeController",
    "BombController",
    "BomberController",
    "BomberFlashbangController",
    "BomberMadController",
    "ChickenController",
    "CystController",
    "EvilEyeController",
    "EvilEyePinataController",
    "EvilEyePinataLargeController",
    "FakeGorillaController",
    "FlyingSwarmController",
    "GiantController",
    "NextBotController",
    "NextBotStaticController",
    "PhantomController",
    "RedGreenController",
    "RedGreenMadController",
    "SegwayController",
    "SpiderController",
    "PrototypeSlenderController",
];
const VFXTypes = {
    None: 255,
    MuzzleFlash_Shotgun: 0,
    MuzzleFlash_FlareGun: 1,
    CrateBreak: 2,
    MuzzleFlash_SmallGun: 3,
    MuzzleFlash_GoldRevolver: 4,
    MuzzleFlash_DragonPistol: 5,
    MuzzleFlash_ViperShotgun: 6,
    Explosion_FlareGun: 32,
    Explosion_Coins: 33,
    Explosion_Nuts: 34,
    Explosion_Keys: 35,
    Explosion_Balloon: 36,
    Explosion_TeleGrenadeSrc: 37,
    Player_Touch_Lava: 38,
    Portal_Teleport: 39,
    Explosion_Coins_Vertical: 40,
    Autumn_Leaves_Burst: 41,
    Explosion_Feathers: 42,
    Explosion_Popcorn: 43,
    Electricity_Small: 44,
    Impact_Flaregun: 64,
    Impact_Snowball: 65,
    Impact_GoldRevolver: 66,
    Impact_MeleeHit: 67,
    Impact_BigGroundHit: 68,
    Impact_MeleeHit_CriticalSmall: 69,
    Impact_MeleeHit_CriticalLarge: 70,
    Impact_MeleeHit_AoE: 71,
    Research_ZiplineAttachDetach: 96,
    Research_Purchase1RP: 97,
    Research_Purchase5RP: 98,
    Research_Purchase10RP: 99,
    Research_PurchaseRPBundle: 100,
    Rope_ZiplineAttachDetach: 110,
    MeatExplosion_1: 128,
    MeatExplosion_2: 129,
    MeatExplosion_Headshot: 130,
    ServerRoomSplash_Small: 160,
    ServerRoomSplash_Big: 161,
    RAMActivationSparks: 162,
    GreenBlink: 170,
    ConfettiBurst: 174,
    Ethereal_Void: 180,
    MomBoss_NailBreak: 181,
    MidAirJump_Fart: 182,
    FuelExplosion: 183
};
const version = "";
const menuName = "Pizza Menu" + version;
let menu = null;
let reference = null;
let referenceCollider = null;
let buttonClickDelay = 0.0;
let LerpMenu = false;
let vfxNames = Object.keys(VFXTypes); // This pulls ["None", "MuzzleFlash_Shotgun", ...]
let vfxSelectorIndex = 0;
let menuscale = 1.0;
let righthand = false;
let deltaTime = 0.0;
let lastHeldID = "item_apple"; // Default fallback
let time = 0.0;
let stashDupeEnabled = false;
let stashDupe = false;
let itemIndex = 0;
let itemGunDelay = 0;
let mobIndex = 0;
let machineAvatar = null;
let prefabIndex = 0;
let mobGunDelay2 = 0;
let bgColor = [0.25, 0.41, 0.88, 1.0]; // dark bg
let textColor = [0.75, 0.94, 1.0, 1.0]; // #3682fc blue text
let buttonColor = [0.0, 0.0, 1.0, 1.0]; // dark button
let buttonPressedColor = [0.3, 0.7, 1.0, 1.0];
let currentNotification = "";
let notifactionResetTime = 0;
let currentCategory = 0;
let currentPage = 0;
let hueVal = 0;
let satVal = 0;
let orbiters = [];
let orbitprefabs = [];
let tagGunDelay = 0;
let frameCount = 0;
let noclipFlyEnabled = false;
let flySpeeds = [10.0, 25.0, 60.0, 150.0]; // Slow, Med, Fast, Nuclear
let flySpeedIndex = 1; // Default to Medium
let flySpeedMult = 10.0;
let noclipEnabled = false;
let platformsEnabled = false;
let leftPlatform = null;
let rightPlatform = null;
let leftPlatformLatched = false;
let rightPlatformLatched = false;
let preUhhFlyUseGravity = true;
let preUhhFlyGravityValid = false;
let flySpeed = 20;
let controlledPrefab = null;
let prefabTargetPos = null;
let leftPrimary = false;
let leftSecondary = false;
let rightPrimary = false;
let rightSecondary = false;
let leftGrab = false;
let rightGrab = false;
let leftTrigger = false;
let rightTrigger = false;
let vfxIndex = 32; // Default explosion ID
let vfxGunDelay = 0;
let leftStick = false;
let rightStick = false;
let InfAmmo = false;
let isAdmin = false;
let ejectDupeAmount = 2;
let ejectDupeIndex = 0;
let noRecoil = false;
let infDamage = false;
const ejectDupeValues = [1, 2, 5, 10, 20, 30, 50, 100];
let scaleVal = 0;
let cachedItems = null;
let backpackDupe = false;
let lagGunDelay = 0;
let controlledMachine = null;
let machineTargetPos = null;
let mobGunDelay = 0;
let idGunDelay = 0;
let whitelist = [];
let netplrs = [];
let whitelistDisintegrateDelays = {};
let soundFiles = []; // List of .wav names
let currentSoundIdx = 0;
let photonRecorder = null;
let whitelistGunRe = false;
let whitelistOrbitData = new Map(); // Key: PlayerID, Value: { prefabs: [], orbData: [] }
let lastOrbitPrefab = "";
let sellingmachineSpawns = [];
let presentSpawns = [];
let heldMobObjects = [];
let heldPrefabObjects = [];
const QUIVER_ITEM_LIMIT = 15;
const GIVEAWAY_BAG_ITEM_LIMIT = 20;
const TELEPORT_TO_LAKE = [-213.170, 56.764, -15.242];
const TELEPORT_TO_MOON = [1021.538, 980.105, 1054.145];
const TELEPORT_TO_SEWERS = [88.541, -103.024, 140.867];
const TELEPORT_TO_ABOVELAKE = [-103.231, 4.467, -3.131];
const TELEPORT_TO_SPAWN = [-6.182, 4.601, 14.069];
const TELEPORT_TO_WATERTOWER = [49.446, 50.186, -33.340];
const ORE_ITEM_IDS = [
    "item_upsidedown_loot",
    "item_uranium_chunk_l",
    "item_uranium_chunk_m",
    "item_uranium_chunk_s",
    "item_ore_copper_l",
    "item_ore_copper_m",
    "item_ore_copper_s",
    "item_ore_gold_l",
    "item_ore_gold_m",
    "item_ore_gold_s",
    "item_ore_hell",
    "item_ore_silver_l",
    "item_ore_silver_m",
    "item_ore_silver_s"
];
const FLARE_DELAY_MODES = [
    { label: "Zero", delay: 0.0 },
    { label: "Half", delay: 0.075 },
    { label: "Normal", delay: 0.15 }
];
let flareDelayModeIndex = 2;
let orbitPrefabName = "";
let followedPrefabObject = null;
let followedPrefabTransform = null;
let soundboardSource = null; // Set this during your 'prepare' function
let followedPrefabName = "";
let zeroCooldown = false;
let followedMobObject = null;
let noclipCooldown = 0;
let noclipModMaster = false; // Is the mod turned on in the menu?
let isCurrentlyNoclipping = false; // Are we physically passing through walls right now?
let noclipHotkeyCooldown = 0.0;
let followedMobTransform = null;
let followedMobName = "";
let crossbowDupeLastItemID = null;
let crossbowDupeLastHue = 0;
let crossbowDupeLastSat = 0;
let crossbowDupeLastScale = 0;
let crossbowDupeNextReloadTime = 0;

let devModeHookSet = false;
let GunPointer = null;
// --- SFX Data from your snippet ---
let sfxIndex = 7; // Default: BasicPistol
const sfxNameMap = {
    0: "none", 1: "Arena_Defeat", 2: "Arena_HitPlayer", 3: "Arena_KillPlayer", 4: "Arena_Victory",
    5: "Barrel_Beans_Explode", 6: "Barrel_Oil_Explode", 7: "BasicPistol", 8: "Big_Shark_Roar",
    9: "blackhole_close", 10: "blackhole_open", 11: "bloody", 12: "Bonfire_Ignite",
    13: "Bonfire_Ignite_loud", 37: "CoinEarn", 68: "Explosion_Dynamite",
    107: "HitSound_Boing", 162: "Item_heart_gun_shoot", 258: "Lightning_Normal",
};

// Create a list of the IDs so we can cycle through them easily
const sfxIDList = Object.keys(sfxNameMap).map(Number);
let sfxListPointer = 7; // Start at BasicPistol (which is at index 7 in the map keys)
let explosionTypes = [
    "Explosion_AntiGravity", "Explosion_Bomb", "Explosion_Bomber", "Explosion_Bomber_Flashbang",
    "Explosion_BroccoliGrenade", "Explosion_BroccoliShrinkGrenade", "Explosion_ClusterGrenade",
    "Explosion_Dynamite", "Explosion_Egg", "Explosion_EggCluster", "Explosion_EggSubCluster",
    "Explosion_Flashbang", "Explosion_FlyingSwarm", "Explosion_Grenade", "Explosion_Heart_Gun",
    "Explosion_ImpulseGrenade", "Explosion_Landmine", "Explosion_RPG", "Explosion_RPG_Spear",
    "Explosion_SellingMachine", "Explosion_StashGrenade", "Explosion_TeleGrenade", 
    "Explosion_Tripwire", "Explosion_Vehicle"
];
let explosionIdx = 0;
let _selfRPCBypass = false;
let GunLine = null;
let gunColor = [0.08, 0.08, 0.08, 0.75];
// HELPER: Use this to run your own mods so they don't get blocked by the shield
function selfRPC(fn) {
    _selfRPCBypass = true;
    try {
        return fn();
    } finally {
        _selfRPCBypass = false;
    }
}
let soundFileIndex = 0;
let previousSoundKey = false;
const loadedBundles = {};
const loadedObjects = {};
console.log("Waiting 5 seconds for game to stabilize.../my menu yes");
const TopBanner = `  my menu yess                                                
`;
setTimeout(() => {
Il2Cpp.perform(() => {
    function findClass(image, fullName) {
    const klass = image.class(fullName);
    if (!klass || klass.handle.isNull()) {
        console.log(`[WARNING] Missing class: ${fullName}`);
        return null;
    }
    return klass;
}
        // --- MASTER HELPERS ---

    function getHandInteractor(player, handIndex) {
        try {
            if (!player || player.handle.isNull()) return null;
            // Try different possible method/field names for the interactor
            const getter = tryMethod(player, "GetHandInteractor", 1);
            if (getter) return getter.invoke(handIndex);
            
            const fieldName = (handIndex === 0) ? "_leftHandInteractor" : "_rightHandInteractor";
            const val = player.field(fieldName).value;
            return (val && !val.handle.isNull()) ? val : null;
        } catch (e) { return null; }
    }

    function spawnMobAtPos(mobEntry, pos, rot) {
    try {
        const mobID = (typeof mobEntry === "string") ? mobEntry : (mobEntry?.id ?? mobEntry?.name ?? null);
        if (!mobID) {
            console.log("[SPAWNER] Missing mob ID");
            return;
        }
        try {
            const result = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("mob_prefab/" + mobID), pos, rot, NULL);
            if (result && !result.isNull()) return result;
        } catch (_) { }

        const PG = AssemblyCSharp.class("AnimalCompany.PrefabGenerator");
        const inst = PG.field("_instance").value;
        if (!inst || inst.isNull()) return null;

        const spawnMethod = tryMethod(inst, "SpawnMobAsync", 6) ||
                            tryMethod(inst, "SpawnMob", 6) ||
                            tryMethod(inst, "SpawnMobInternal", 6);

        if (spawnMethod) {
            spawnMethod.invoke(mobID, pos, rot, NULL, NULL, Il2Cpp.string("mod_menu"));
            return true;
        }
        console.log("[SPAWNER] Could not find a valid Mob Spawn method.");
    } catch (e) {
        console.error("spawnMobAtPos Error: " + e);
    }
    return null;
}

    // This replaces the crashing FindObjectOfType
    function findActiveObject(klass) {
        try {
            const all = Object.method("FindObjectsByType", 1).inflate(klass).invoke(0);
            return (all && all.length > 0) ? all.get(0) : null;
        } catch (e) { return null; }
    }
    // This tracks the last item you physically dropped from your hand
try {
    const AnchorClass = AssemblyCSharp.class("AnimalCompany.AttachedItemAnchor");
    const dropHook = AnchorClass.method("RPC_DropObject");
    
    dropHook.implementation = function(id, pos, rot, vel, ang, death) {
        let droppedGbo = null;
        try {
            // Check if the hand dropping the item is OURS
            if (this.method("get_IsMine").invoke()) {
                const gbo = getComponentInParent(this, GBOClass);
                if (gbo && !gbo.handle.isNull()) {
                    droppedGbo = gbo;
                    const gbi = gbo.method("GetComponent", 1).inflate(GBIClass).invoke();
                    const idStr = gbi.method("get_itemID").invoke()?.content;
                    if (idStr) {
                        lastHeldID = idStr; // Save it to memory!
                        console.log("[TRACKER] Last held item updated: " + idStr);
                    }
                }
            }
        } catch (e) { }
        const result = dropHook.invoke(this, id, pos, rot, vel, ang, death);
        try {
            if (droppedGbo && !droppedGbo.isNull()) {
                const rb = getRigidbodySafe(droppedGbo);
                if (rb && !rb.isNull()) {
                    try { rb.method("set_isKinematic").invoke(false); } catch (_) { }
                    try { rb.method("WakeUp").invoke(); } catch (_) { }
                    try { rb.method("set_linearVelocity").invoke(vel); } catch (_) { }
                    try { rb.method("set_angularVelocity").invoke(ang); } catch (_) { }
                }
            }
        } catch (_) { }
        return result;
    };
} catch (e) { console.log("Tracker hook failed: " + e); }
    // ---- Assemblies ----
    const images = {
        "AnimalCompany": Il2Cpp.domain.assembly("AnimalCompany").image,
        "UnityEngine.CoreModule": Il2Cpp.domain.assembly("UnityEngine.CoreModule").image,
        "UnityEngine.PhysicsModule": Il2Cpp.domain.assembly("UnityEngine.PhysicsModule").image,
        "UnityEngine.UIModule": Il2Cpp.domain.assembly("UnityEngine.UIModule").image,
        "UnityEngine.UI": Il2Cpp.domain.assembly("UnityEngine.UI").image,
        "UnityEngine.TextRenderingModule": Il2Cpp.domain.assembly("UnityEngine.TextRenderingModule").image,
        "PhotonFusionNetworking": Il2Cpp.domain.assembly("Fusion.Runtime").image,
        "PhotonFusionNetworkingRealtime": Il2Cpp.domain.assembly("Fusion.Realtime").image,
        "Unity.TextMeshPro": Il2Cpp.domain.assembly("Unity.TextMeshPro").image,
        "UnityEngine.XRModule": Il2Cpp.domain.assembly("UnityEngine.XRModule").image,
        "UnityEngine.AudioModule": Il2Cpp.domain.assembly("UnityEngine.AudioModule").image,
        "Oculus.Platform": Il2Cpp.domain.assembly("Oculus.Platform").image,
        "Nakama": Il2Cpp.domain.assembly("Nakama").image,
    };
    const AssemblyCSharp = images["AnimalCompany"];
    const Nakama = images["Nakama"];
    const UnityEngineCore = images["UnityEngine.CoreModule"];
    const UnityEnginePhysics = images["UnityEngine.PhysicsModule"];
    const UnityEngineUI = images["UnityEngine.UI"];
    const UnityEngineUIModule = images["UnityEngine.UIModule"];
    const UnityEngineTextRendering = images["UnityEngine.TextRenderingModule"];
    const PhotonFusionNetworking = images["PhotonFusionNetworking"];
    const UnityTextMeshPro = images["Unity.TextMeshPro"];
    const UnityEngineXR = images["UnityEngine.XRModule"];
    const UnityEngineAudio = images["UnityEngine.AudioModule"];
    const OculusPlatformSettings = images["Oculus.Platform"].class("Oculus.Platform.PlatformSettings");
    // ---- Classes ----
    const GTPlayerClass = AssemblyCSharp.class("AnimalCompany.GorillaLocomotion");
    const NetworkedLootManagerClass = AssemblyCSharp.class("AnimalCompany.NetworkedLootManager");
    let lootManagerInstance = null;
    const NetPlayer = AssemblyCSharp.class("AnimalCompany.NetPlayer");
    const MobileState = AssemblyCSharp.class("AnimalCompany.MobileState");
    //const DevMenuButton         = AssemblyCSharp.class("AnimalCompany.DevMenuButton");
    const PlayerController = AssemblyCSharp.class("AnimalCompany.PlayerController");
    const UserState = AssemblyCSharp.class("AnimalCompany.UserState");
    const ItemVendingMachineView = AssemblyCSharp.class("AnimalCompany.ItemVendingMachineView");
    const ElevatorManager = AssemblyCSharp.class("AnimalCompany.ElevatorManager");
    const ArenaGameManager = AssemblyCSharp.class("AnimalCompany.ArenaGameManager");
    const GrabbableObject = AssemblyCSharp.class("AnimalCompany.GrabbableObject");
    const ItemSellingMachineController = AssemblyCSharp.class("AnimalCompany.ItemSellingMachineController");
    const PrefabGen = AssemblyCSharp.class("AnimalCompany.PrefabGenerator");
    const GBIClass = AssemblyCSharp.class("AnimalCompany.GrabbableItem");
    const BackpackItemClass = AssemblyCSharp.class("AnimalCompany.BackpackItem");
    // Replace with your actual folder path
    const soundsPath = "/storage/emulated/0/Download/sounds/"; 
    const QuiverClass = AssemblyCSharp.class("AnimalCompany.Quiver");
    let CrossbowClass = null;
    try {
        CrossbowClass = AssemblyCSharp.class("AnimalCompany.Crossbow");
    }
    catch (_) { }
    let HeartGunClass = null;
    try {
        HeartGunClass = AssemblyCSharp.class("AnimalCompany.HeartGun");
    }
    catch (_) { }
    let GrenadeLauncherClass = null;
    try {
        GrenadeLauncherClass = AssemblyCSharp.class("AnimalCompany.GrenadeLauncher");
    }
    catch (_) { }
    let SalmonCannonClass = null;
    try {
        SalmonCannonClass = AssemblyCSharp.class("AnimalCompany.SalmonCannon");
    }
    catch (_) { }
    let MobControllerBase = null;
    try {
        MobControllerBase = AssemblyCSharp.class("AnimalCompany.MobController");
    }
    catch (_) { }
    const PCClass = AssemblyCSharp.class("AnimalCompany.PlayerController");
    let PlayerWatchClass = null;
    try { PlayerWatchClass = AssemblyCSharp.class("AnimalCompany.PlayerWatch"); } catch (_) { }
    let HandInteractorClass = null;
    try { HandInteractorClass = AssemblyCSharp.class("AnimalCompany.HandInteractor"); } catch (_) { }
    let NetPlayerColliderClass = null;
    try { NetPlayerColliderClass = AssemblyCSharp.class("AnimalCompany.NetPlayerCollider"); } catch (_) { }
    let NetRigTransformClass = null;
    try { NetRigTransformClass = AssemblyCSharp.class("AnimalCompany.NetRigTransform"); } catch (_) { }
    const GBOClass = AssemblyCSharp.class("AnimalCompany.GrabbableObject");
    const SFXManager = AssemblyCSharp.class("AnimalCompany.SFXManager");
    const NManager = AssemblyCSharp.class("AnimalCompany.NetworkManager");
    const GorillaReportButton = AssemblyCSharp.class("AnimalCompany.ComputerTerminalKey");
    const InputDevices = UnityEngineXR.class("UnityEngine.XR.InputDevices");
    const CommonUsages = UnityEngineXR.class("UnityEngine.XR.CommonUsages");
    const GameObject = UnityEngineCore.class("UnityEngine.GameObject");
    const Object = UnityEngineCore.class("UnityEngine.Object");
    const Vector3 = UnityEngineCore.class("UnityEngine.Vector3");
    const Quaternion = UnityEngineCore.class("UnityEngine.Quaternion");
    const Time = UnityEngineCore.class("UnityEngine.Time");
    const Resources = UnityEngineCore.class("UnityEngine.Resources");
    const Material = UnityEngineCore.class("UnityEngine.Material");
    const Renderer = UnityEngineCore.class("UnityEngine.Renderer");
    const Shader = UnityEngineCore.class("UnityEngine.Shader");
    const RectTransform = UnityEngineCore.class("UnityEngine.RectTransform");
    const LineRenderer = UnityEngineCore.class("UnityEngine.LineRenderer");
    const BoxCollider = UnityEnginePhysics.class("UnityEngine.BoxCollider");
    const Collider = UnityEnginePhysics.class("UnityEngine.Collider");
    const Rigidbody = UnityEnginePhysics.class("UnityEngine.Rigidbody");
    const Physics = UnityEnginePhysics.class("UnityEngine.Physics");
    const Component = UnityEngineCore.class("UnityEngine.Component");
    const ParticleManager = AssemblyCSharp.class("AnimalCompany.ParticleManager");
    const ParticleManagerClass = ParticleManager;
    const Canvas = UnityEngineUIModule.class("UnityEngine.Canvas");
    const CanvasScaler = UnityEngineUI.class("UnityEngine.UI.CanvasScaler");
    const GraphicRaycaster = UnityEngineUI.class("UnityEngine.UI.GraphicRaycaster");
    const Text = UnityEngineUI.class("UnityEngine.UI.Text");
    const Font = UnityEngineTextRendering.class("UnityEngine.Font");
    //const NakamaSession = Nakama ? Nakama.class("Nakama.Session") : null;
    // ---- Core instances ----
    const GTPlayer = GTPlayerClass.field("<Instance>k__BackingField").value;
    const UberShader = Shader.method("Find").invoke(Il2Cpp.string("Universal Render Pipeline/Unlit"));
    const TextShader = Shader.method("Find").invoke(Il2Cpp.string("UI/Default"));
    const zeroVector = Vector3.field("zeroVector").value;
    const oneVector = Vector3.field("oneVector").value;
    const identityQuaternion = Quaternion.field("identityQuaternion").value;
    const leftHandTransform = GTPlayer.field("leftHandTransform").value;
    const rightHandTransform = GTPlayer.field("rightHandTransform").value;
    // Replacement logic for the Loop inside Kill All Players:
const allPlayers = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
for (let i = 0; i < allPlayers.length; i++) {
    const target = allPlayers.get(i);
    if (!target || target.handle.isNull() || playerIsLocal(target)) continue;
    
    // ... rest of your kill logic (RPC_DoPlayerDie) ...
}
    const headCollider = GTPlayer.field("headCollider").value;
    const bodyCollider = GTPlayer.field("bodyCollider").value;
    const arial = Resources.method("GetBuiltinResource", 2).invoke(Font.type.object, Il2Cpp.string("Arial.ttf"));
    // ---- Helpers ----
    function Destroy(object) { Object.method("Destroy", 1).invoke(object); }
    function getComponent(obj, type) { return obj.method("GetComponent", 1).inflate(type).invoke(); }
    function getComponentInParent(obj, type) { return obj.method("GetComponentInParent", 0).inflate(type).invoke(); }
    function addComponent(obj, type) { return obj.method("AddComponent", 1).inflate(type).invoke(); }
    function getTransform(obj) { return obj.method("get_transform").invoke(); }
    function playerIsLocal(player) { return player.method("get_IsMine").invoke(); }
    function getNetworkObjectSafe(target) {
        if (!target || target.isNull()) return null;
        try {
            if (target.class && target.class.name === "NetworkObject") return target;
        } catch (_) { }
        const getters = [
            () => target.method("get_Object").invoke(),
            () => target.method("get_NetworkObject").invoke(),
            () => target.method("get_networkObject").invoke(),
            () => target.method("GetComponent", 1).inflate(Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkObject")).invoke()
        ];
        for (const getter of getters) {
            try {
                const obj = getter();
                if (obj && !obj.isNull()) return obj;
            } catch (_) { }
        }
        try {
            const go = target.method("get_gameObject").invoke();
            if (go && !go.isNull()) {
                const obj = go.method("GetComponent", 1).inflate(Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkObject")).invoke();
                if (obj && !obj.isNull()) return obj;
            }
        } catch (_) { }
        return null;
    }
    function getLocalNetPlayer() {
        try {
            const player = NetPlayer.method("get_localPlayer").invoke();
            return (player && !player.isNull()) ? player : null;
        } catch (_) {
            return null;
        }
    }
    const NetworkRunner = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkRunner");
    const NULL = Il2Cpp.reference(Il2Cpp.domain.assembly("mscorlib").image.class("System.Object").alloc());
    let cachedSpawnPrefab = null;
        // --- CORE MOD HELPERS (PASTE AT TOP OF IL2CPP.PERFORM) ---

    function blockrpc() {
        try {
            const NetPlayerCls = AssemblyCSharp.class("AnimalCompany.NetPlayer");
            // List of RPCs to block on ourselves
            const hostile = ["RPC_AddForce", "RPC_Teleport", "RPC_PlayerStun", "RPC_TagAsStinky", "RPC_PlayerHit"];
            hostile.forEach(rpc => {
                const m = NetPlayerCls.method(rpc);
                if (m) {
                    m.implementation = function (...args) {
                        if (this.method("get_IsMine").invoke() && !_selfRPCBypass) return;
                        return this.method(rpc).invoke(...args);
                    };
                }
            });
            console.log("[SHIELD] blockrpc installed");
        } catch (e) { console.error("blockrpc error: " + e); }
    }

    // Crucial for the launchers we just made!
    function tryMethod(target, name, parameterCount) {
        try {
            if (!target || target.handle.isNull()) return null;
            return parameterCount == null ? target.method(name) : target.method(name, parameterCount);
        } catch (_) { return null; }
    }
    function randInt(min, max) {
        return Math.floor(Math.random() * (max - min + 1)) + min;
    }
    function dumpNamedList(title, values) {
        console.log("\n========== " + title + " ==========");
        for (let i = 0; i < values.length; i++) {
            console.log("[" + i + "] " + values[i]);
        }
        console.log("========== TOTAL: " + values.length + " ==========\n");
    }
    function dumpClassMembers(title, klass) {
        if (!klass || klass.handle.isNull()) {
            console.log("[DUMP] Missing class for " + title);
            return;
        }
        console.log("\n========== " + title + " ==========");
        console.log("[CLASS] " + klass.fullName);
        console.log("-- Fields --");
        klass.fields.forEach(f => {
            try { console.log("  - " + f.name + " : " + f.type.name); } catch (_) { }
        });
        console.log("-- Methods --");
        klass.methods.forEach(m => {
            try {
                const params = m.parameters.map(p => p.type.name + " " + p.name).join(", ");
                console.log("  - " + m.name + "(" + params + ")");
            } catch (_) { }
        });
        console.log("========== END " + title + " ==========\n");
    }
    function refreshSoundList() {
    try {
        const DirectoryClass = Il2Cpp.domain.assembly("mscorlib").image.class("System.IO.Directory");
        
        // Let's try to find any file in that folder to test pathing
        const getFiles = DirectoryClass.method("GetFiles", 1); // (path)
        const rawFiles = getFiles.invoke(Il2Cpp.string(soundsPath));

        if (!rawFiles || rawFiles.handle.isNull() || rawFiles.length === 0) {
            console.log("[SOUNDBOARD] No files found in " + soundsPath);
            return;
        }

        soundFiles = [];
        for (let i = 0; i < rawFiles.length; i++) {
            const fullPath = rawFiles.get(i).toString();
            // Only add if it ends in .wav (case insensitive)
            if (fullPath.toLowerCase().endsWith(".wav")) {
                const name = fullPath.substring(fullPath.lastIndexOf('/') + 1);
                soundFiles.push(name);
            }
        }
        
        console.log(`[SOUNDBOARD] Success! Found ${soundFiles.length} wav files.`);
        sendNotification(`Loaded ${soundFiles.length} sounds.`, false);
    } catch (e) {
        console.log("[SOUNDBOARD] Folder Error: " + e);
    }
}
function prepareSoundboard() {
    try {
        const RecorderClass = Il2Cpp.domain.assembly("PhotonVoice").image.class("Photon.Voice.Unity.Recorder");
        const allRecorders = Resources.method("FindObjectsOfTypeAll", 1).inflate(RecorderClass).invoke();

        if (allRecorders && allRecorders.length > 0) {
            photonRecorder = allRecorders.get(0);
            
            // Fix: Use your script's current player instance variable (GTPlayer)
            const player = GTPlayer || getLivePlayer(); 
            if (player) {
                const go = player.method("get_gameObject").invoke();
                soundboardSource = addComponent(go, UnityEngineAudio.class("UnityEngine.AudioSource"));
                
                soundboardSource.method("set_playOnAwake").invoke(false);
                soundboardSource.method("set_spatialBlend").invoke(0.0);
                
                console.log("[SOUNDBOARD] Ready to Hijack.");
            }
        }
    } catch (e) { console.log("[SOUNDBOARD] Prepare Error: " + e); }
}

function playCustomWav(fileName) {
    try {
        // 1. Construct the local file URL
        // Example: "file:///sdcard/Android/data/com.Thunda.H4kTag/files/sounds/test.wav"
        const url = "file://" + soundsPath + fileName;

        // 2. Resolve the Unity Web Request Class
        const UWRModule = Il2Cpp.domain.assembly("UnityEngine.UnityWebRequestMultimediaModule").image;
        const UWR = UWRModule.class("UnityEngine.Networking.UnityWebRequestMultimedia");

        // 3. Create the request: GetAudioClip(string url, AudioType type)
        // AudioType 20 = WAV
        const request = UWR.method("GetAudioClip", 2).invoke(Il2Cpp.string(url), 20);

        // 4. Send the request to the game engine
        const operation = request.method("SendWebRequest").invoke();

        // 5. ASYNC WAIT: We give the Quest 200ms to read the file from the SSD
        setTimeout(() => {
            try {
                // Check if the request finished successfully
                const downloadHandler = request.method("get_downloadHandler").invoke();
                if (!downloadHandler || downloadHandler.isNull()) return;

                // Extract the physical AudioClip from the download handler
                const clip = downloadHandler.method("get_audioClip").invoke();

                if (clip && !clip.isNull()) {
                    // Assign the loaded sound to our hijacked AudioSource
                    soundboardSource.method("set_clip").invoke(clip);
                    
                    // PLAY! (Since SourceType is 1, this goes to your mic)
                    soundboardSource.method("Play").invoke();
                    
                    sendNotification("Playing: " + fileName, false, 3);
                    console.log("[SOUNDBOARD] Successfully broadcasting: " + fileName);
                } else {
                    console.log("[SOUNDBOARD] Failed to convert file to AudioClip.");
                }
            } catch (innerE) {
                console.error("Audio Clip Extraction Error: " + innerE);
            }
        }, 200); // 200ms is usually enough for local files

    } catch (e) {
        console.error("playCustomWav Critical Error: " + e);
    }
}
    const blockedContainerItemIDs = {
        "item_crate": true,
    };
    function getQuiverFillPool() {
        return itemIDs.filter(id => !blockedContainerItemIDs[id]);
    }
    function getCurrentFlareDelay() {
        return FLARE_DELAY_MODES[flareDelayModeIndex].delay;
    }
    function cycleFlareDelayMode() {
        flareDelayModeIndex = (flareDelayModeIndex + 1) % FLARE_DELAY_MODES.length;
        const mode = FLARE_DELAY_MODES[flareDelayModeIndex];
        sendNotification("Flare delay: " + mode.label + " (" + mode.delay + "s)", false, 2);
    }
    function spawnItemPrefab(itemID, pos, rot) {
        try {
            return PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + itemID), pos, rot, NULL);
        } catch (e) {
            console.error("spawnItemPrefab error: " + e);
            return null;
        }
    }
    function getContainerComponent(go) {
        if (!go || go.isNull()) return null;
        const tryTypes = [QuiverClass, BackpackItemClass, CrossbowClass, HeartGunClass, GrenadeLauncherClass, SalmonCannonClass];
        for (const type of tryTypes) {
            if (!type) continue;
            try {
                const direct = go.method("GetComponent", 1).inflate(type).invoke();
                if (direct && !direct.isNull()) return direct;
            } catch (_) { }
            try {
                const child = go.method("GetComponentInChildren", 1).inflate(type).invoke();
                if (child && !child.isNull()) return child;
            } catch (_) { }
        }
        return null;
    }
    function setContainerCapacity(container, capacity) {
        if (!container || container.isNull()) return;
        try { container.field("_capacity").value = capacity; } catch (_) { }
        try { container.field("capacity").value = capacity; } catch (_) { }
        try { container.method("set_capacity").invoke(capacity); } catch (_) { }
    }
    function setContainerOpen(container, isOpen) {
        if (!container || container.isNull()) return;
        try {
            const getter = tryMethod(container, "get_isOpen", 0);
            if (getter) {
                const currentlyOpen = getter.invoke();
                if (!!currentlyOpen === !!isOpen) return;
            }
        } catch (_) { }
        try {
            const setter = tryMethod(container, "set_isOpen", 1) || tryMethod(container, "set_open", 1);
            if (setter) {
                setter.invoke(!!isOpen);
                return;
            }
        } catch (_) { }
        try {
            const fn = isOpen ? tryMethod(container, "Open", 0) : tryMethod(container, "Close", 0);
            if (fn) {
                fn.invoke();
                return;
            }
        } catch (_) { }
        try {
            const toggle = tryMethod(container, "RPC_ToggleOnOff", 0);
            if (toggle) toggle.invoke();
        } catch (_) { }
    }
    function tryAddItemToContainer(container, itemObj) {
        if (!container || container.isNull() || !itemObj || itemObj.isNull()) return false;
        requestStateAuthoritySafe(container);
        requestStateAuthoritySafe(itemObj);
        const itemGbi = (() => {
            try { return itemObj.method("GetComponent", 1).inflate(GBIClass).invoke(); } catch (_) { return null; }
        })();
        const actions = [
            () => itemGbi && !itemGbi.isNull() && itemGbi.method("AddToBag").invoke(container),
            () => itemGbi && !itemGbi.isNull() && itemGbi.method("AddToBagInternal").invoke(container),
            () => itemGbi && !itemGbi.isNull() && itemGbi.method("AddToQuiver").invoke(container),
            () => container.method("TryAddItem").invoke(itemObj)
        ];
        for (const action of actions) {
            try {
                const result = action();
                if (result !== false) return true;
            } catch (_) { }
        }
        return false;
    }
    function getCurrentRunnerSafe() {
        const getters = [
            () => SFXManager.field("_instance").value.method("get__currentRunner").invoke(),
            () => PrefabGen.field("_instance").value.method("get_runner").invoke(),
            () => findActiveObject(NetworkRunner)
        ];
        for (const getter of getters) {
            try {
                const runner = getter();
                if (runner && !runner.isNull()) return runner;
            } catch (_) { }
        }
        return null;
    }
    function requestStateAuthoritySafe(target) {
        if (!target || target.isNull()) return;
        try {
            const netObj = getNetworkObjectSafe(target);
            if (netObj && !netObj.isNull()) {
                netObj.method("RequestStateAuthority").invoke();
            }
        } catch (_) { }
    }
    function forEachPlayerColliderRoot(visitor) {
        const roots = [];
        try { if (GTPlayer && !GTPlayer.isNull()) roots.push(GTPlayer); } catch (_) { }
        try {
            const localPlayer = getLocalNetPlayer();
            if (localPlayer && !localPlayer.isNull()) roots.push(localPlayer);
        } catch (_) { }
        try {
            const pc = PCClass.method("get_instance").invoke();
            if (pc && !pc.isNull()) roots.push(pc);
        } catch (_) { }
        try {
            if (NetPlayerColliderClass) {
                const colliders = Object.method("FindObjectsByType", 1).inflate(NetPlayerColliderClass).invoke(0);
                for (let i = 0; i < colliders.length; i++) {
                    const npc = colliders.get(i);
                    if (npc && !npc.isNull()) roots.push(npc);
                }
            }
        } catch (_) { }
        for (const root of roots) {
            try { visitor(root); } catch (_) { }
        }
    }
    function setPlayerCollidersEnabled(enabled) {
        let touchedAny = false;
        forEachPlayerColliderRoot(root => {
            const allCols = root.method("GetComponentsInChildren", 1).inflate(Collider).invoke(true);
            for (let i = 0; i < allCols.length; i++) {
                try {
                    const col = allCols.get(i);
                    if (!col || col.isNull()) continue;
                    const go = col.method("get_gameObject").invoke();
                    const name = go && !go.isNull() ? go.method("get_name").invoke().toString().toLowerCase() : "";
                    if (name.includes("menu") || name.includes("pointer") || name.includes("watchcanvas")) continue;
                    col.method("set_enabled").invoke(!!enabled);
                    touchedAny = true;
                } catch (_) { }
            }
        });
        if (!touchedAny) {
            try { if (headCollider) headCollider.method("set_enabled").invoke(!!enabled); } catch (_) { }
            try { if (bodyCollider) bodyCollider.method("set_enabled").invoke(!!enabled); } catch (_) { }
        }
    }
    function getGameObjectSafe(target) {
        if (!target || target.isNull()) return null;
        try {
            const go = target.method("get_gameObject").invoke();
            if (go && !go.isNull()) return go;
        } catch (_) { }
        try {
            const netObj = getNetworkObjectSafe(target);
            if (netObj && !netObj.isNull()) {
                const go = netObj.method("get_gameObject").invoke();
                if (go && !go.isNull()) return go;
            }
        } catch (_) { }
        return null;
    }
    function getRigidbodySafe(target) {
        const sources = [target, getGameObjectSafe(target)];
        for (const source of sources) {
            if (!source || source.isNull()) continue;
            try {
                const rb = source.method("GetComponent", 1).inflate(Rigidbody).invoke();
                if (rb && !rb.isNull()) return rb;
            } catch (_) { }
            try {
                const rb = source.method("GetComponentInChildren", 1).inflate(Rigidbody).invoke();
                if (rb && !rb.isNull()) return rb;
            } catch (_) { }
        }
        return null;
    }
    function isPlayerRelatedObject(target) {
        const go = getGameObjectSafe(target);
        if (!go || go.isNull()) return false;
        const checks = [PCClass, NetPlayer, PlayerWatchClass, HandInteractorClass, NetPlayerColliderClass, NetRigTransformClass];
        for (const klass of checks) {
            if (!klass) continue;
            try {
                const hit = go.method("GetComponent", 1).inflate(klass).invoke();
                if (hit && !hit.isNull()) return true;
            } catch (_) { }
            try {
                const hit = go.method("GetComponentInParent", 0).inflate(klass).invoke();
                if (hit && !hit.isNull()) return true;
            } catch (_) { }
        }
        try {
            const name = go.method("get_name").invoke().toString().toLowerCase();
            if (name.includes("watch") || name.includes("netplayer") || name.includes("player") || name.includes("rig")) return true;
        } catch (_) { }
        return false;
    }
    function updateHeldEntries(entries, handTransform, spacingY, forwardOffset = 0.0) {
        if (!entries || entries.length === 0 || !handTransform || handTransform.isNull()) return;
        const handPos = handTransform.method("get_position").invoke();
        const handRot = handTransform.method("get_rotation").invoke();
        const handForward = handTransform.method("get_forward").invoke();
        for (let i = 0; i < entries.length; i++) {
            const entry = entries[i];
            const obj = entry?.obj ?? entry;
            if (!obj || obj.isNull()) continue;
            try {
                requestStateAuthoritySafe(obj);
                const tf = getTransform(obj);
                let targetPos = Vector3.method("op_Addition", 2).invoke(handPos, [0, i * spacingY, 0]);
                if (forwardOffset) {
                    const offset = Vector3.method("op_Multiply", 2).invoke(handForward, forwardOffset);
                    targetPos = Vector3.method("op_Addition", 2).invoke(targetPos, offset);
                }
                tryTeleportMob(obj, targetPos);
                try { tf.method("set_position").invoke(targetPos); } catch (_) { }
                try { tf.method("set_rotation").invoke(handRot); } catch (_) { }
                const rb = getRigidbodySafe(obj);
                if (rb && !rb.isNull()) {
                    try { rb.method("set_isKinematic").invoke(true); } catch (_) { }
                    try { rb.method("set_linearVelocity").invoke(zeroVector); } catch (_) { }
                    try { rb.method("set_angularVelocity").invoke(zeroVector); } catch (_) { }
                }
            } catch (_) { }
        }
    }
    function spawnHeldMobEntries() {
        const entries = [];
        for (let i = 0; i < mobIDs.length; i++) {
            try {
                const spawnPos = Vector3.method("op_Addition", 2).invoke(rightHandTransform.method("get_position").invoke(), [0, i * 1.5, 0]);
                const spawned = spawnMobAtPos(mobIDs[i], spawnPos, rightHandTransform.method("get_rotation").invoke());
                const mob = (spawned && spawned !== true && !spawned.isNull()) ? spawned : findClosestMobToPosition(spawnPos, mobIDs[i]);
                if (mob && !mob.isNull()) entries.push({ obj: mob, spawned: true });
            } catch (_) { }
        }
        return entries;
    }
    function spawnHeldPrefabEntries() {
        const entries = [];
        for (let i = 0; i < prefabList.length; i++) {
            try {
                const spawnPos = Vector3.method("op_Addition", 2).invoke(rightHandTransform.method("get_position").invoke(), [0, i * 0.5, 0]);
                const spawned = spawnNetworkPrefab(prefabList[i], spawnPos, rightHandTransform.method("get_rotation").invoke());
                if (spawned && !spawned.isNull() && !isPlayerRelatedObject(spawned)) entries.push({ obj: spawned, spawned: true });
            } catch (_) { }
        }
        return entries;
    }
    function playSelectedVFXAt(position) {
        try {
            const runner = getCurrentRunnerSafe();
            if (!runner || runner.isNull()) return false;
            try {
                ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(runner, vfxIndex, position, identityQuaternion);
                return true;
            } catch (_) { }
            const pmInst = findActiveObject(ParticleManagerClass);
            if (pmInst && !pmInst.isNull()) {
                requestStateAuthoritySafe(pmInst);
                pmInst.method("RPC_PlayVFX", 4).invoke(runner, vfxIndex, position, identityQuaternion);
                return true;
            }
        } catch (_) { }
        return false;
    }
    function playSelectedSFXAt(position) {
        try {
            const runner = getCurrentRunnerSafe();
            const sfxInst = SFXManager.field("_instance").value || findActiveObject(SFXManager);
            if (!runner || runner.isNull() || !sfxInst || sfxInst.isNull()) return false;
            requestStateAuthoritySafe(sfxInst);
            sfxInst.method("RPC_PlaySFX").invoke(runner, sfxIndex, position, 1.0);
            return true;
        } catch (_) {
            return false;
        }
    }
    function getHeldGrabbableFromHand(handIndex) {
        try {
            const player = getLocalNetPlayer();
            if (!player || player.isNull()) return null;
            const interactor = getHandInteractor(player, handIndex);
            if (!interactor || interactor.isNull()) return null;
            const itemAnchor = interactor.field("_itemAnchor").value;
            if (!itemAnchor || itemAnchor.isNull()) return null;
            const grabbable = itemAnchor.method("get_grabbableObject").invoke();
            return (grabbable && !grabbable.isNull()) ? grabbable : null;
        } catch (_) {
            return null;
        }
    }
    function updateLastHeldIDFromHands() {
        for (const handIndex of [0, 1]) {
            try {
                const grabbable = getHeldGrabbableFromHand(handIndex);
                const itemID = getItemIDFromGrabbable(grabbable);
                if (itemID) {
                    lastHeldID = itemID;
                    return itemID;
                }
            } catch (_) { }
        }
        return lastHeldID;
    }
    function getItemIDFromGrabbable(grabbable) {
        if (!grabbable || grabbable.isNull()) return null;
        try {
            const gbi = grabbable.method("GetComponent", 1).inflate(GBIClass).invoke();
            if (!gbi || gbi.isNull()) return null;
            const idObj = gbi.method("get_itemID").invoke();
            if (!idObj || idObj.isNull()) return null;
            return idObj.content;
        } catch (_) {
            return null;
        }
    }
    function trySetWorldPosition(target, worldPos) {
        try {
            getTransform(target).method("set_position").invoke(worldPos);
            return true;
        } catch (_) {
            return false;
        }
    }
    function tryTeleportGrabbable(target, worldPos) {
        if (!target || target.isNull()) return false;
        requestStateAuthoritySafe(target);
        try {
            target.method("RPC_Teleport").invoke(worldPos);
            return true;
        } catch (_) { }
        return trySetWorldPosition(target, worldPos);
    }
    function tryTeleportNetPlayer(target, worldPos) {
        if (!target || target.isNull()) return false;
        requestStateAuthoritySafe(target);
        try {
            if (playerIsLocal(target)) {
                return !!selfRPC(() => {
                    try {
                        target.method("RPC_Teleport").invoke(world2Player(worldPos));
                        return true;
                    } catch (_) {
                        target.method("RPC_Teleport").invoke(worldPos);
                        return true;
                    }
                });
            }
            try {
                target.method("RPC_Teleport").invoke(worldPos);
                return true;
            } catch (_) { }
            try {
                target.method("RPC_Teleport").invoke(world2Player(worldPos));
                return true;
            } catch (_) { }
        } catch (_) { }
        return trySetWorldPosition(target, worldPos);
    }
    function tryTeleportMob(target, worldPos) {
        if (!target || target.isNull()) return false;
        requestStateAuthoritySafe(target);
        const methodNames = ["RPC_Teleport", "Teleport", "Warp", "MoveTo", "SetPosition"];
        for (const methodName of methodNames) {
            try {
                const method = tryMethod(target, methodName, 1);
                if (!method) continue;
                method.invoke(worldPos);
                return true;
            } catch (_) { }
        }
        return trySetWorldPosition(target, worldPos);
    }
    function getHeldCrossbow() {
        try {
            const player = getLocalNetPlayer();
            if (!player || player.handle.isNull()) return null;
            for (let hand = 0; hand <= 1; hand++) {
                const interactor = getHandInteractor(player, hand);
                if (!interactor || interactor.isNull()) continue;
                const itemAnchor = interactor.field("_itemAnchor").value;
                if (!itemAnchor || itemAnchor.isNull()) continue;
                const grabbable = itemAnchor.method("get_grabbableObject").invoke();
                if (!grabbable || grabbable.isNull()) continue;
                const crossbow = grabbable.method("GetComponent", 1).inflate(CrossbowClass).invoke();
                if (crossbow && !crossbow.isNull()) return crossbow;
            }
        } catch (_) { }
        return null;
    }
    function getCrossbowLoadedObject(crossbow) {
        if (!crossbow || crossbow.isNull()) return null;
        try {
            const anchor = crossbow.field("_attachAnchor").value;
            if (anchor && !anchor.isNull()) {
                const obj = anchor.method("get_grabbableObject").invoke();
                if (obj && !obj.isNull()) return obj;
            }
        } catch (_) { }
        try {
            const attached = crossbow.method("get_attachedObject").invoke();
            if (attached && !attached.isNull()) return attached;
        } catch (_) { }
        return null;
    }
    function tryAttachToAnchor(anchor, itemObj) {
        if (!anchor || anchor.isNull() || !itemObj || itemObj.isNull()) return false;
        const attempts = [
            () => anchor.method("Attach").invoke(itemObj),
            () => anchor.method("TryAttach").invoke(itemObj),
            () => anchor.method("SetAttachedObject").invoke(itemObj),
            () => anchor.method("set_grabbableObject").invoke(itemObj)
        ];
        for (const attempt of attempts) {
            try {
                const result = attempt();
                if (result !== false) return true;
            } catch (_) { }
        }
        return false;
    }
    function tryRefillHeldCrossbow() {
        if (!CrossbowClass || !crossbowDupeLastItemID || time < crossbowDupeNextReloadTime) return false;
        const crossbow = getHeldCrossbow();
        if (!crossbow || crossbow.isNull()) return false;
        const loaded = getCrossbowLoadedObject(crossbow);
        if (loaded && !loaded.isNull()) return false;
        crossbowDupeNextReloadTime = time + 0.25;
        try {
            const anchor = crossbow.field("_attachAnchor").value;
            const spawnPos = anchor && !anchor.isNull() ? getTransform(anchor).method("get_position").invoke() : getTransform(crossbow).method("get_position").invoke();
            const spawned = spawnItemPrefab(crossbowDupeLastItemID, spawnPos, identityQuaternion);
            if (!spawned || spawned.isNull()) return false;
            requestStateAuthoritySafe(crossbow);
            requestStateAuthoritySafe(spawned);
            try { spawned.method("set_colorHue").invoke(crossbowDupeLastHue); } catch (_) { }
            try { spawned.method("set_colorSaturation").invoke(crossbowDupeLastSat); } catch (_) { }
            try { spawned.method("set_scaleModifier").invoke(crossbowDupeLastScale); } catch (_) { }
            if (anchor && !anchor.isNull() && tryAttachToAnchor(anchor, spawned)) {
                try { crossbow.method("HandleAttachedChanged").invoke(0, spawned); } catch (_) { }
                return true;
            }
            if (tryAddItemToContainer(crossbow, spawned)) return true;
            try {
                crossbow.method("HandleAttachedChanged").invoke(0, spawned);
                return true;
            } catch (_) { }
        } catch (_) { }
        return false;
    }
    function teleportAllPlayersTo(worldPos) {
        let moved = 0;
        try {
            const allNetPlayers = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
            for (let i = 0; i < allNetPlayers.length; i++) {
                const target = allNetPlayers.get(i);
                if (!target || target.handle.isNull()) continue;
                if (tryTeleportNetPlayer(target, worldPos)) moved++;
            }
        } catch (_) { }
        return moved;
    }
    if (CrossbowClass) {
        try {
            CrossbowClass.method("HandleAttachedChanged").implementation = function (location, obj) {
                try {
                    if (obj && !obj.isNull()) {
                        const attachedItemID = getItemIDFromGrabbable(obj);
                        if (attachedItemID) {
                            crossbowDupeLastItemID = attachedItemID;
                            try { crossbowDupeLastHue = obj.method("get_colorHue").invoke(); } catch (_) { }
                            try { crossbowDupeLastSat = obj.method("get_colorSaturation").invoke(); } catch (_) { }
                            try { crossbowDupeLastScale = obj.method("get_scaleModifier").invoke(); } catch (_) { }
                        }
                    }
                } catch (_) { }
                return this.method("HandleAttachedChanged").invoke(location, obj);
            };
            console.log("[CrossbowDupe] tracking hook installed");
        } catch (e) {
            console.error("[CrossbowDupe] tracking hook failed: " + e);
        }
    }
    function fillContainerWithItems(container, spawnPos, count, itemSelector) {
        let insertedCount = 0;
        let attempts = 0;
        while (insertedCount < count && attempts < count * 12) {
            attempts++;
            const itemID = itemSelector(insertedCount, attempts);
            if (!itemID) continue;
            const child = spawnItemPrefab(itemID, spawnPos, identityQuaternion);
            if (!child || child.isNull()) continue;
            try {
                const childGBO = child.method("GetComponent", 1).inflate(GBOClass).invoke();
                if (childGBO && !childGBO.isNull()) {
                    childGBO.method("set_scaleModifier").invoke(randInt(-50, 50));
                    childGBO.method("set_colorHue").invoke(randInt(-127, 127));
                    childGBO.method("set_colorSaturation").invoke(randInt(-20, 127));
                }
            } catch (_) { }
            if (tryAddItemToContainer(container, child)) {
                insertedCount++;
            } else {
                try { despawnNetworkThing(child); } catch (_) { }
            }
        }
        return insertedCount;
    }
    function spawnFilledQuiver(selectedID = null, overridePos = null, overrideRot = null) {
        try {
            const spawnPos = overridePos || leftHandTransform.method("get_position").invoke();
            const spawnRot = overrideRot || identityQuaternion;
            const quiverIDs = ["item_quiver", "item_quiver_heart"];
            const quiverResult = spawnItemPrefab(quiverIDs[randInt(0, quiverIDs.length - 1)], spawnPos, spawnRot);
            if (!quiverResult || quiverResult.isNull()) return null;
            const quiverGBO = quiverResult.method("GetComponent", 1).inflate(GBOClass).invoke();
            if (quiverGBO && !quiverGBO.isNull()) {
                quiverGBO.method("set_scaleModifier").invoke(randInt(-50, 50));
                quiverGBO.method("set_colorHue").invoke(randInt(-127, 127));
                quiverGBO.method("set_colorSaturation").invoke(randInt(-20, 127));
            }
            const container = getContainerComponent(quiverResult);
            if (!container || container.isNull()) return null;
            setContainerCapacity(container, QUIVER_ITEM_LIMIT);
            setContainerOpen(container, true);
            const pool = getQuiverFillPool();
            const insertedCount = fillContainerWithItems(container, spawnPos, QUIVER_ITEM_LIMIT, () => selectedID || pool[randInt(0, pool.length - 1)]);
            setContainerCapacity(container, QUIVER_ITEM_LIMIT);
            setContainerOpen(container, false);
            return { netObj: quiverResult, container, insertedCount };
        } catch (e) {
            console.error("spawnFilledQuiver error: " + e);
            return null;
        }
    }
    function spawnOreQuiver() {
        try {
            const spawnPos = leftHandTransform.method("get_position").invoke();
            const quiverIDs = ["item_quiver", "item_quiver_heart"];
            const quiverResult = spawnItemPrefab(quiverIDs[randInt(0, quiverIDs.length - 1)], spawnPos, identityQuaternion);
            if (!quiverResult || quiverResult.isNull()) return null;
            const quiverGBO = quiverResult.method("GetComponent", 1).inflate(GBOClass).invoke();
            if (quiverGBO && !quiverGBO.isNull()) {
                quiverGBO.method("set_scaleModifier").invoke(randInt(-50, 50));
                quiverGBO.method("set_colorHue").invoke(randInt(-127, 127));
                quiverGBO.method("set_colorSaturation").invoke(randInt(-20, 127));
            }
            const container = getContainerComponent(quiverResult);
            if (!container || container.isNull()) return null;
            setContainerCapacity(container, QUIVER_ITEM_LIMIT);
            setContainerOpen(container, true);
            const insertedCount = fillContainerWithItems(container, spawnPos, QUIVER_ITEM_LIMIT, () => ORE_ITEM_IDS[randInt(0, ORE_ITEM_IDS.length - 1)]);
            setContainerCapacity(container, QUIVER_ITEM_LIMIT);
            setContainerOpen(container, false);
            return { netObj: quiverResult, container, insertedCount };
        } catch (e) {
            console.error("spawnOreQuiver error: " + e);
            return null;
        }
    }
    function spawnGiveawayBag() {
        try {
            const spawnPos = leftHandTransform.method("get_position").invoke();
            const bagIDs = ["item_backpack_large_clover", "item_backpack_large_basketball", "item_backpack_large_base"];
            const bagResult = spawnItemPrefab(bagIDs[randInt(0, bagIDs.length - 1)], spawnPos, identityQuaternion);
            if (!bagResult || bagResult.isNull()) return null;
            const bagGBO = bagResult.method("GetComponent", 1).inflate(GBOClass).invoke();
            if (bagGBO && !bagGBO.isNull()) {
                bagGBO.method("set_scaleModifier").invoke(randInt(-50, 50));
                bagGBO.method("set_colorHue").invoke(randInt(-127, 127));
                bagGBO.method("set_colorSaturation").invoke(randInt(-20, 127));
            }
            const bagContainer = getContainerComponent(bagResult);
            if (!bagContainer || bagContainer.isNull()) return null;
            setContainerCapacity(bagContainer, GIVEAWAY_BAG_ITEM_LIMIT);
            setContainerOpen(bagContainer, true);
            const insertedCount = fillContainerWithItems(bagContainer, spawnPos, GIVEAWAY_BAG_ITEM_LIMIT, () => itemIDs[randInt(0, itemIDs.length - 1)]);
            setContainerCapacity(bagContainer, GIVEAWAY_BAG_ITEM_LIMIT);
            setContainerOpen(bagContainer, false);
            return { netObj: bagResult, container: bagContainer, insertedCount };
        } catch (e) {
            console.error("spawnGiveawayBag error: " + e);
            return null;
        }
    }
    function despawnNetworkThing(obj) {
        if (!obj || obj.isNull()) return;
        try {
            let netObj = getNetworkObjectSafe(obj) || obj;
            const runner = netObj.method("get_Runner").invoke();
            if (runner && !runner.isNull()) runner.method("Despawn").invoke(netObj);
        } catch (_) { }
    }
    function detachAndRestoreObject(obj) {
        if (!obj || obj.isNull()) return;
        try {
            const tf = obj.method("get_transform").invoke();
            tf.method("SetParent", 2).invoke(NULL, true);
        } catch (_) { }
        try {
            const rb = obj.method("GetComponent", 1).inflate(Rigidbody).invoke();
            if (rb && !rb.isNull()) {
                rb.method("set_isKinematic").invoke(false);
                try { rb.method("WakeUp").invoke(); } catch (_) { }
            }
        } catch (_) { }
    }
    function releaseHeldObjects(list, shouldDespawn = false) {
        if (!list) return;
        for (const entry of list) {
            const obj = entry?.obj ?? entry;
            try {
                detachAndRestoreObject(obj);
                if (shouldDespawn) despawnNetworkThing(obj);
            } catch (_) { }
        }
        list.length = 0;
    }
    function clearFollowedPrefab() {
        try { despawnNetworkThing(followedPrefabObject); } catch (_) { }
        followedPrefabObject = null;
        followedPrefabTransform = null;
        followedPrefabName = "";
    }
    function getRightHandFollowPosition(zOffset = 0.0) {
        const handPos = rightHandTransform.method("get_position").invoke();
        if (!zOffset) return handPos;
        const handForward = rightHandTransform.method("get_forward").invoke();
        const offset = Vector3.method("op_Multiply", 2).invoke(handForward, zOffset);
        return Vector3.method("op_Addition", 2).invoke(handPos, offset);
    }
    function updateFollowedPrefabByName(name, zOffset = 0.0) {
        if (!name) return;
        if (!followedPrefabObject || followedPrefabObject.isNull() || followedPrefabName !== name) {
            clearFollowedPrefab();
            const spawnPos = getRightHandFollowPosition(zOffset);
            const spawnRot = rightHandTransform.method("get_rotation").invoke();
            followedPrefabObject = spawnNetworkPrefab(name, spawnPos, spawnRot);
            if (!followedPrefabObject || followedPrefabObject.isNull()) return;
            followedPrefabName = name;
            try { followedPrefabObject.method("RequestStateAuthority").invoke(); } catch (_) { }
            try {
                const go = followedPrefabObject.method("get_gameObject").invoke();
                followedPrefabTransform = go && !go.isNull() ? go.method("get_transform").invoke() : null;
            } catch (_) {
                followedPrefabTransform = null;
            }
        }
        if (followedPrefabTransform && !followedPrefabTransform.isNull()) {
            followedPrefabTransform.method("set_position").invoke(getRightHandFollowPosition(zOffset));
            followedPrefabTransform.method("set_rotation").invoke(rightHandTransform.method("get_rotation").invoke());
        }
    }
    function updateFollowedPrefab() {
        updateFollowedPrefabByName(prefabList[prefabIndex]);
    }
    function clearFollowedMob() {
        try { despawnNetworkThing(followedMobObject); } catch (_) { }
        followedMobObject = null;
        followedMobTransform = null;
        followedMobName = "";
    }
    function findClosestMobToPosition(position, preferredName = null) {
        try {
            if (!MobControllerBase) return null;
            const allMobs = Object.method("FindObjectsByType", 1).inflate(MobControllerBase).invoke(0);
            let nearest = null;
            let nearestDist = Number.MAX_SAFE_INTEGER;
            for (let i = 0; i < allMobs.length; i++) {
                const mob = allMobs.get(i);
                if (!mob || mob.handle.isNull()) continue;
                if (preferredName) {
                    try {
                        const className = mob.class.name || "";
                        const objName = mob.method("get_name").invoke().toString();
                        if (!className.includes(preferredName) && !objName.includes(preferredName)) continue;
                    } catch (_) { }
                }
                const mobPos = getTransform(mob).method("get_position").invoke();
                const dist = Vector3.method("Distance").invoke(position, mobPos);
                if (dist < nearestDist) {
                    nearestDist = dist;
                    nearest = mob;
                }
            }
            return nearest;
        } catch (_) {
            return null;
        }
    }
    function updateFollowedMob() {
        updateFollowedMobWithOffset(0.0);
    }
    function updateFollowedMobWithOffset(zOffset = 0.0) {
        const name = mobIDs[mobIndex];
        if (!name) return;
        if (!followedMobObject || followedMobObject.isNull() || followedMobName !== name) {
            clearFollowedMob();
            const spawnPos = getRightHandFollowPosition(zOffset);
            const spawnRot = rightHandTransform.method("get_rotation").invoke();
            const spawned = spawnMobAtPos(name, spawnPos, spawnRot);
            if (spawned && spawned !== true && !spawned.isNull()) {
                followedMobObject = spawned;
            } else {
                followedMobObject = findClosestMobToPosition(spawnPos, name);
            }
            if (!followedMobObject || followedMobObject.isNull()) return;
            followedMobName = name;
            followedMobTransform = getTransform(followedMobObject);
        }
        if (followedMobTransform && !followedMobTransform.isNull()) {
            followedMobTransform.method("set_position").invoke(getRightHandFollowPosition(zOffset));
            followedMobTransform.method("set_rotation").invoke(rightHandTransform.method("get_rotation").invoke());
        }
    }
    let cachedSpawnPrefabName = "";
    let lastSpawnSearchTime = 0;
    function spawnNetworkPrefab(prefabName, pos, rot) {
        try {
            const runner = PrefabGen.field("_instance").value.method("get_runner").invoke();
            if (!runner || runner.isNull())
                return null;
            const sources = runner.field("_config").value.field("PrefabTable").value.field("_sources").value;
            const count = sources.method("get_Count").invoke();
            for (let i = 0; i < count; i++) {
                try {
                    const source = sources.method("get_Item").invoke(i);
                    const desc = source.method("get_Description").invoke().toString();
                    if (desc.includes(prefabName)) {
                        const no = source.method("WaitForResult").invoke();
                        if (!no || no.isNull())
                            return null;
                        const makeZeroForType = (type) => {
                            if (type.class.isEnum || type.isPrimitive)
                                return 0;
                            if (!type.class.isValueType)
                                return NULL;
                            const fields = type.class.fields.filter(f => !f.isStatic);
                            if (fields.length === 0)
                                return 0;
                            return fields.map(f => makeZeroForType(f.type));
                        };
                        const buildNullableArg = (nullableType, hasValue, value) => {
                            const fields = nullableType.class.fields.filter(f => !f.isStatic);
                            return fields.map(f => {
                                const lname = f.name.toLowerCase();
                                if (lname.includes("hasvalue"))
                                    return hasValue ? 1 : 0;
                                if (lname === "value")
                                    return hasValue ? value : makeZeroForType(f.type);
                                return makeZeroForType(f.type);
                            });
                        };
                        const normalizeValue = (type, value) => {
                            if (typeof value === "boolean")
                                return value ? 1 : 0;
                            if (value instanceof Il2Cpp.ValueType) {
                                const fields = type.class.fields.filter(f => !f.isStatic);
                                if (fields.length === 0)
                                    return 0;
                                return fields.map(f => normalizeValue(f.type, f.bind(value).value));
                            }
                            if (Array.isArray(value))
                                return value.map(v => normalizeValue(type, v));
                            return value;
                        };
                        const buildNullableFromValueType = (nullableType, valueType) => {
                            return buildNullableArg(nullableType, true, normalizeValue(valueType.type, valueType));
                        };
                        let spawnMethod = null;
                        for (const m of runner.method("Spawn").overloads()) {
                            if (m.parameterCount !== 6 || m.isGeneric)
                                continue;
                            const p = m.parameters;
                            if (p[0].type.name.includes("Fusion.NetworkObject") &&
                                p[1].type.name.startsWith("System.Nullable") && p[1].type.name.includes("Vector3") &&
                                p[2].type.name.startsWith("System.Nullable") && p[2].type.name.includes("Quaternion") &&
                                p[3].type.name.startsWith("System.Nullable") && p[3].type.name.includes("PlayerRef") &&
                                p[4].type.name.includes("OnBeforeSpawned") &&
                                p[5].type.name.includes("NetworkSpawnFlags")) {
                                spawnMethod = m;
                                break;
                            }
                        }
                        if (!spawnMethod)
                            return null;
                        const posArg = buildNullableFromValueType(spawnMethod.parameters[1].type, pos);
                        const rotArg = buildNullableFromValueType(spawnMethod.parameters[2].type, rot);
                        const authArg = buildNullableArg(spawnMethod.parameters[3].type, false, makeZeroForType(spawnMethod.parameters[3].type));
                        const onBeforeArg = spawnMethod.parameters[4].type.class.isValueType ? makeZeroForType(spawnMethod.parameters[4].type) : NULL;
                        return spawnMethod.bind(runner).invoke(no, posArg, rotArg, authArg, onBeforeArg, 0);
                    }
                }
                catch (_) { }
            }
        }
        catch (e) {
            console.error("spawnNetworkPrefab error: " + e);
        }
        return null;
    }
    function kickPlayer(player) {
        const PrefabGenerator = AssemblyCSharp.class("AnimalCompany.NetSessionRPCs");
        const netinst = PrefabGenerator.field("_instance").value;
        if (!netinst)
            return null;
        const kickplr = PrefabGenerator.method("KickPlayer");
        if (!kickplr)
            return null;
        const rpckickplr = netinst.method("RPC_KickPlayer");
        if (!rpckickplr)
            return null;
        const plruserID = player.field("_userID").value;
        if (!plruserID)
            return null;
        const playerRef = player.method("get_Object").invoke()
            .method("get_InputAuthority").invoke();
        rpckickplr.invoke(playerRef);
        const playerRefInt = playerRef.field("_val").value;
        kickplr.invoke(playerRefInt);
    }
    function getRightFingers(rig) {
        try {
            const view = rig.method("get_view").invoke();
            if (!view || view.isNull())
                return null;
            const fingerViews = view.field("_fingerViews").value;
            if (!fingerViews || fingerViews.isNull() || fingerViews.length < 2)
                return null;
            return fingerViews.get(1);
        }
        catch (_) {
            return null;
        }
    }
    function world2Player(position) {
        position = Vector3.method("op_Subtraction", 2).invoke(position, getTransform(bodyCollider).method("get_position").invoke());
        position = Vector3.method("op_Addition", 2).invoke(position, getTransform(GTPlayer).method("get_position").invoke());
        return position;
    }
    function teleportPlayer(position) {
        const player = NetPlayer.method("get_localPlayer").invoke();
        if (!player)
            return;
        player.method("RPC_Teleport").invoke(world2Player(position));
    }
    function sendNotification(text = "", requiresReload = true, clearTime = 5) {
        const isOld = (currentNotification == text);
        notifactionResetTime = time + clearTime;
        currentNotification = text;
        if (requiresReload && !isOld)
            reloadMenu();
    }
    function applyTheme(name, background, text, button, pressed = null) {
        bgColor = background;
        textColor = text;
        buttonColor = button;
        if (pressed) buttonPressedColor = pressed;
        sendNotification("Theme: " + name, true, 2);
    }
    function trySetMasterClient() {
        try {
            const localNetPlayer = getLocalNetPlayer();
            if (!localNetPlayer || localNetPlayer.isNull()) {
                sendNotification("Local player not found.", false);
                return false;
            }
            const localPlayerRef = localNetPlayer.method("get_Object").invoke().method("get_InputAuthority").invoke();
            const runner = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
            if (!runner || runner.isNull()) {
                sendNotification("Runner not found.", false);
                return false;
            }

            const attempts = [
                () => runner.method("SetMasterClient").invoke(localPlayerRef),
                () => runner.method("RPC_SetMasterClient").invoke(localPlayerRef),
                () => runner.method("SetHost").invoke(localPlayerRef),
                () => runner.method("SetSharedModeMasterClient").invoke(localPlayerRef),
                () => {
                    const sessionManager = NManager.method("get_instance").invoke();
                    return sessionManager.method("SetMasterClient").invoke(localPlayerRef);
                },
                () => {
                    const sessionManager = NManager.method("get_instance").invoke();
                    return sessionManager.method("RPC_SetMasterClient").invoke(localPlayerRef);
                },
                () => {
                    const sessionManager = NManager.method("get_instance").invoke();
                    return sessionManager.method("SetHost").invoke(localPlayerRef);
                }
            ];

            for (const attempt of attempts) {
                try {
                    attempt();
                    sendNotification("Tried to set you as masterclient.", false, 2);
                    return true;
                } catch (_) { }
            }
        } catch (_) { }

        sendNotification("No masterclient setter found in this build.", false, 2.5);
        return false;
    }
    function createObject(pos = zeroVector, rot = identityQuaternion, scale = oneVector, primitiveType = 3, colorArr = [1, 1, 1, 1], parent = null, enableCollider = false) {
        const obj = GameObject.method("CreatePrimitive").invoke(primitiveType);
        const renderer = getComponent(obj, Renderer);
        if (colorArr[3] == 0) {
            renderer.method("set_enabled").invoke(false);
        }
        else {
            const material = renderer.method("get_material").invoke();
            material.method("set_shader").invoke(UberShader);
            material.method("set_color").invoke(colorArr);
        }
        const col = getComponent(obj, Collider);
        if (!col.isNull()) {
            if (enableCollider) {
                col.method("set_enabled").invoke(true);
                col.method("set_isTrigger").invoke(false);
            }
            else {
                col.method("set_isTrigger").invoke(true);
            }
        }
        const transform = getTransform(obj);
        if (parent != null)
            transform.method("SetParent", 2).invoke(parent, false);
        transform.method("set_position").invoke(pos);
        transform.method("set_rotation").invoke(rot);
        transform.method("set_localScale").invoke(scale);
        return obj;
    }
    function renderMenuText(canvasObject, text = "", color = [1, 1, 1, 1], pos = zeroVector, size = oneVector) {
        const title = addComponent(createObject(zeroVector, identityQuaternion, oneVector, 3, [0, 0, 0, 0], getTransform(canvasObject)), Text);
        title.method("set_text").invoke(Il2Cpp.string(text));
        title.method("set_font").invoke(arial);
        title.method("set_fontSize").invoke(1);
        title.method("set_color").invoke(color);
        title.method("set_fontStyle").invoke(2);
        title.method("set_alignment").invoke(4);
        title.method("set_resizeTextForBestFit").invoke(true);
        title.method("set_resizeTextMinSize").invoke(0);
        const rectTransform = getComponent(title, RectTransform);
        rectTransform.method("set_sizeDelta").invoke(size);
        rectTransform.method("set_position").invoke(pos);
        rectTransform.method("set_rotation").invoke(Quaternion.method("Euler").invoke(180.0, 90.0, 90.0));
    }
    function updateButtonColor(button, buttonData) {
        const RendererClass = Il2Cpp.domain.assembly("UnityEngine.CoreModule").image.class("UnityEngine.Renderer");
        const renderer = getComponent(button, RendererClass);
        if (!renderer)
            return;
        const material = renderer.method("get_material").invoke();
        material.method("set_color").invoke(buttonData.enabled ? buttonPressedColor : buttonColor);
    }
    function reloadMenu() {
        if (menu != null) {
            Object.method("Destroy", 1).invoke(menu);
            menu = null;
        }
    }
    let gunLocked = false;
    let lockTarget = null;
    let GunPointer = null;
    let GunLine = null;
    function renderMenu() {
        menu = createObject(zeroVector, identityQuaternion, [0.1, 0.3, 0.3825], 3, [0, 0, 0, 0]);
        Destroy(getComponent(menu, BoxCollider));
        const menuBackground = createObject([0.1, 0, 0], identityQuaternion, [0.1, 1, 1], 3, bgColor, getTransform(menu));
        Destroy(getComponent(menuBackground, BoxCollider));
        const canvasObject = createObject(zeroVector, identityQuaternion, oneVector, 3, [0, 0, 0, 0], getTransform(menu));
        const canvas = addComponent(canvasObject, Canvas);
        Destroy(getComponent(canvasObject, BoxCollider));
        const canvasScaler = addComponent(canvasObject, CanvasScaler);
        addComponent(canvasObject, GraphicRaycaster);
        canvas.method("set_renderMode").invoke(2);
        canvasScaler.method("set_dynamicPixelsPerUnit").invoke(1000.0);
        renderMenuText(canvasObject, menuName + ` [${currentPage + 1}]`, textColor, [0.11, 0, 0.175], [1, 0.1]);
        if (time > notifactionResetTime)
            currentNotification = "";
        renderMenuText(canvasObject, currentNotification, textColor, [0.11, 0, 0.275], [1, 0.1]);
        const disconnectButton = createObject([0.1, 0.0, 0.225], identityQuaternion, [0.09, 0.9, 0.08], 3, buttonColor, getTransform(menu), true);
        disconnectButton.method("set_name").invoke(Il2Cpp.string("@Disconnect"));
        addComponent(disconnectButton, GorillaReportButton);
        getComponent(disconnectButton, BoxCollider).method("set_isTrigger").invoke(true);
        renderMenuText(canvasObject, "Disconnect", textColor, [0.11, 0, 0.225], [1, 0.1]);
        const returnButton = createObject([0.1, -0.175, -0.225], identityQuaternion, [0.09, 0.09, 0.09], 3, buttonColor, getTransform(menu));
        returnButton.method("set_name").invoke(Il2Cpp.string("@GlobalReturn"));
        addComponent(returnButton, GorillaReportButton);
        getComponent(returnButton, BoxCollider).method("set_isTrigger").invoke(true);
        renderMenuText(canvasObject, "<", textColor, [0.11, -0.175, -0.225], [1, 0.1]);
        {
            const pageButton = createObject([0.1, 0.2, 0], identityQuaternion, [0.09, 0.2, 0.9], 3, buttonColor, getTransform(menu));
            pageButton.method("set_name").invoke(Il2Cpp.string("@PreviousPage"));
            addComponent(pageButton, GorillaReportButton);
            getComponent(pageButton, BoxCollider).method("set_isTrigger").invoke(true);
            renderMenuText(canvasObject, "<", textColor, [0.11, 0.2, 0], [1, 0.1]);
        }
        {
            const pageButton = createObject([0.1, -0.2, 0], identityQuaternion, [0.09, 0.2, 0.9], 3, buttonColor, getTransform(menu));
            pageButton.method("set_name").invoke(Il2Cpp.string("@NextPage"));
            addComponent(pageButton, GorillaReportButton);
            getComponent(pageButton, BoxCollider).method("set_isTrigger").invoke(true);
            renderMenuText(canvasObject, ">", textColor, [0.11, -0.2, 0], [1, 0.1]);
        }
        let i = 0;
        const targetMods = buttons[currentCategory].slice(currentPage * 8).slice(0, 8);
        targetMods.forEach((buttonData) => {
            const button = createObject([0.105, 0, 0.13 - (i * 0.04)], identityQuaternion, [0.09, 0.9, 0.08], 3, buttonColor, getTransform(menu));
            button.method("set_name").invoke(Il2Cpp.string("@" + buttonData.buttonText));
            addComponent(button, GorillaReportButton);
            getComponent(button, BoxCollider).method("set_isTrigger").invoke(true);
            renderMenuText(canvasObject, buttonData.buttonText, textColor, [0.11, 0, 0.13 - (i * 0.04)], [1, 0.1]);
            updateButtonColor(button, buttonData);
            i++;
        });
        getTransform(menu).method("set_localScale").invoke(Vector3.method("op_Multiply").invoke(Vector3.method("op_Multiply").invoke(getTransform(menu).method("get_localScale").invoke(), GTPlayer.field("<playerScale>k__BackingField").value), menuscale));
        recenterMenu();
    }
    function renderReference() {
        if (righthand) {
            reference = createObject(zeroVector, identityQuaternion, [0.01, 0.01, 0.01], 0, bgColor, leftHandTransform);
        }
        else {
            reference = createObject(zeroVector, identityQuaternion, [0.01, 0.01, 0.01], 0, bgColor, rightHandTransform);
        }
        referenceCollider = getComponent(reference, Collider);
        getTransform(reference).method("set_localPosition").invoke([0.01, -0.117, 0.05]);
        reference.method("set_layer").invoke(2);
        addComponent(reference, Rigidbody).method("set_isKinematic").invoke(true);
    }
    function renderGun(overrideLayerMask = null) {
        const StartPosition = rightHandTransform.method("get_position").invoke();
        const Direction = rightHandTransform.method("get_forward").invoke();
        const DirectionDivided = Vector3.method("op_Division").invoke(Direction, 4);
        const rayStartPosition = Vector3.method("op_Addition").invoke(StartPosition, DirectionDivided);
        const layerMask = overrideLayerMask || -3180559;
        //const layerMask = ~(1 << 7); it works to block some colliders but not all (selling machine)
        const hits = Physics.method("RaycastAll", 4).invoke(rayStartPosition, Direction, 512.0, layerMask);
        let finalDistance = Infinity;
        let finalRay = null;
        for (const hit of hits) {
            const distance = Vector3.method("Distance").invoke(hit.method("get_point").invoke(), StartPosition);
            if (distance < finalDistance) {
                finalRay = hit;
                finalDistance = distance;
            }
        }
        let EndPosition;
        if (gunLocked) {
            EndPosition = getTransform(lockTarget).method("get_position").invoke();
        }
        else {
            EndPosition = finalRay.method("get_point").invoke();
        }
        if (Vector3.method("op_Equality").invoke(EndPosition, zeroVector)) {
            const farDirection = Vector3.method("op_Multiply").invoke(Direction, 512);
            EndPosition = Vector3.method("op_Addition").invoke(StartPosition, farDirection);
        }
        if (GunPointer == null) {
            GunPointer = createObject(EndPosition, identityQuaternion, [0.1, 0.1, 0.1], 0, [1, 1, 1, 1]);
        }
        GunPointer.method("SetActive").invoke(true);
        const pointerTransform = getTransform(GunPointer);
        pointerTransform.method("set_position").invoke(EndPosition);
        const PointerRenderer = getComponent(GunPointer, Renderer);
        const material = PointerRenderer.method("get_material").invoke();
        material.method("set_shader").invoke(TextShader);
        const pointerColor = (gunLocked || rightTrigger) ? buttonPressedColor : buttonColor;
        material.method("set_color").invoke(pointerColor);
        const collider = getComponent(GunPointer, Collider);
        if (collider != null) {
            Destroy(collider);
        }
        if (GunLine == null) {
            const lineObj = createObject(zeroVector, identityQuaternion, oneVector, 0, [0, 0, 0, 0]);
            GunLine = addComponent(lineObj, LineRenderer);
        }
        else {
            GunLine.method("get_gameObject").invoke().method("SetActive").invoke(true);
        }
        const lineMaterial = GunLine.method("get_material").invoke();
        lineMaterial.method("set_shader").invoke(TextShader);
        GunLine.method("set_startColor").invoke(gunColor);
        GunLine.method("set_endColor").invoke(gunColor);
        const lineWidth = 0.025;
        GunLine.method("set_startWidth").invoke(lineWidth);
        GunLine.method("set_endWidth").invoke(lineWidth);
        GunLine.method("set_positionCount").invoke(2);
        GunLine.method("set_useWorldSpace").invoke(true);
        //GunLine.method("set_numCapVertices").invoke(10);
        GunLine.method("SetPosition").invoke(0, StartPosition);
        GunLine.method("SetPosition").invoke(1, EndPosition);
        if (rightTrigger || gunLocked) {
            const Step = 10;
            GunLine.method("set_positionCount").invoke(Step);
            GunLine.method("SetPosition").invoke(0, StartPosition);
            for (let i = 1; i < (Step - 1); i++) {
                const t = i / (Step - 1);
                const Position = Vector3.method("Lerp").invoke(StartPosition, EndPosition, t);
                const randomValue = Math.random();
                let offset = zeroVector;
                if (randomValue > 0.75) {
                    offset = [
                        (Math.random() * 0.2) - 0.1,
                        (Math.random() * 0.2) - 0.1,
                        (Math.random() * 0.2) - 0.1
                    ];
                }
                const finalPosition = Vector3.method("op_Addition").invoke(Position, offset);
                GunLine.method("SetPosition").invoke(i, finalPosition);
            }
            GunLine.method("SetPosition").invoke(Step - 1, EndPosition);
        }
        return { ray: finalRay, gunPointer: GunPointer };
    }
    function recenterMenu() {
        const menuTransform = getTransform(menu);
        let targetPos, targetRot;
        if (righthand) {
            targetPos = rightHandTransform.method("get_position").invoke();
            targetRot = rightHandTransform.method("get_rotation").invoke();
            targetRot = Quaternion.method("op_Multiply").invoke(targetRot, Quaternion.method("Euler").invoke(0, 0, 180));
        }
        else {
            targetPos = leftHandTransform.method("get_position").invoke();
            targetRot = leftHandTransform.method("get_rotation").invoke();
        }
        if (LerpMenu) {
            const menuPos = menuTransform.method("get_position").invoke();
            const distance = Vector3.method("Distance").invoke(menuPos, zeroVector);
            if (distance < 1) {
                menuTransform.method("set_position").invoke(targetPos);
                menuTransform.method("set_rotation").invoke(targetRot);
            }
            else {
                menuTransform.method("set_position").invoke(Vector3.method("Lerp").invoke(menuPos, targetPos, deltaTime * 15));
                menuTransform.method("set_rotation").invoke(Quaternion.method("Slerp").invoke(menuTransform.method("get_rotation").invoke(), targetRot, deltaTime * 15));
            }
        }
        else {
            menuTransform.method("set_position").invoke(targetPos);
            menuTransform.method("set_rotation").invoke(targetRot);
        }
    }
    class ButtonInfo {
        constructor(config) {
            this.buttonText = config.buttonText;
            this.method = config.method;
            this.enableMethod = config.enableMethod;
            this.disableMethod = config.disableMethod;
            this.isTogglable = config.isTogglable ?? true;
            this.toolTip = config.toolTip ?? null;
            this.enabled = config.enabled ?? false;
        }
    }
    const buttons = [
        [
            new ButtonInfo({
                buttonText: "▲Settings▲",
                method: () => { currentCategory = 2; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens settings."
            }),
            new ButtonInfo({
                buttonText: "▲Movement mods▲",
                method: () => { currentCategory = 3; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens the player mods category."
            }),
            new ButtonInfo({
                buttonText: "▲Player mods▲",
                method: () => { currentCategory = 4; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens the movement mods category."
            
            }),
            new ButtonInfo({
                buttonText: "▲Other player mods▲",
                method: () => { currentCategory = 5; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens the test category."
            }),
            new ButtonInfo({
                buttonText: "▲Item mods▲",
                method: () => { currentCategory = 6; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens the item mods category."
            }),
            new ButtonInfo({
                buttonText: "▲Spawning▲",
                method: () => { currentCategory = 7; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens the Spawning category."
            }),
            new ButtonInfo({
                buttonText: "▲pew pew mods▲",
                method: () => { currentCategory = 8; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens the gun mods category."
            }),
            new ButtonInfo({
                buttonText: "▲Misc▲",
                method: () => { currentCategory = 9; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens the misc category."
            }),
            new ButtonInfo({
                buttonText: "▲Prefab Stuff▲",
                method: () => { currentCategory = 11; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens the prefab spawning category."
            }),
            new ButtonInfo({
                buttonText: "▲Themes/colors▲",
                method: () => { currentCategory = 14; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens the menu theme category."
            }),
            
            new ButtonInfo({
                    buttonText: "▲dump▲",
                    method: () => { currentCategory = 12; currentPage = 0; },
                    isTogglable: false,
                    toolTip: "Opens the dump category."
                }),
                new ButtonInfo({
                    buttonText: "▲more mods▲",
                    method: () => { currentCategory = 13; currentPage = 0; },
                    isTogglable: false,
                    toolTip: "Opens the testing category."
                }),
                new ButtonInfo({
                    buttonText: "▲soundboard▲",
                    method: () => { currentCategory = 15; currentPage = 0; },
                    isTogglable: false,
                    toolTip: "Opens the soundboard category."
                }),
                
        ],
        [
            new ButtonInfo({
                buttonText: "LEAVE",
                method: () => {
                    const networkrunner = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                    const NManagerInst = NManager.method("get_instance").invoke();
                    NManagerInst.method("OnPlayerLeft").invoke(networkrunner, networkrunner.method("get_LocalPlayer").invoke());
                },
                isTogglable: false,
                toolTip: "Disconnects you from the room."
            }),
            new ButtonInfo({
                buttonText: "PreviousPage",
                method: () => {
                    const lastPage = Math.ceil(buttons[currentCategory].length / 8) - 1;
                    currentPage--;
                    if (currentPage < 0)
                        currentPage = lastPage;
                },
                isTogglable: false
            }),
            new ButtonInfo({
                buttonText: "NextPage",
                method: () => {
                    const lastPage = Math.ceil(buttons[currentCategory].length / 8) - 1;
                    currentPage++;
                    currentPage %= lastPage + 1;
                },
                isTogglable: false
            }),
            new ButtonInfo({
                buttonText: "GlobalReturn",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
        ],
        [
            
            new ButtonInfo({
                buttonText: "Exit Settings",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
                buttonText: "Change Item ID",
                method: () => {
                    if (rightGrab) {
                        itemIndex--;
                    }
                    else {
                        itemIndex++;
                    }
                    itemIndex = ((itemIndex % itemIDs.length) + itemIDs.length) % itemIDs.length;
                    console.log(itemIDs[itemIndex]);
                    sendNotification("<color=grey>[</color><color=#8000ff>MENU</color><color=grey>]</color> " + "New item index: " + itemIDs[itemIndex], false);
                },
                isTogglable: false,
                toolTip: "Changes the item ID. Hold right grip to go down"
            }),
            new ButtonInfo({
                buttonText: "Change Mob ID",
                method: () => {
                    if (rightGrab) {
                        mobIndex--;
                    }
                    else {
                        mobIndex++;
                    }
                    mobIndex = ((mobIndex % mobIDs.length) + mobIDs.length) % mobIDs.length;
                    sendNotification("<color=grey>[</color><color=#8000ff>MENU</color><color=grey>]</color> Mob: " + mobIDs[mobIndex], false);
                },
                isTogglable: false,
                toolTip: "Changes the mob to spawn. Hold right grip to go back."
            }),
            new ButtonInfo({
    buttonText: "SFX: " + (sfxNameMap[sfxIndex] ?? sfxIndex),
    method: () => {
        // Move to the next ID in our specific list
        sfxListPointer = (sfxListPointer + 1) % sfxIDList.length;
        sfxIndex = sfxIDList[sfxListPointer]; // Update the actual ID used by the mods
        
        const currentName = sfxNameMap[sfxIndex];
        sendNotification("Selected SFX: " + currentName, false, 2);
        reloadMenu(); // Refresh to update the button name
    },
    isTogglable: false,
    toolTip: "Cycles through the specific SFX names from your list."
}),
new ButtonInfo({
    buttonText: "Cycle VFX: " + vfxNames[vfxSelectorIndex],
    method: () => {
        vfxSelectorIndex = (vfxSelectorIndex + 1) % vfxNames.length;
        const currentName = vfxNames[vfxSelectorIndex];
        vfxIndex = VFXTypes[currentName]; // Update the ID used by the guns
        sendNotification("Selected VFX: " + currentName, false, 3);
        reloadMenu(); // Refresh menu to show the new name on the button
    },
    isTogglable: false,
    toolTip: "Cycles through your provided VFX list by name."
}),
            new ButtonInfo({
                buttonText: "unlock all",
                method: () => {
                    if (!devModeHookSet) {
                        ItemVendingMachineView.method("RefreshItems").implementation = function () {
                            this.field("_devModeOn").value = true;
                            this.method("RefreshItems").invoke();
                        };
                        devModeHookSet = true;
                    }
                },
                disableMethod: () => {
                    ItemVendingMachineView.method("RefreshItems").implementation = null;
                    devModeHookSet = false;
                },
                isTogglable: true,
                toolTip: "Unlocks all items in the vending machine."
            }),
            new ButtonInfo({
                buttonText: "set masterclient",
                method: () => {
                    trySetMasterClient();
                },
                isTogglable: false,
                toolTip: "Attempts to make you the current masterclient/host."
            }),
            new ButtonInfo({
                buttonText: "dev menu",
                method: () => {
                    try {
                        const App = AssemblyCSharp.class("AnimalCompany.App");
                        const userState = App.method("get_state").invoke().method("get_user").invoke();
                        const prim = userState.method("get_isDeveloper").invoke();
                        prim.field("_value").value = true;
                    }
                    catch (e) { }
                    try {
                        const PlayerWatch = AssemblyCSharp.class("AnimalCompany.PlayerWatch");
                        const allWatches = PlayerWatch.field("_allWatches").value;
                        const enumerator = allWatches.method("GetEnumerator").invoke();
                        while (enumerator.method("MoveNext").invoke()) {
                            const watch = enumerator.method("get_Current").invoke();
                            if (watch.method("get_IsMine").invoke()) {
                                const dv = watch.field("_devMenuView").value;
                                if (dv && !dv.isNull()) {
                                    dv.method("get_gameObject").invoke().method("SetActive").invoke(true);
                                }
                                break;
                            }
                        }
                    }
                    catch (e) { }
                    try {
                        const paths = [
                            "LeftHand/WalkieTalkieWatch/PlayerWatchView/SizeRoot/WalkieWatchV3/Canvases/Adventure_Canvas/PlayerWatchSideMenu/Interface (Dev)",
                            "LeftHand/WalkieTalkieWatch/PlayerWatchView/SizeRoot/WalkieWatchV3/Canvases/Adventure_Canvas/SideMenu/Interface (Dev)",
                        ];
                        const player = NetPlayer.method("get_localPlayer").invoke();
                        if (player && !player.isNull()) {
                            for (const path of paths) {
                                try {
                                    const t = getTransform(player).method("Find").invoke(Il2Cpp.string(path));
                                    if (t && !t.isNull()) {
                                        getComponent(t, Component).method("get_gameObject").invoke().method("SetActive").invoke(true);
                                    }
                                }
                                catch (_) { }
                            }
                        }
                    }
                    catch (e) { }
                },
                disableMethod: () => {
                    try {
                        const NLM = AssemblyCSharp.class("AnimalCompany.NetworkedLootManager");
                        const inst = findActiveObject(NLM);
                        if (inst && !inst.isNull())
                            inst.field("_isDeveloper").value = false;
                    }
                    catch (e) { }
                    try {
                        const App = AssemblyCSharp.class("AnimalCompany.App");
                        const userState = App.method("get_state").invoke().method("get_user").invoke();
                        userState.method("get_isDeveloper").invoke().field("_value").value = false;
                    }
                    catch (e) { }
                    try {
                        const PlayerWatch = AssemblyCSharp.class("AnimalCompany.PlayerWatch");
                        const allWatches = PlayerWatch.field("_allWatches").value;
                        const enumerator = allWatches.method("GetEnumerator").invoke();
                        while (enumerator.method("MoveNext").invoke()) {
                            const watch = enumerator.method("get_Current").invoke();
                            if (watch.method("get_IsMine").invoke()) {
                                const dv = watch.field("_devMenuView").value;
                                if (dv && !dv.isNull())
                                    dv.method("get_gameObject").invoke().method("SetActive").invoke(false);
                                break;
                            }
                        }
                    }
                    catch (e) { }
                },
                isTogglable: true,
                toolTip: "hi"
            }),
            new ButtonInfo({
                buttonText: "set name to Moony",
                method: () => {
                    const netplayer = NetPlayer.method("get_localPlayer").invoke();
                    netplayer.method("set_displayName").invoke(Il2Cpp.string("Moony"));
                },
                isTogglable: true,
                toolTip: "self explanatory"
            }),
            new ButtonInfo({
                buttonText: "set name to Faze-zehab1",
                method: () => {
                    const netplayer = NetPlayer.method("get_localPlayer").invoke();
                    netplayer.method("set_displayName").invoke(Il2Cpp.string("Faze-zehab1"));
            },
                isTogglable: true,
                enabled: true,
                toolTip: "self explanatory"
            }),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Movement mods",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
                buttonText: "Fly",
                method: () => {
                    const rb = GTPlayer.method("GetComponent", 1).inflate(Rigidbody).invoke();
                    if (!rb)
                        return;
                    if (rightTrigger) {
                        const handForward = rightHandTransform.method("get_forward").invoke();
                        const flyForce = Vector3.method("op_Multiply", 2).invoke(handForward, flySpeed * deltaTime);
                        rb.method("AddForce", 2).invoke(flyForce, 2);
                    }
                    if (leftTrigger) {
                        const handForward = leftHandTransform.method("get_forward").invoke();
                        const flyForce = Vector3.method("op_Multiply", 2).invoke(handForward, flySpeed * deltaTime);
                        rb.method("AddForce", 2).invoke(flyForce, 2);
                    }
                },
                isTogglable: true,
                toolTip: "Hold triggers to fly where your hands point"
            }),
            new ButtonInfo({
                    buttonText: "uhh flying",
                    isTogglable: true,
                    enableMethod: () => {
                        noclipFlyEnabled = true;
                        const rb = getComponent(GTPlayer, Rigidbody);
                        if (rb && !rb.isNull()) {
                            try {
                                preUhhFlyUseGravity = rb.method("get_useGravity").invoke();
                                preUhhFlyGravityValid = true;
                            } catch (_) {
                                preUhhFlyGravityValid = false;
                            }
                            rb.method("set_useGravity").invoke(false);
                            try { rb.method("set_linearVelocity").invoke(zeroVector); } catch (_) { }
                            try { rb.method("set_angularVelocity").invoke(zeroVector); } catch (_) { }
                        }
                    },
                    disableMethod: () => {
                        noclipFlyEnabled = false;
                        const rb = getComponent(GTPlayer, Rigidbody);
                        if (rb && !rb.isNull()) {
                            rb.method("set_useGravity").invoke(preUhhFlyGravityValid ? preUhhFlyUseGravity : true);
                            try { rb.method("set_linearVelocity").invoke(zeroVector); } catch (_) { }
                            try { rb.method("set_angularVelocity").invoke(zeroVector); } catch (_) { }
                        }
                    },
                    method: () => {
                        if (!noclipFlyEnabled) return;

                        try {
                            const rb = getComponent(GTPlayer, Rigidbody);
                            const tf = getTransform(GTPlayer);

                            // 1. Kill all momentum so you stay perfectly still when not moving
                            rb.method("set_linearVelocity").invoke(zeroVector);
                            rb.method("set_angularVelocity").invoke(zeroVector);

                            if (rightTrigger || leftTrigger) {
                                // 2. BOOST CHECK: If BOTH triggers are held, double the speed!
                                let currentMult = flySpeeds[flySpeedIndex];
                                if (rightTrigger && leftTrigger) currentMult *= 2.5;

                                // 3. Get direction
                                const hand = rightTrigger ? rightHandTransform : leftHandTransform;
                                const forward = hand.method("get_forward").invoke();

                                // 4. BIG MATH: Position += Forward * (Speed * DeltaTime)
                                // We use a larger multiplier here to make it feel 'snappy'
                                const moveStep = currentMult * deltaTime;
                                const moveAmount = Vector3.method("op_Multiply", 2).invoke(forward, moveStep);

                                const currentPos = tf.method("get_position").invoke();
                                const newPos = Vector3.method("op_Addition", 2).invoke(currentPos, moveAmount);

                                // 5. HARD SET POSITION
                                tf.method("set_position").invoke(newPos);
                            }
                        } catch (e) { }
                    },
                    toolTip: "Hold triggers to fly. Hold BOTH triggers for a massive speed boost."
                }),

                // --- ADD THIS BUTTON TO CHANGE SPEED ---
                new ButtonInfo({
                    buttonText: "change fly speed",
                    isTogglable: false,
                    method: () => {
                        flySpeedIndex = (flySpeedIndex + 1) % flySpeeds.length;
                        const speedNames = ["Slow", "Medium", "FAST", "NUCLEAR"];
                        sendNotification("Fly Speed: " + speedNames[flySpeedIndex], false);
                    },
                    toolTip: "Switch between 10, 25, 60, and 150 speed."
                }),
                new ButtonInfo({
    buttonText: "noclip (click B)",
    isTogglable: true,
    enableMethod: () => {
        noclipModMaster = true;
        sendNotification("Noclip Mod Armed! Press B to toggle.", false, 3);
    },
    disableMethod: () => {
        noclipModMaster = false;
        isCurrentlyNoclipping = false;
        // Safety: Force colliders ON when the mod is disabled in menu
        try {
            setPlayerCollidersEnabled(true);
        } catch(e) {}
        sendNotification("Noclip Mod Disabled.", false, 2);
    },
    toolTip: "When ON, press the B button on your controller to pass through walls."
}),
new ButtonInfo({
    buttonText: "platforms",
    isTogglable: true,
    enableMethod: () => {
        platformsEnabled = true;
        leftPlatformLatched = false;
        rightPlatformLatched = false;
    },
    disableMethod: () => { 
        platformsEnabled = false; 
        leftPlatformLatched = false;
        rightPlatformLatched = false;
        if (leftPlatform) { Destroy(leftPlatform); leftPlatform = null; }
        if (rightPlatform) { Destroy(rightPlatform); rightPlatform = null; }
    },
    method: () => {
        if (!platformsEnabled) return;

        // --- RIGHT HAND PLATFORM ---
        if (rightGrab) {
            if (!rightPlatformLatched || rightPlatform == null || rightPlatform.handle.isNull()) {
                if (rightPlatform && !rightPlatform.handle.isNull()) Destroy(rightPlatform);
                rightPlatform = createObject(zeroVector, identityQuaternion, [0.4, 0.05, 0.4], 3, buttonColor, null, true);
                rightPlatformLatched = true;
                const tf = getTransform(rightPlatform);
                const handPos = rightHandTransform.method("get_position").invoke();
                const handRot = rightHandTransform.method("get_rotation").invoke();
                const offset = Vector3.method("op_Multiply", 2).invoke(rightHandTransform.method("get_up").invoke(), -0.05);
                const finalPos = Vector3.method("op_Addition", 2).invoke(handPos, offset);
                tf.method("set_position").invoke(finalPos);
                tf.method("set_rotation").invoke(handRot);
            }
        } else if (rightPlatform != null) {
            Destroy(rightPlatform);
            rightPlatform = null;
            rightPlatformLatched = false;
        }

        // --- LEFT HAND PLATFORM ---
        if (leftGrab) {
            if (!leftPlatformLatched || leftPlatform == null || leftPlatform.handle.isNull()) {
                if (leftPlatform && !leftPlatform.handle.isNull()) Destroy(leftPlatform);
                leftPlatform = createObject(zeroVector, identityQuaternion, [0.4, 0.05, 0.4], 3, buttonColor, null, true);
                leftPlatformLatched = true;
                const tf = getTransform(leftPlatform);
                const handPos = leftHandTransform.method("get_position").invoke();
                const handRot = leftHandTransform.method("get_rotation").invoke();
                const offset = Vector3.method("op_Multiply", 2).invoke(leftHandTransform.method("get_up").invoke(), -0.05);
                const finalPos = Vector3.method("op_Addition", 2).invoke(handPos, offset);
                tf.method("set_position").invoke(finalPos);
                tf.method("set_rotation").invoke(handRot);
            }
        } else if (leftPlatform != null) {
            Destroy(leftPlatform);
            leftPlatform = null;
            leftPlatformLatched = false;
        }
    },
    toolTip: "Hold Grip to spawn a platform under your hand. Rotate your hand to tilt the platform."
}),
            new ButtonInfo({
                buttonText: "Long Arms",
                method: () => {
                    getTransform(GTPlayer).method("set_localScale").invoke([1.25, 1.25, 1.25]);
                },
                disableMethod: () => {
                    getTransform(GTPlayer).method("set_localScale").invoke(oneVector);
                },
                toolTip: "Gives you longer arms."
            }),
            new ButtonInfo({
                buttonText: "Longer Arms",
                method: () => {
                    getTransform(GTPlayer).method("set_localScale").invoke([1.75, 1.75, 1.75]);
                },
                disableMethod: () => {
                    getTransform(GTPlayer).method("set_localScale").invoke(oneVector);
                },
                toolTip: "Gives you longer arms."
            }),
            new ButtonInfo({
                buttonText: "Longerer Arms",
                method: () => {
                    getTransform(GTPlayer).method("set_localScale").invoke([2, 2, 2]);
                },
                disableMethod: () => {
                    getTransform(GTPlayer).method("set_localScale").invoke(oneVector);
                },
                toolTip: "Gives you longer arms."
            }),
            new ButtonInfo({
                buttonText: "Longererer Arms",
                method: () => {
                    getTransform(GTPlayer).method("set_localScale").invoke([3, 3, 3]);
                },
                disableMethod: () => {
                    getTransform(GTPlayer).method("set_localScale").invoke(oneVector);
                },
                toolTip: "Gives you longer arms."
        
            }),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Player mods",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
                buttonText: "Invisible Toggleable",
                enableMethod: () => {
                    const player = PCClass.method("get_instance").invoke();
                    const RIGTESTBETA = player.method("get_playerView").invoke();
                    if (!RIGTESTBETA)
                        return null;
                    const bodyRootTF = RIGTESTBETA.field("_cameraTransform").value;
                    if (!bodyRootTF)
                        return null;
                    bodyRootTF.method("set_position").invoke([0, -99999, 0]);
                },
                disableMethod: () => {
                    const player = PCClass.method("get_instance").invoke();
                    const RIGTESTBETA = player.method("get_playerView").invoke();
                    if (!RIGTESTBETA)
                        return null;
                    const bodyRootTF = RIGTESTBETA.field("_cameraTransform").value;
                    if (!bodyRootTF)
                        return null;
                    bodyRootTF.method("set_position").invoke(getTransform(headCollider).method("get_position").invoke());
                },
                isTogglable: true,
                toolTip: "Turns you invisible."
            }),
            new ButtonInfo({
                buttonText: "invincible",
                method: () => {
                    {
                        const LocalPlayer = NetPlayer.method("get_localPlayer").invoke();
                        const localcontroller = PlayerController.method("get_instance").invoke();
                        LocalPlayer.method("set_maxHealth").invoke(200000000000000000000000000);
                        LocalPlayer.method("set_healthGained").invoke(200000000000000000000000000);
                        LocalPlayer.method("set_healthLost").invoke(-333333);
                        localcontroller.method("set_healthLost").invoke(-333333);
                        localcontroller.method("SubtractPlayerHealth").invoke(-333333);
                        localcontroller.method("set_healthHealed").invoke(200000000000000000000000000);
                    }
                },
                disableMethod: () => {
                    const LocalPlayer = NetPlayer.method("get_localPlayer").invoke();
                    const localcontroller = PlayerController.method("get_instance").invoke();
                    LocalPlayer.method("set_maxHealth").invoke(125);
                    LocalPlayer.method("set_healthGained").invoke(125);
                    LocalPlayer.method("set_healthLost").invoke(0);
                    localcontroller.method("set_healthLost").invoke(0);
                    localcontroller.method("set_healthHealed").invoke(125);
                    localcontroller.method("SubtractPlayerHealth").invoke(0);
                },
                isTogglable: true,
                toolTip: "hi"
            }),
            new ButtonInfo({
                buttonText: "no red watch test",
                method: () => {
                    {
                        const LocalPlayer = NetPlayer.method("get_localPlayer").invoke();
                        LocalPlayer.method("set_isWanted").invoke(false);
                    }
                },
                disableMethod: () => {
                    const LocalPlayer = NetPlayer.method("get_localPlayer").invoke();
                    LocalPlayer.method("set_isWanted").invoke(false);
                },
                isTogglable: true,
                toolTip: "hi"
            }),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Other Players",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
                buttonText: "Whitelist mods",
                method: () => { currentCategory = 10; currentPage = 0; },
                isTogglable: false,
                toolTip: "Opens the test category."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Lake [All]",
                method: () => {
                    const moved = teleportAllPlayersTo(TELEPORT_TO_LAKE);
                    sendNotification(`Teleported ${moved} players to Lake.`, false);
                },
                isTogglable: false,
                toolTip: "Teleports every player to the Lake coordinates."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Moon [All]",
                method: () => {
                    const moved = teleportAllPlayersTo(TELEPORT_TO_MOON);
                    sendNotification(`Teleported ${moved} players to Moon.`, false);
                },
                isTogglable: false,
                toolTip: "Teleports every player to the Moon coordinates."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Sewers [All]",
                method: () => {
                    const moved = teleportAllPlayersTo(TELEPORT_TO_SEWERS);
                    sendNotification(`Teleported ${moved} players to Sewers.`, false);
                },
                isTogglable: false,
                toolTip: "Teleports every player to the Sewers coordinates."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Above Water [All]",
                method: () => {
                    const moved = teleportAllPlayersTo(TELEPORT_TO_ABOVELAKE);
                    sendNotification(`Teleported ${moved} players to Above Water.`, false);
                },
                isTogglable: false,
                toolTip: "Teleports every player Above Water coordinates."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Spawn [All]",
                method: () => {
                    const moved = teleportAllPlayersTo(TELEPORT_TO_SPAWN);
                    sendNotification(`Teleported ${moved} players to Spawn.`, false);
                },
                isTogglable: false,
                toolTip: "Teleports every player to the Spawn coordinates."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Water Tower [All]",
                method: () => {
                    const moved = teleportAllPlayersTo(TELEPORT_TO_WATERTOWER);
                    sendNotification(`Teleported ${moved} players to Water Tower.`, false);
                },
                isTogglable: false,
                toolTip: "Teleports every player to the Water Tower coordinates."
            }),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Item mods",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
                buttonText: "Crossbow Dupe",
                isTogglable: true,
                method: () => {
                    if (!CrossbowClass) return;
                    if (frameCount % 4 !== 0) return;
                    if (tryRefillHeldCrossbow()) {
                        sendNotification("Crossbow reloaded from saved ammo.", false, 1.5);
                    }
                },
                enableMethod: () => {
                    crossbowDupeNextReloadTime = 0;
                    sendNotification("Crossbow Dupe enabled. Load any item once to save it.", false, 2);
                },
                disableMethod: () => {
                    sendNotification("Crossbow Dupe disabled.", false, 1.5);
                },
                toolTip: "Records the last item loaded into a held crossbow and refills it when empty."
            }),
            new ButtonInfo({
                    buttonText: "COMPLETELY Randomize Held Item",
                    isTogglable: true,
                    method: () => {
                        // Helper function for random numbers
                        function randInt(min, max) {
                            return Math.floor(Math.random() * (max - min + 1)) + min;
                        }

                        try {
                            const player = NetPlayer.method("get_localPlayer").invoke();
                            if (!player || player.isNull()) return;

                            // Check both hands
                            for (let hand = 0; hand <= 1; hand++) {
                                const interactor = getHandInteractor(player, hand);
                                if (!interactor || interactor.isNull()) continue;

                                const itemAnchor = interactor.field("_itemAnchor").value;
                                if (!itemAnchor || itemAnchor.isNull()) continue;

                                const grabbable = itemAnchor.method("get_grabbableObject").invoke();
                                if (!grabbable || grabbable.isNull()) continue;

                                // Pull trigger (left or right) to randomize
                                if (rightTrigger || leftTrigger) {
                                    // Randomize All 3 Properties
                                    const newHue = randInt(-127, 127);
                                    const newSat = randInt(-127, 127);
                                    const newScale = randInt(-128, 127);

                                    grabbable.method("set_colorHue").invoke(newHue);
                                    grabbable.method("set_colorSaturation").invoke(newSat);
                                    grabbable.method("set_scaleModifier").invoke(newScale);

                                    // Tiny delay so it doesn't spin through 100 colors a second
                                    sendNotification("Item Randomized!", false);
                                }
                            }
                        } catch (e) {
                            console.error("Randomizer Error: " + e);
                        }
                    },
                    toolTip: "Hold an item and pull Trigger to randomize its Hue, Saturation, and Scale."
                }),
            new ButtonInfo({
                buttonText: "Custom hue Held Item",
                method: () => {
                    try {
                        const player = NetPlayer.method("get_localPlayer").invoke();
                        if (!player)
                            return;
                        const interactor = player.method("GetHandInteractor", 1).invoke(0);
                        if (!interactor)
                            return;
                        const itemAnchor = interactor.field("_itemAnchor").value;
                        if (!itemAnchor)
                            return;
                        const grabbable = itemAnchor.method("get_grabbableObject").invoke();
                        if (!grabbable)
                            return;
                        if (rightGrab) {
                            scaleVal++;
                            if (hueVal > 127)
                                hueVal = -127;
                        }
                        if (leftGrab) {
                            scaleVal--;
                            if (hueVal < -127)
                                hueVal = 127;
                        }
                        grabbable.method("set_colorHue").invoke(scaleVal);
                    }
                    catch (e) {
                        console.error(e);
                    }
                },
                isTogglable: true,
                toolTip: "Hold A to increase, grip to decrease scale of held item."
            }),
            new ButtonInfo({
                buttonText: "Custom saturation Held Item",
                method: () => {
                    try {
                        const player = NetPlayer.method("get_localPlayer").invoke();
                        if (!player)
                            return;
                        const interactor = player.method("GetHandInteractor", 1).invoke(0);
                        if (!interactor)
                            return;
                        const itemAnchor = interactor.field("_itemAnchor").value;
                        if (!itemAnchor)
                            return;
                        const grabbable = itemAnchor.method("get_grabbableObject").invoke();
                        if (!grabbable)
                            return;
                        if (rightGrab) {
                            satVal++;
                            if (satVal > 127)
                                scaleVal = -127;
                        }
                        if (leftGrab) {
                            satVal--;
                            if (satVal < -127)
                                scaleVal = 127;
                        }
                        grabbable.method("set_colorSaturation").invoke(scaleVal);
                    }
                    catch (e) {
                        console.error(e);
                    }
                },
                isTogglable: true,
                toolTip: "Hold A to increase, grip to decrease scale of held item."
            }),
            new ButtonInfo({
                buttonText: "Custom Scale Held Item",
                method: () => {
                    try {
                        const player = NetPlayer.method("get_localPlayer").invoke();
                        if (!player)
                            return;
                        const interactor = player.method("GetHandInteractor", 1).invoke(0);
                        if (!interactor)
                            return;
                        const itemAnchor = interactor.field("_itemAnchor").value;
                        if (!itemAnchor)
                            return;
                        const grabbable = itemAnchor.method("get_grabbableObject").invoke();
                        if (!grabbable)
                            return;
                        if (rightGrab) {
                            scaleVal++;
                            if (scaleVal > 127)
                                scaleVal = -127;
                        }
                        if (leftGrab) {
                            scaleVal--;
                            if (scaleVal < -127)
                                scaleVal = 127;
                        }
                        grabbable.method("set_scaleModifier").invoke(scaleVal);
                    }
                    catch (e) {
                        console.error(e);
                    }
                },
                isTogglable: true,
                toolTip: "Hold A to increase, grip to decrease scale of held item."
            }),
            new ButtonInfo({
                buttonText: "Rainbow Held Item",
                method: () => {
                    try {
                        const player = NetPlayer.method("get_localPlayer").invoke();
                        if (!player)
                            return;
                        const interactor = player.method("GetHandInteractor", 1).invoke(0);
                        if (!interactor)
                            return;
                        const itemAnchor = interactor.field("_itemAnchor").value;
                        if (!itemAnchor)
                            return;
                        const grabbable = itemAnchor.method("get_grabbableObject").invoke();
                        if (!grabbable)
                            return;
                        if (rightGrab) {
                            hueVal++;
                            satVal++;
                            if (hueVal > 127)
                                hueVal = -127;
                            if (satVal > 127)
                                satVal = -127;
                        }
                        if (leftGrab) {
                            hueVal--;
                            satVal--;
                            if (hueVal < -127)
                                hueVal = 127;
                            if (satVal > 127)
                                satVal = -127;
                        }
                        grabbable.method("set_colorHue").invoke(hueVal);
                        grabbable.method("set_colorSaturation").invoke(satVal);
                    }
                    catch (e) {
                        console.error(e);
                    }
                },
                isTogglable: true,
                toolTip: "Hold A to increase, grip to decrease hue of held item."
            }),
            new ButtonInfo({
                buttonText: "Rainbow All Items",
                method: () => {
                    try {
                        if (rightGrab && time > tagGunDelay) {
                            tagGunDelay = time + 0.1;
                            hueVal += 5;
                            satVal += 5;
                            if (hueVal > 127)
                                hueVal = -127;
                            if (satVal > 127)
                                satVal = -127;
                        }
                        if (leftGrab && time > tagGunDelay) {
                            tagGunDelay = time + 0.1;
                            hueVal -= 5;
                            satVal -= 5;
                            if (hueVal > 127)
                                hueVal = -127;
                            if (satVal > 127)
                                satVal = -127;
                        }
                        if (frameCount % 60 == 0 || !cachedItems) {
                            cachedItems = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                        }
                        if (frameCount % 5 != 0)
                            return;
                        if (cachedItems) {
                            for (let i = 0; i < cachedItems.length; i++) {
                                const item = cachedItems.get(i);
                                if (!item || item.handle.isNull())
                                    continue;
                                item.method("set_colorHue").invoke(hueVal);
                                item.method("set_colorSaturation").invoke(satVal);
                            }
                        }
                    }
                    catch (e) {
                        console.error(e);
                    }
                },
                isTogglable: true,
                toolTip: "Hold grip to cycle rainbow on all items."
            }),
            new ButtonInfo({
                buttonText: "random Held Item",
                method: () => {
                    try {
                        const player = NetPlayer.method("get_localPlayer").invoke();
                        if (!player)
                            return;
                        const interactor = player.method("GetHandInteractor", 1).invoke(0);
                        if (!interactor)
                            return;
                        const itemAnchor = interactor.field("_itemAnchor").value;
                        if (!itemAnchor)
                            return;
                        const grabbable = itemAnchor.method("get_grabbableObject").invoke();
                        if (!grabbable)
                            return;
                        if (rightGrab) {
                            hueVal++;
                            satVal++;
                            scaleVal++;
                            if (hueVal > 127)
                                hueVal = -127;
                            if (satVal > 127)
                                satVal = -127;
                            if (scaleVal > 127)
                                scaleVal = -127;
                        }
                        if (leftGrab) {
                            hueVal--;
                            satVal--;
                            scaleVal--;
                            if (hueVal > 127)
                                hueVal = -127;
                            if (hueVal > 127)
                                hueVal = -127;
                            if (satVal > 127)
                                satVal = -127;
                        }
                        grabbable.method("set_colorHue").invoke(hueVal);
                        grabbable.method("set_colorSaturation").invoke(satVal);
                        grabbable.method("set_scaleModifier").invoke(scaleVal);
                    }
                    catch (e) {
                        console.error(e);
                    }
                },
                isTogglable: true,
                toolTip: "Hold A to increase, grip to decrease hue of held item."
            }),
            new ButtonInfo({
                buttonText: "random All Items",
                method: () => {
                    try {
                        if (rightGrab && time > tagGunDelay) {
                            tagGunDelay = time + 0.1;
                            hueVal += 5;
                            satVal += 5;
                            scaleVal += 5;
                            if (hueVal > 127)
                                hueVal = -127;
                            if (satVal > 127)
                                satVal = -127;
                            if (scaleVal > 127)
                                scaleVal = -127;
                        }
                        if (leftGrab && time > tagGunDelay) {
                            tagGunDelay = time + 0.1;
                            hueVal -= 5;
                            satVal -= 5;
                            scaleVal -= 5;
                            if (hueVal > 127)
                                hueVal = -127;
                            if (satVal > 127)
                                satVal = -127;
                            if (scaleVal > 127)
                                scaleVal = -127;
                        }
                        if (frameCount % 60 == 0 || !cachedItems) {
                            cachedItems = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                        }
                        if (frameCount % 5 != 0)
                            return;
                        if (cachedItems) {
                            for (let i = 0; i < cachedItems.length; i++) {
                                const item = cachedItems.get(i);
                                if (!item || item.handle.isNull())
                                    continue;
                                item.method("set_colorHue").invoke(hueVal);
                                item.method("set_colorSaturation").invoke(satVal);
                                item.method("set_scaleModifier").invoke(scaleVal);
                            }
                        }
                    }
                    catch (e) {
                        console.error(e);
                    }
                },
                isTogglable: true,
                toolTip: "Hold grip to cycle rainbow on all items."
            }),
        ],
        [    
        
            new ButtonInfo({
                buttonText: "Exit Spawning mods",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
                buttonText: "Spawn Items",
                method: () => {
                    const handTransform = rightHandTransform;
                    if (rightSecondary && rightGrab) {
                        try {
                            const result = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + itemIDs[itemIndex]), handTransform.method("get_position").invoke(), handTransform.method("get_rotation").invoke(), 
                            // @ts-ignore
                            NULL);
                            if (!result || result.handle.isNull()) {
                                sendNotification("Spawn returned null: " + itemIDs[itemIndex], false);
                            }
                            else {
                                sendNotification("Spawned: " + itemIDs[itemIndex], false);
                            }
                        }
                        catch (e) {
                            console.error("Hand spawn error:", e);
                            sendNotification("Spawn failed: " + e, false);
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Spawns items in your right hand (hold grip + B)."
            }),
            new ButtonInfo({
                buttonText: "Random Hand Duper",
                method: () => {
                    if (!rightSecondary)
                        return;
                    function rand(min, max) {
                        return Math.floor(Math.random() * (max - min + 1)) + min;
                    }
                    try {
                        const player = NetPlayer.method("get_localPlayer").invoke();
                        if (!player)
                            return;
                        const interactor = player.method("GetHandInteractor", 1).invoke(0);
                        if (!interactor)
                            return;
                        const itemAnchor = interactor.field("_itemAnchor").value;
                        if (!itemAnchor)
                            return;
                        const grabbable = itemAnchor.method("get_grabbableObject").invoke();
                        if (!grabbable || grabbable.isNull())
                            return;
                        let itemIDStr = "item_treestick";
                        const grabbableItem = grabbable.method("GetComponent", 1).inflate(GBIClass).invoke();
                        if (grabbableItem && !grabbableItem.isNull()) {
                            const idObj = grabbableItem.method("get_itemID").invoke();
                            if (idObj && !idObj.isNull())
                                itemIDStr = idObj.content;
                        }
                        const spawnPos = leftHandTransform.method("get_position").invoke();
                        const result = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + itemIDStr), spawnPos, identityQuaternion, 
                        // @ts-ignore
                        NULL);
                        if (!result || result.isNull())
                            return;
                        const spawnedGrabbable = result.method("GetComponent", 1).inflate(GBOClass).invoke();
                        if (spawnedGrabbable && !spawnedGrabbable.isNull()) {
                            spawnedGrabbable.method("set_scaleModifier").invoke(rand(-128, 127));
                            spawnedGrabbable.method("set_colorHue").invoke(rand(-127, 127));
                            spawnedGrabbable.method("set_colorSaturation").invoke(rand(-20, 127));
                            const forward = leftHandTransform.method("get_forward").invoke();
                            const velocity = Vector3.method("op_Multiply", 2).invoke(forward, 10);
                            spawnedGrabbable.method("AddExternalForceVelocity", 1).invoke(velocity);
                        }
                    }
                    catch (e) {
                        console.error("Random Hand Duper error:", e);
                    }
                },
                isTogglable: true,
                toolTip: "Dupe item held in left hand with random config (Hold B)."
            }),
            new ButtonInfo({
                    buttonText: "random modifier item launcher (lauches your currently selected item in settings)",
                    method: () => {

                        if (!rightSecondary) return;

                        function rand(min, max) {
                            return Math.floor(Math.random() * (max - min + 1)) + min;
                        }

                        try {
                            const handTransform = rightHandTransform;

                            const spawnPos = handTransform.method("get_position").invoke();
                            const spawnRot = handTransform.method("get_rotation").invoke();

                            // ✅ USE YOUR CURRENT SELECTED ITEM
                            const itemPath = "item_prefab/" + itemIDs[itemIndex];

                            const result = PrefabGen.method("SpawnItem", 4).invoke(
                                Il2Cpp.string(itemPath),
                                spawnPos,
                                spawnRot,
                                // @ts-ignore
                                NULL
                            );

                            if (!result || result.isNull()) {
                                console.log("Spawn failed:", itemPath);
                                return;
                            }

                            // ✅ GET GRABBABLE (same as your duper)
                            const spawnedGrabbable = result
                                .method("GetComponent", 1)
                                .inflate(GBOClass)
                                .invoke();

                            if (!spawnedGrabbable || spawnedGrabbable.isNull()) {
                                console.log("No grabbable");
                                return;
                            }

                            // 🎨 RANDOMIZE (from your duper)
                            spawnedGrabbable.method("set_scaleModifier").invoke(rand(-128, 127));
                            spawnedGrabbable.method("set_colorHue").invoke(rand(-127, 127));
                            spawnedGrabbable.method("set_colorSaturation").invoke(rand(-20, 127));

                            // 🚀 LAUNCH (cleaned up version)
                            let forward = handTransform.method("get_forward").invoke();

                            // 🔥 RANDOM SPREAD (what Harry meant)
                            forward.x += (Math.random() - 0.5) * 0.3;
                            forward.y += (Math.random() - 0.5) * 0.3;
                            forward.z += (Math.random() - 0.5) * 0.3;

                            const velocity = Vector3.method("op_Multiply", 2).invoke(forward, 15);

                            spawnedGrabbable.method("AddExternalForceVelocity", 1).invoke(velocity);

                        } catch (e) {
                            console.error("Item Launcher error:", e);
                        }
                    },
                    isTogglable: true,
                    toolTip: "Launches selected item with random effects."
                }),
                new ButtonInfo({
    buttonText: "entirely randomized item launcher (launches literally random items)",
    method: () => {
        // TRIGGER: Hold Right Secondary (B)
        if (!rightSecondary) return;

        function rand(min, max) {
            return Math.floor(Math.random() * (max - min + 1)) + min;
        }

        try {
            // 1. FILTER: Create a temporary list that does NOT include the crate
            const filteredItems = itemIDs.filter(id => id !== "item_crate");

            // 2. PICK RANDOM: Use the filtered list instead of the full list
            const randomItem = filteredItems[rand(0, filteredItems.length - 1)];
            const itemPath = "item_prefab/" + randomItem;

            const handTransform = rightHandTransform;
            const spawnPos = handTransform.method("get_position").invoke();
            const spawnRot = handTransform.method("get_rotation").invoke();

            // 3. SPAWN
            const result = PrefabGen.method("SpawnItem", 4).invoke(
                Il2Cpp.string(itemPath),
                spawnPos,
                spawnRot,
                // @ts-ignore
                NULL
            );

            if (!result || result.isNull()) return;

            const spawnedGrabbable = result.method("GetComponent", 1).inflate(GBOClass).invoke();
            if (!spawnedGrabbable || spawnedGrabbable.isNull()) return;

            // 4. NETWORK SYNC: Request Authority so others see the launch
            requestStateAuthoritySafe(result);

            // 5. RANDOMIZE PROPERTIES
            spawnedGrabbable.method("set_scaleModifier").invoke(rand(-128, 127));
            spawnedGrabbable.method("set_colorHue").invoke(rand(-127, 127));
            spawnedGrabbable.method("set_colorSaturation").invoke(rand(-127, 127));

            // 6. LAUNCH PHYSICS
            let forward = handTransform.method("get_forward").invoke();
            // Add slight spread/jitter to the launch direction
            forward.x += (Math.random() - 0.5) * 0.3;
            forward.y += (Math.random() - 0.5) * 0.3;
            forward.z += (Math.random() - 0.5) * 0.3;

            const velocity = Vector3.method("op_Multiply", 2).invoke(forward, 15);
            
            const velMethod = tryMethod(spawnedGrabbable, "AddExternalForceVelocity", 1) || 
                              tryMethod(spawnedGrabbable, "AddForce", 1);
            
            if (velMethod) velMethod.invoke(velocity);
            else {
                const rb = result.method("GetComponent", 1).inflate(Rigidbody).invoke();
                if (rb && !rb.isNull()) {
                    rb.method("set_isKinematic").invoke(false);
                    try { rb.method("WakeUp").invoke(); } catch (_) { }
                    rb.method("set_linearVelocity").invoke(velocity);
                }
            }

        } catch (e) {
            console.error("Random Item Launcher error: " + e);
        }
    },
    isTogglable: true,
    toolTip: "Launches any RANDOM item EXCEPT the crate (hold B)."
}),
                new ButtonInfo({
                    buttonText: "Snowball minigun (works))",
                    isTogglable: true,
                    method: () => {
                        try {
                            let sexyassdelay = 0;
                            function rand(min, max) {
                                return Math.floor(Math.random() * (max - min + 1)) + min;
                            }
                            if (!rightGrab) return;
                            if (!rightTrigger) return;
                            if (time < sexyassdelay) return;
                            sexyassdelay = time + 0.00;
                            const pos = rightHandTransform.method("get_position").invoke();
                            const forward = rightHandTransform.method("get_forward").invoke();
                            const result = PrefabGen.method("SpawnItem", 4).invoke(
                                Il2Cpp.string("item_prefab/item_snowball"),
                                pos, identityQuaternion, NULL
                            );
                            if (!result || result.handle.isNull()) return;
                            const g = result.method("GetComponent", 1).inflate(GBOClass).invoke();
                            if (g && !g.isNull()) {
                                g.method("set_colorHue").invoke(rand(-120, 120));
                                g.method("set_colorSaturation").invoke(rand(-127, 127));
                                g.method("set_scaleModifier").invoke(127);
                                g.method("AddExternalForceVelocity", 1).invoke(
                                    Vector3.method("op_Multiply", 2).invoke(forward, 100)
                                );
                            }
                        } catch (e) { console.error("sperm error:", e); }
                    },
                    toolTip: "Spawns white snowballs in a stream (hold right grip)."
                }),
                new ButtonInfo({
                    buttonText: "apple minigun",
                    isTogglable: true,
                    method: () => {
                        try {
                            let sexyassdelay = 0;
                            function rand(min, max) {
                                return Math.floor(Math.random() * (max - min + 1)) + min;
                            }
                            if (!rightGrab) return;
                            if (!rightTrigger) return;
                            if (time < sexyassdelay) return;
                            sexyassdelay = time + 0.00;
                            const pos = rightHandTransform.method("get_position").invoke();
                            const forward = rightHandTransform.method("get_forward").invoke();
                            const result = PrefabGen.method("SpawnItem", 4).invoke(
                                Il2Cpp.string("item_prefab/item_apple"),
                                pos, identityQuaternion, NULL
                            );
                            if (!result || result.handle.isNull()) return;
                            const g = result.method("GetComponent", 1).inflate(GBOClass).invoke();
                            if (g && !g.isNull()) {
                                g.method("set_colorHue").invoke(rand(-120, 120));
                                g.method("set_colorSaturation").invoke(rand(-127, 127));
                                g.method("set_scaleModifier").invoke(127);
                                g.method("AddExternalForceVelocity", 1).invoke(
                                    Vector3.method("op_Multiply", 2).invoke(forward, 100)
                                );
                            }
                        } catch (e) { console.error("sperm error:", e); }
                    },
                    toolTip: "Spawns apple in a stream (hold right grip)."
                }),
                new ButtonInfo({
                    buttonText: "cracker MiniGun",
                    isTogglable: true,
                    method: () => {
                        try {
                            let sexyassdelay = 0;
                            function rand(min, max) {
                                return Math.floor(Math.random() * (max - min + 1)) + min;
                            }
                            if (!rightGrab) return;
                            if (!rightTrigger) return;
                            if (time < sexyassdelay) return;
                            sexyassdelay = time + 0.00;
                            const pos = rightHandTransform.method("get_position").invoke();
                            const forward = rightHandTransform.method("get_forward").invoke();
                            const result = PrefabGen.method("SpawnItem", 4).invoke(
                                Il2Cpp.string("item_prefab/item_cracker"),
                                pos, identityQuaternion, NULL
                            );
                            if (!result || result.handle.isNull()) return;
                            const g = result.method("GetComponent", 1).inflate(GBOClass).invoke();
                            if (g && !g.isNull()) {
                                g.method("set_colorHue").invoke(rand(-120, 120));
                                g.method("set_colorSaturation").invoke(rand(-127, 127));
                                g.method("set_scaleModifier").invoke(127);
                                g.method("AddExternalForceVelocity", 1).invoke(
                                    Vector3.method("op_Multiply", 2).invoke(forward, 100)
                                );
                            }
                        } catch (e) { console.error("sperm error:", e); }
                    },
                    toolTip: "Spawns arenapistol in a stream (hold right grip)."
                }), 
                new ButtonInfo({
                    buttonText: "Black Crate MiniGun",
                    isTogglable: true,
                    method: () => {
                        try {
                            let sexyassdelay = 1;
                            function rand(min, max) {
                                return Math.floor(Math.random() * (max - min + 1)) + min;
                            }
                            if (!rightGrab) return;
                            if (!rightTrigger) return;
                            if (time < sexyassdelay) return;
                            sexyassdelay = time + 0.00;
                            const pos = rightHandTransform.method("get_position").invoke();
                            const forward = rightHandTransform.method("get_forward").invoke();
                            const result = PrefabGen.method("SpawnItem", 4).invoke(
                                Il2Cpp.string("item_prefab/item_pelican_case"),
                                pos, identityQuaternion, NULL
                            );
                            if (!result || result.handle.isNull()) return;
                            const g = result.method("GetComponent", 1).inflate(GBOClass).invoke();
                            if (g && !g.isNull()) {
                                g.method("set_colorHue").invoke(rand(-120, 120));
                                g.method("set_colorSaturation").invoke(rand(-127, 127));
                                g.method("set_scaleModifier").invoke(10);
                                g.method("AddExternalForceVelocity", 1).invoke(
                                    Vector3.method("op_Multiply", 2).invoke(forward, 100)
                                );
                            }
                        } catch (e) { console.error("sperm error:", e); }
                    },
                    toolTip: "Spawns arenapistol in a stream (hold right grip)."
                }),
                new ButtonInfo({
                    buttonText: "arenapistol MiniGun",
                    isTogglable: true,
                    method: () => {
                        try {
                            let sexyassdelay = 0;
                            function rand(min, max) {
                                return Math.floor(Math.random() * (max - min + 1)) + min;
                            }
                            if (!rightGrab) return;
                            if (!rightTrigger) return;
                            if (time < sexyassdelay) return;
                            sexyassdelay = time + 0.00;
                            const pos = rightHandTransform.method("get_position").invoke();
                            const forward = rightHandTransform.method("get_forward").invoke();
                            const result = PrefabGen.method("SpawnItem", 4).invoke(
                                Il2Cpp.string("item_prefab/item_arena_pistol"),
                                pos, identityQuaternion, NULL
                            );
                            if (!result || result.handle.isNull()) return;
                            const g = result.method("GetComponent", 1).inflate(GBOClass).invoke();
                            if (g && !g.isNull()) {
                                g.method("set_colorHue").invoke(rand(-120, 120));
                                g.method("set_colorSaturation").invoke(rand(-127, 127));
                                g.method("set_scaleModifier").invoke(127);
                                g.method("AddExternalForceVelocity", 1).invoke(
                                    Vector3.method("op_Multiply", 2).invoke(forward, 100)
                                );
                            }
                        } catch (e) { console.error("sperm error:", e); }
                    },
                    toolTip: "Spawns arenapistol in a stream (hold right grip)."
                
                }),
                new ButtonInfo({
                    buttonText: "cola MiniGun",
                    isTogglable: true,
                    method: () => {
                        try {
                            let sexyassdelay = 0;
                            function rand(min, max) {
                                return Math.floor(Math.random() * (max - min + 1)) + min;
                            }
                            if (!rightGrab) return;
                            if (!rightTrigger) return;
                            if (time < sexyassdelay) return;
                            sexyassdelay = time + 0.00;
                            const pos = rightHandTransform.method("get_position").invoke();
                            const forward = rightHandTransform.method("get_forward").invoke();
                            const result = PrefabGen.method("SpawnItem", 4).invoke(
                                Il2Cpp.string("item_prefab/item_cola"),
                                pos, identityQuaternion, NULL
                            );
                            if (!result || result.handle.isNull()) return;
                            const g = result.method("GetComponent", 1).inflate(GBOClass).invoke();
                            if (g && !g.isNull()) {
                                g.method("set_colorHue").invoke(rand(-120, 120));
                                g.method("set_colorSaturation").invoke(rand(-127, 127));
                                g.method("set_scaleModifier").invoke(127);
                                g.method("AddExternalForceVelocity", 1).invoke(
                                    Vector3.method("op_Multiply", 2).invoke(forward, 100)
                                );
                            }
                        } catch (e) { console.error("sperm error:", e); }
                    },
                    toolTip: "Spawns arenapistol in a stream (hold right grip)."
                }),
                new ButtonInfo({
                    buttonText: "sticky bomb MiniGun",
                    isTogglable: true,
                    method: () => {
                        try {
                            let sexyassdelay = 0;
                            function rand(min, max) {
                                return Math.floor(Math.random() * (max - min + 1)) + min;
                            }
                            if (!rightGrab) return;
                            if (!rightTrigger) return;
                            if (time < sexyassdelay) return;
                            sexyassdelay = time + 0.00;
                            const pos = rightHandTransform.method("get_position").invoke();
                            const forward = rightHandTransform.method("get_forward").invoke();
                            const result = PrefabGen.method("SpawnItem", 4).invoke(
                                Il2Cpp.string("item_prefab/item_sticky_dynamite"),
                                pos, identityQuaternion, NULL
                            );
                            if (!result || result.handle.isNull()) return;
                            const g = result.method("GetComponent", 1).inflate(GBOClass).invoke();
                            if (g && !g.isNull()) {
                                g.method("set_colorHue").invoke(rand(-120, 120));
                                g.method("set_colorSaturation").invoke(rand(-127, 127));
                                g.method("set_scaleModifier").invoke(127);
                                g.method("AddExternalForceVelocity", 1).invoke(
                                    Vector3.method("op_Multiply", 2).invoke(forward, 100)
                                );
                            }
                        } catch (e) { console.error("sperm error:", e); }
                    },
                    toolTip: "Spawns arenapistol in a stream (hold right grip)."
                
                }),
                new ButtonInfo({
                    buttonText: "Spawn Ore Quiver",
                    method: () => {
                        if (!rightSecondary) return;
                        const result = spawnOreQuiver();
                        if (result) sendNotification("Spawned ore quiver with " + result.insertedCount + " ores!", false);
                    },
                    isTogglable: true,
                    toolTip: "Spawns a randomized quiver filled with randomized ores."
                }),
new ButtonInfo({
    buttonText: "quiver launcher",
    isTogglable: true,
    method: () => {
        // TRIGGER: Hold Right Grip + Right Trigger
        if (!rightGrab || !rightTrigger || time < itemGunDelay) return;
        itemGunDelay = time + 0.2; // Cooldown

        try {
            // 1. GET RIGHT HAND DATA
            const pos = rightHandTransform.method("get_position").invoke();
            const rot = rightHandTransform.method("get_rotation").invoke();
            const forward = rightHandTransform.method("get_forward").invoke();

            // 2. SPAWN (Ensure your spawnFilledQuiver helper can take pos/rot)
            // If it doesn't take arguments, you may need to update the helper function itself!
            const result = spawnFilledQuiver(null, pos, rot);

            if (!result || !result.netObj || result.netObj.isNull()) return;

            // 3. LAUNCH PHYSICS
            const quiverGBO = result.netObj.method("GetComponent", 1).inflate(GBOClass).invoke();
            
            if (quiverGBO && !quiverGBO.isNull()) {
                // Request Authority (Mandatory for newer updates)
                requestStateAuthoritySafe(result.netObj);

                // Calculate Velocity (Forward direction of Right Hand * 35 speed)
                const velocity = Vector3.method("op_Multiply", 2).invoke(forward, 35.0);

                // Try modern physics method first
                const velMethod = tryMethod(quiverGBO, "AddExternalForceVelocity", 1) || 
                                  tryMethod(quiverGBO, "AddForce", 1);
                
                if (velMethod) velMethod.invoke(velocity);
                else {
                    const rb = result.netObj.method("GetComponent", 1).inflate(Rigidbody).invoke();
                    if (rb && !rb.isNull()) {
                        rb.method("set_isKinematic").invoke(false);
                        try { rb.method("WakeUp").invoke(); } catch (_) { }
                        rb.method("set_linearVelocity").invoke(velocity);
                    }
                }
            }
            
            sendNotification(`Launched ${result.insertedCount}-item quiver!`, false);

        } catch (e) {
            console.error("Quiver Launcher Error: " + e);
        }
    },
    toolTip: "Hold right grip + trigger to fire a closed 15-item quiver from your RIGHT hand."
}),
            new ButtonInfo({
                buttonText: "Spawn Items Gun",
                isTogglable: true,
                method: () => {
                    if (!rightGrab)
                        return;
                    const gunData = renderGun();
                    const ray = gunData.ray;
                    if (!ray || ray.handle.isNull())
                        return;
                    if (rightTrigger && time > itemGunDelay) {
                        itemGunDelay = time + 0.1;
                        try {
                            const hitPoint = ray.method("get_point").invoke();
                            const result = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + itemIDs[itemIndex]), hitPoint, identityQuaternion, 
                            // @ts-ignore
                            NULL);
                            if (!result || result.handle.isNull()) {
                                sendNotification("Spawn returned null: " + itemIDs[itemIndex], false);
                            }
                            else {
                                console.log("\u2713 Spawned item:", itemIDs[itemIndex]);
                                sendNotification("Spawned: " + itemIDs[itemIndex], false);
                            }
                        }
                        catch (e) {
                            console.error("Item spawn error:", e);
                            sendNotification("Spawn failed: " + e, false);
                        }
                    }
                },
                toolTip: "Spawns items where you aim (hold grip, pull trigger)."
            }),
            new ButtonInfo({
                buttonText: "Spawn Mob",
                method: () => {
                    const handTransform = rightHandTransform;
                    if (rightSecondary && rightGrab) {
                        try {
                            const result = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("mob_prefab/" + mobIDs[mobIndex]), handTransform.method("get_position").invoke(), handTransform.method("get_rotation").invoke(), 
                            // @ts-ignore
                            NULL);
                            if (!result || result.handle.isNull()) {
                                sendNotification("Mob spawn returned null: " + mobIDs[mobIndex], false);
                            }
                            else {
                                sendNotification("Spawned mob: " + mobIDs[mobIndex], false);
                            }
                        }
                        catch (e) {
                            console.error("Mob hand spawn error:", e);
                            sendNotification("Mob spawn failed: " + e, false);
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Spawns mob at your right hand (hold grip + B)."
            }),
            new ButtonInfo({
                buttonText: "Spawn Mob Gun",
                isTogglable: true,
                method: () => {
                    if (!rightGrab)
                        return;
                    const gunData = renderGun();
                    const ray = gunData.ray;
                    if (!ray || ray.handle.isNull())
                        return;
                    if (rightTrigger && time > mobGunDelay2) {
                        mobGunDelay2 = time + 0.3;
                        try {
                            const hitPoint = ray.method("get_point").invoke();
                            const result = PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("mob_prefab/" + mobIDs[mobIndex]), hitPoint, identityQuaternion, 
                            // @ts-ignore
                            NULL);
                            if (!result || result.handle.isNull()) {
                                sendNotification("Mob spawn returned null: " + mobIDs[mobIndex], false);
                            }
                            else {
                                sendNotification("Spawned mob: " + mobIDs[mobIndex], false);
                            }
                        }
                        catch (e) {
                            console.error("Mob gun spawn error:", e);
                            sendNotification("Mob spawn failed: " + e, false);
                        }
                    }
                },
                toolTip: "Spawns mob where you aim (hold grip, pull trigger)."
            }),
            new ButtonInfo({
                    buttonText: "Delete All Items (click twice)",
                    method: () => {
                        try {
                            const runner = PrefabGen.field("_instance").value.method("get_runner").invoke();
                            if (!runner || runner.isNull()) return;

                            // Find every GrabbableObject in the scene
                            const allItems = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                            let count = 0;

                            for (let i = 0; i < allItems.length; i++) {
                                const item = allItems.get(i);
                                if (!item || item.handle.isNull()) continue;

                                try {
                                    const netObj = item.method("get_Object").invoke();
                                    if (!netObj || netObj.isNull()) continue;

                                    // --- THE AUTHORITY TRICK ---
                                    // This forces the server to give YOU control of the item, 
                                    // even if it's in someone else's hand.
                                    netObj.method("RequestStateAuthority").invoke();

                                    // Now that we (theoretically) have authority, delete it
                                    runner.method("Despawn").invoke(netObj);
                                    count++;
                                } catch (_) { }
                            }
                            sendNotification(`NUCLEAR CLEANUP: Deleted ${count} items.`, false);
                        } catch (e) { console.error("Nuclear Delete Error: " + e); }
                    },
                    isTogglable: false,
                    toolTip: "Takes authority and deletes EVERY item, including those in players' hands."
                }),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Gun mods",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Lake [Gun]",
                isTogglable: true,
                method: () => {
                    if (!rightGrab) return;
                    const gunData = renderGun();
                    if (!rightTrigger || !gunData.ray || gunData.ray.isNull() || time <= lagGunDelay) return;
                    lagGunDelay = time + 0.2;
                    try {
                        const hitCollider = gunData.ray.method("get_collider").invoke();
                        const target = getComponentInParent(hitCollider, NetPlayer);
                        if (!target || target.isNull()) return;
                        if (tryTeleportNetPlayer(target, TELEPORT_TO_LAKE)) {
                            sendNotification("Teleported target to Lake.", false);
                        }
                    } catch (_) { }
                },
                toolTip: "Aim at a player and pull trigger to teleport them to the Lake coordinates."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Moon [Gun]",
                isTogglable: true,
                method: () => {
                    if (!rightGrab) return;
                    const gunData = renderGun();
                    if (!rightTrigger || !gunData.ray || gunData.ray.isNull() || time <= lagGunDelay) return;
                    lagGunDelay = time + 0.2;
                    try {
                        const hitCollider = gunData.ray.method("get_collider").invoke();
                        const target = getComponentInParent(hitCollider, NetPlayer);
                        if (!target || target.isNull()) return;
                        if (tryTeleportNetPlayer(target, TELEPORT_TO_MOON)) {
                            sendNotification("Teleported target to Moon.", false);
                        }
                    } catch (_) { }
                },
                toolTip: "Aim at a player and pull trigger to teleport them to the Moon coordinates."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Sewers [Gun]",
                isTogglable: true,
                method: () => {
                    if (!rightGrab) return;
                    const gunData = renderGun();
                    if (!rightTrigger || !gunData.ray || gunData.ray.isNull() || time <= lagGunDelay) return;
                    lagGunDelay = time + 0.2;
                    try {
                        const hitCollider = gunData.ray.method("get_collider").invoke();
                        const target = getComponentInParent(hitCollider, NetPlayer);
                        if (!target || target.isNull()) return;
                        if (tryTeleportNetPlayer(target, TELEPORT_TO_SEWERS)) {
                            sendNotification("Teleported target to Sewers.", false);
                        }
                    } catch (_) { }
                },
                toolTip: "Aim at a player and pull trigger to teleport them to the Sewers coordinates."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Spawn [Gun]",
                isTogglable: true,
                method: () => {
                    if (!rightGrab) return;
                    const gunData = renderGun();
                    if (!rightTrigger || !gunData.ray || gunData.ray.isNull() || time <= lagGunDelay) return;
                    lagGunDelay = time + 0.2;
                    try {
                        const hitCollider = gunData.ray.method("get_collider").invoke();
                        const target = getComponentInParent(hitCollider, NetPlayer);
                        if (!target || target.isNull()) return;
                        if (tryTeleportNetPlayer(target, TELEPORT_TO_SPAWN)) {
                            sendNotification("Teleported target to Spawn.", false);
                        }
                    } catch (_) { }
                },
                toolTip: "Aim at a player and pull trigger to teleport them to the Spawn coordinates."
            }),
            new ButtonInfo({
                buttonText: "Teleport To Above Water [Gun]",
                isTogglable: true,
                method: () => {
                    if (!rightGrab) return;
                    const gunData = renderGun();
                    if (!rightTrigger || !gunData.ray || gunData.ray.isNull() || time <= lagGunDelay) return;
                    lagGunDelay = time + 0.2;
                    try {
                        const hitCollider = gunData.ray.method("get_collider").invoke();
                        const target = getComponentInParent(hitCollider, NetPlayer);
                        if (!target || target.isNull()) return;
                        if (tryTeleportNetPlayer(target, TELEPORT_TO_ABOVELAKE)) {
                            sendNotification("Teleported target to Above Water.", false);
                        }
                    } catch (_) { }
                },
                toolTip: "Aim at a player and pull trigger to teleport them to the Above Water coordinates."
            }),
            new ButtonInfo({
                buttonText: "Teleport To WaterTower [Gun]",
                isTogglable: true,
                method: () => {
                    if (!rightGrab) return;
                    const gunData = renderGun();
                    if (!rightTrigger || !gunData.ray || gunData.ray.isNull() || time <= lagGunDelay) return;
                    lagGunDelay = time + 0.2;
                    try {
                        const hitCollider = gunData.ray.method("get_collider").invoke();
                        const target = getComponentInParent(hitCollider, NetPlayer);
                        if (!target || target.isNull()) return;
                        if (tryTeleportNetPlayer(target, TELEPORT_TO_WATER_TOWER)) {
                            sendNotification("Teleported target to WaterTower.", false);
                        }
                    } catch (_) { }
                },
                toolTip: "Aim at a player and pull trigger to teleport them to the WaterTower coordinates."
            }),
            new ButtonInfo({
                buttonText: "Disintegrate Gun",
                method: () => {
                    if (rightGrab) {
                        const gunData = renderGun();
                        const gunPointer = gunData.gunPointer;
                        if (rightTrigger && time > lagGunDelay) {
                            lagGunDelay = time + 0.5;
                            try {
                                const pointerPos = getTransform(gunPointer).method("get_position").invoke();
                                let lowestDistance = Number.MAX_SAFE_INTEGER;
                                let nearestPlayer = null;
                                const playerDict = NetPlayer.field("playerIDToNetPlayer").value;
                                const playerValues = playerDict.method("get_Values").invoke();
                                const enumerator = playerValues.method("GetEnumerator").invoke();
                                while (enumerator.method("MoveNext").invoke()) {
                                    const player = enumerator.method("get_Current").invoke();
                                    if (!player || player.handle.isNull())
                                        continue;
                                    if (playerIsLocal(player))
                                        continue;
                                    const playerPos = getTransform(player).method("get_position").invoke();
                                    const dist = Vector3.method("Distance").invoke(pointerPos, playerPos);
                                    if (dist < lowestDistance) {
                                        lowestDistance = dist;
                                        nearestPlayer = player;
                                    }
                                }
                                if (nearestPlayer && !nearestPlayer.handle.isNull()) {
                                    const targetPos = getTransform(nearestPlayer).method("get_position").invoke();
                                    const allVFX = [
                                        VFXTypes.MuzzleFlash_Shotgun,
                                        VFXTypes.MuzzleFlash_FlareGun,
                                        VFXTypes.CrateBreak,
                                        VFXTypes.MuzzleFlash_SmallGun,
                                        VFXTypes.MuzzleFlash_GoldRevolver,
                                        VFXTypes.MuzzleFlash_DragonPistol,
                                        VFXTypes.MuzzleFlash_ViperShotgun,
                                        VFXTypes.Explosion_FlareGun,
                                        VFXTypes.Explosion_Coins,
                                        VFXTypes.Explosion_Nuts,
                                        VFXTypes.Explosion_Keys,
                                        VFXTypes.Explosion_Balloon,
                                        VFXTypes.Explosion_TeleGrenadeSrc,
                                        VFXTypes.Player_Touch_Lava,
                                        VFXTypes.Portal_Teleport,
                                        VFXTypes.Explosion_Coins_Vertical,
                                        VFXTypes.Autumn_Leaves_Burst,
                                        VFXTypes.Explosion_Feathers,
                                        VFXTypes.Explosion_Popcorn,
                                        VFXTypes.Electricity_Small,
                                        VFXTypes.Impact_Flaregun,
                                        VFXTypes.Impact_Snowball,
                                        VFXTypes.Impact_GoldRevolver,
                                        VFXTypes.Impact_MeleeHit,
                                        VFXTypes.Impact_BigGroundHit,
                                        VFXTypes.Impact_MeleeHit_CriticalSmall,
                                        VFXTypes.Impact_MeleeHit_CriticalLarge,
                                        VFXTypes.Impact_MeleeHit_AoE,
                                        VFXTypes.Research_ZiplineAttachDetach,
                                        VFXTypes.Research_Purchase1RP,
                                        VFXTypes.Research_Purchase5RP,
                                        VFXTypes.Research_Purchase10RP,
                                        VFXTypes.Research_PurchaseRPBundle,
                                        VFXTypes.Rope_ZiplineAttachDetach,
                                        VFXTypes.MeatExplosion_1,
                                        VFXTypes.MeatExplosion_2,
                                        VFXTypes.MeatExplosion_Headshot,
                                        VFXTypes.ServerRoomSplash_Small,
                                        VFXTypes.ServerRoomSplash_Big,
                                        VFXTypes.RAMActivationSparks,
                                        VFXTypes.GreenBlink,
                                        VFXTypes.ConfettiBurst,
                                        VFXTypes.Ethereal_Void,
                                        VFXTypes.MomBoss_NailBreak,
                                        VFXTypes.MidAirJump_Fart,
                                        VFXTypes.FuelExplosion
                                    ];
                                    const NetworkRunner = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                                    for (const vfxType of allVFX) {
                                        ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(NetworkRunner, vfxType, targetPos, identityQuaternion);
                                    }
                                    const transform = nearestPlayer.method("get_transform").invoke();
                                    const forward = transform.method("get_forward").invoke();
                                    const forceVec = Vector3.method("op_Multiply", 2).invoke(forward, 1500 * deltaTime);
                                    nearestPlayer.method("RPC_Teleport").invoke([0, -9999999, 0]);
                                    nearestPlayer.method("RPC_AddForce").invoke(forceVec);
                                    nearestPlayer.method("RPC_SetColorHSV").invoke(NaN, NaN, NaN, NaN);
                                    sendNotification("Disintegrated nearest player to pointer", false);
                                    console.log("✓ Disintegrated nearest player to gun pointer with all VFX");
                                }
                            }
                            catch (e) {
                                console.error("Disintegrate gun error:", e);
                            }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Disintegrates player nearest to gun pointer with ALL VFX (hold grip, pull trigger)"
            }),
            new ButtonInfo({
                    buttonText: "apply all buffs gun",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        if (rightTrigger && gunData.ray) {
                            try {
                                const target = getComponentInParent(gunData.ray.method("get_collider").invoke(), NetPlayer);
                                if (target && !target.isNull()) {
                                    target.method("RequestStateAuthority").invoke();

                                    // Apply Buff ID 1 (Change this number to find cool effects!)
                                    target.method("RPC_ApplyBuff").invoke(1);
                                    sendNotification("Buff Applied to Player!", false);
                                }
                            } catch (e) { }
                        }
                    },
                    toolTip: "Shoots a networked buff at a player. Try changing the ID (1, 2, 3...)."
                }),
                new ButtonInfo({
                    buttonText: "haunted house light thingy gun",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return; // Must hold Grip to aim
                        const gunData = renderGun(); // Uses your laser logic
                        const ray = gunData.ray;

                        if (rightTrigger && ray && !ray.isNull() && time > lagGunDelay) {
                            lagGunDelay = time + 0.3; // Small cooldown to prevent spamming

                            try {
                                // 1. Get the collider your laser is hitting
                                const hitCollider = ray.method("get_collider").invoke();

                                // 2. Find the Flashlight component
                                const FlashlightClass = AssemblyCSharp.class("AnimalCompany.Flashlight");
                                const flashlight = getComponentInParent(hitCollider, FlashlightClass);

                                if (flashlight && !flashlight.isNull()) {
                                    // --- THE AUTHORITY FIX ---
                                    // Since RPCs often fail if you don't 'own' the item, 
                                    // we request authority over the NetworkObject first.
                                    const netObj = flashlight.method("get_Object").invoke();
                                    if (netObj && !netObj.isNull()) {
                                        netObj.method("RequestStateAuthority").invoke();
                                    }

                                    // 3. TRIGGER THE EFFECT
                                    // This calls the specific RPC from your dump
                                    flashlight.method("RPC_TriggerGhostLightEffect").invoke();

                                    sendNotification("Ghost Effect Triggered!", false);
                                    console.log("✓ Ghost Effect sent to Flashlight via RPC.");
                                } else {
                                    sendNotification("Not a Flashlight!", false);
                                }
                            } catch (e) {
                                console.error("Ghost Light Gun Error: " + e);
                            }
                        }
                    },
                    toolTip: "Aim at a flashlight and pull trigger to make it flicker/glitch for everyone."
                }),
                new ButtonInfo({
                    buttonText: "instakill gun",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        if (rightTrigger && gunData.ray && !gunData.ray.isNull()) {
                            try {
                                const hitCollider = gunData.ray.method("get_collider").invoke();
                                const target = getComponentInParent(hitCollider, NetPlayer);

                                if (target && !target.isNull()) {
                                    const netObj = target.method("get_Object").invoke();
                                    netObj.method("RequestStateAuthority").invoke();

                                    const pos = gunData.ray.method("get_point").invoke();

                                    // Call RPC_PlayerHit(damage, pos, hitName, sourceID)
                                    // We pass 9999 damage and '0' for source
                                    target.method("RPC_PlayerHit", 4).invoke(9999, pos, Il2Cpp.string("Explosion"), 0);

                                    sendNotification("Player Eliminated via RPC!", false);
                                }
                            } catch (e) { }
                        }
                    },
                    toolTip: "Sends a lethal damage packet to the target player via RPC."
                }),
                new ButtonInfo({
                    buttonText: "pop balloon gun idk",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        if (rightTrigger && gunData.ray && !gunData.ray.isNull()) {
                            try {
                                const balloon = getComponentInParent(gunData.ray.method("get_collider").invoke(), AssemblyCSharp.class("AnimalCompany.Balloon"));
                                if (balloon && !balloon.isNull()) {
                                    const netObj = balloon.method("get_Object").invoke();
                                    netObj.method("RequestStateAuthority").invoke();

                                    // Force the pop effect via RPC
                                    balloon.method("RPC_Pop").invoke();
                                    sendNotification("Balloon Popped!", false);
                                }
                            } catch (e) { }
                        }
                    },
                    toolTip: "Snipes any balloon and forces it to pop via RPC."
                }),
                new ButtonInfo({
                    buttonText: "make ore spit out 255 ores",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        if (rightTrigger && gunData.ray && !gunData.ray.isNull()) {
                            try {
                                const vein = getComponentInParent(gunData.ray.method("get_collider").invoke(), AssemblyCSharp.class("AnimalCompany.GameGen.OreVein"));
                                if (vein && !vein.isNull()) {
                                    const netObj = vein.method("get_Object").invoke();
                                    netObj.method("RequestStateAuthority").invoke();

                                    // RPC_SpawnOre(byte oreID, byte count)
                                    // We send 255 for the count (max byte value)
                                    vein.method("RPC_SpawnOre").invoke(1, 255);
                                    sendNotification("Ore Vein Exploded with Resources!", false);
                                }
                            } catch (e) { }
                        }
                    },
                    toolTip: "Forces an ore vein to spawn 255 ores instantly."
                }),
                new ButtonInfo({
                    buttonText: "enable all boomboxes",
                    method: () => {
                        try {
                            const BoomboxClass = AssemblyCSharp.class("AnimalCompany.Boombox");
                            const allRadios = Object.method("FindObjectsByType", 1).inflate(BoomboxClass).invoke(0);

                            for (let i = 0; i < allRadios.length; i++) {
                                const radio = allRadios.get(i);
                                radio.method("get_Object").invoke().method("RequestStateAuthority").invoke();
                                radio.method("RPC_ToggleOnOff").invoke();
                            }
                            sendNotification("Toggled all Boomboxes!", false);
                        } catch (e) { }
                    },
                    isTogglable: false,
                    toolTip: "Toggles every boombox on the map simultaneously."
                }),
                new ButtonInfo({
                    buttonText: "unlock/use all doors or machines",
                    method: () => {
                        try {
                            // 1. Unlock Duplication Machines
                            const DupClass = AssemblyCSharp.class("AnimalCompany.DuplicationMachine");
                            const machines = Object.method("FindObjectsByType", 1).inflate(DupClass).invoke(0);
                            for (let i = 0; i < machines.length; i++) {
                                machines.get(i).method("get_Object").invoke().method("RequestStateAuthority").invoke();
                                machines.get(i).method("RPC_KeycardEnter").invoke();
                            }

                            // 2. Unlock High-Hazard Doors
                            const LockDoorClass = AssemblyCSharp.class("AnimalCompany.HHLockedDoor");
                            const doors = Object.method("FindObjectsByType", 1).inflate(LockDoorClass).invoke(0);
                            for (let i = 0; i < doors.length; i++) {
                                doors.get(i).method("get_Object").invoke().method("RequestStateAuthority").invoke();
                                doors.get(i).method("RPC_Open").invoke();
                            }
                            sendNotification("All Keycard Locks Bypassed!", false);
                        } catch (e) { }
                    },
                    isTogglable: false,
                    toolTip: "Remotely unlocks every keycard door and machine on the map via RPC."
                }),
                new ButtonInfo({
                    buttonText: "activate grenade gun",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        if (rightTrigger && gunData.ray && !gunData.ray.isNull()) {
                            try {
                                const explosive = getComponentInParent(gunData.ray.method("get_collider").invoke(), AssemblyCSharp.class("AnimalCompany.HandExplosive"));
                                if (explosive && !explosive.isNull()) {
                                    explosive.method("get_Object").invoke().method("RequestStateAuthority").invoke();

                                    // RPC_Use(shortTimer = true)
                                    explosive.method("RPC_Use").invoke(true, false);
                                    sendNotification("Grenade Cooked Remotely!", false);
                                }
                            } catch (e) { }
                        }
                    },
                    toolTip: "Forces a held grenade to pull its own pin via RPC."
                }),
                new ButtonInfo({
                    buttonText: "fling gun",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        if (rightTrigger && gunData.ray) {
                            try {
                                const hitCollider = gunData.ray.method("get_collider").invoke();
                                const upForce = [0, 500, 0]; // Massive upward force

                                // Check for Player
                                const targetPlr = getComponentInParent(hitCollider, NetPlayer);
                                if (targetPlr && !targetPlr.isNull()) {
                                    targetPlr.method("RPC_AddForce").invoke(upForce);
                                    sendNotification("Yeeted Player!", false);
                                    return;
                                }

                                // Check for Item
                                const targetItem = getComponentInParent(hitCollider, GBOClass);
                                if (targetItem && !targetItem.isNull()) {
                                    // ForceMode 2 = Impulse (instant blast)
                                    targetItem.method("RPC_AddExternalForce").invoke(upForce, 2);
                                    sendNotification("Yeeted Item!", false);
                                }
                            } catch (e) { }
                        }
                    },
                    toolTip: "Shoots a massive upward force via RPC. Works on players and items."
                }),
                new ButtonInfo({
                    buttonText: "rainbow player gun",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        if (rightTrigger && gunData.ray) {
                            try {
                                const targetPlr = getComponentInParent(gunData.ray.method("get_collider").invoke(), NetPlayer);
                                if (targetPlr && !targetPlr.isNull()) {
                                    // Duration, Hue, Saturation, Brightness
                                    // Setting Hue to a random value every trigger pull
                                    const randomHue = Math.random();
                                    targetPlr.method("RPC_SetColorHSV").invoke(5.0, randomHue, 1.0, 1.0);
                                }
                            } catch (e) { }
                        }
                    },
                    toolTip: "Forces the target player to change color via RPC."
                }),
                new ButtonInfo({
                    buttonText: "heavy item gun",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return; // Hold Grip to aim
                        const gunData = renderGun();

                        if (rightTrigger && gunData.ray && !gunData.ray.isNull()) {
                            try {
                                // 1. Get the physical object from the laser hit
                                const hitCollider = gunData.ray.method("get_collider").invoke();

                                // --- PART A: PHYSICAL WEIGHT (Mass) ---
                                // This makes it heavy on the floor
                                const rb = getComponentInParent(hitCollider, Rigidbody);
                                if (rb && !rb.isNull()) {
                                    rb.method("set_mass").invoke(10000.0); // 10 Tons
                                    rb.method("WakeUp").invoke(); // Force physics to update
                                }

                                // --- PART B: CARRY WEIGHT (Inventory) ---
                                // This makes it slow the player down to 0 speed
                                // We use GBIClass (GrabbableItem) from your Part 1 setup
                                const gbi = getComponentInParent(hitCollider, GBIClass);
                                if (gbi && !gbi.isNull()) {
                                    try {
                                        // In most of these games, '1.0' is normal weight. 
                                        // '100.0' usually makes the player unable to move.
                                        // We try the method first, then the field if method fails.
                                        const setWeight = tryMethod(gbi, "set_itemWeight", 1) || tryMethod(gbi, "set_weight", 1);

                                        if (setWeight) {
                                            setWeight.invoke(100.0);
                                        } else {
                                            // Fallback: Try to find the field directly if the method name is different
                                            gbi.field("itemWeight").value = 100.0;
                                        }
                                    } catch (_) {
                                        // If 'itemWeight' isn't the name, we use the dumper to find it
                                        console.log("Could not find weight field, check dumper for 'weight' name");
                                    }
                                }

                                // Visual Feedback
                                sendNotification("Item is now EXTREMELY HEAVY", false);
                                // Optional: Change the item's color to Black to show it's 'Heavy'
                                const renderer = getComponentInParent(hitCollider, Renderer);
                                if (renderer) renderer.method("get_material").invoke().method("set_color").invoke([0, 0, 0, 1]);

                            } catch (e) {
                                console.error("Heavy Gun Error: " + e);
                            }
                        }
                    },
                    toolTip: "Makes ANY object impossible to move or carry (Sets Physics Mass & Inventory Weight)."
                }),
                new ButtonInfo({
                    buttonText: "zero weight gun",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return; // Must hold Grip to aim
                        const gunData = renderGun();
                        const ray = gunData.ray;

                        if (rightTrigger && ray && !ray.isNull()) {
                            try {
                                // 1. Get the collider the laser is hitting
                                const hitCollider = ray.method("get_collider").invoke();

                                // --- PART A: INVENTORY WEIGHT (Speed) ---
                                // This stops the item from slowing you down when held
                                const gbi = getComponentInParent(hitCollider, GBIClass);
                                if (gbi && !gbi.isNull()) {
                                    // We try to find the weight setter method (set_itemWeight or set_weight)
                                    const setWeight = tryMethod(gbi, "set_itemWeight", 1) ||
                                        tryMethod(gbi, "set_weight", 1);

                                    if (setWeight) {
                                        setWeight.invoke(0.0);
                                    } else {
                                        // Fallback: Try setting the field directly if the method isn't found
                                        try { gbi.field("itemWeight").value = 0.0; } catch (_) { }
                                        try { gbi.field("_itemWeight").value = 0.0; } catch (_) { }
                                    }
                                }

                                // --- PART B: PHYSICS MASS (Throwing) ---
                                // This makes the item light as air for the physics engine
                                const rb = getComponentInParent(hitCollider, Rigidbody);
                                if (rb && !rb.isNull()) {
                                    // Note: In Unity, setting mass to EXACTLY 0 can sometimes 
                                    // cause a crash, so we use 0.001 (weightless for players).
                                    rb.method("set_mass").invoke(0.001);
                                    rb.method("WakeUp").invoke();
                                }

                                sendNotification("Item is now Weightless!", false);
                                console.log("✓ Zero Weight applied to: " + hitCollider.method("get_name").invoke());

                            } catch (e) {
                                console.error("Zero Weight Gun Error: " + e);
                            }
                        }
                    },
                    toolTip: "Point and pull trigger to make any item weigh 0kg (won't slow you down)."
                }),
                new ButtonInfo({
                    buttonText: "max sell gun",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun(); // Uses your existing laser logic

                        // We check if the laser is actually hitting something
                        if (rightTrigger && gunData.ray && !gunData.ray.isNull()) {
                            try {
                                // 1. Get the collider the laser touched
                                const hitCollider = gunData.ray.method("get_collider").invoke();

                                if (hitCollider && !hitCollider.isNull()) {
                                    // 2. Use your existing helper to find the GBI component 
                                    // It will search the object AND its parents
                                    const gbi = getComponentInParent(hitCollider, GBIClass);

                                    if (gbi && !gbi.isNull()) {
                                        // 3. Set the value
                                        gbi.method("SetAdditionalSellValue").invoke(999999);

                                        // Visual feedback
                                        sendNotification("Item Value Set to 999,999!", false);
                                        console.log("✓ Maxed value of: " + gbi.method("get_name").invoke());
                                    } else {
                                        // If we hit something that isn't a grabbable item
                                        sendNotification("Not a sellable item!", false);
                                    }
                                }
                            } catch (e) {
                                console.error("Max Sell Gun Error: " + e);
                            }
                        }
                    },
                    toolTip: "Aim at one specific item and pull trigger to maximize its value."
                }),
                new ButtonInfo({
                    buttonText: "max sell gun v2",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        if (rightTrigger && gunData.ray) {
                            try {
                                const hitCollider = gunData.ray.method("get_collider").invoke();
                                const gbi = getComponentInParent(hitCollider, GBIClass);
                                if (gbi && !gbi.isNull()) {
                                    // This tells the server THIS item is worth 999,999
                                    gbi.method("RPC_SetAdditionalSellValue").invoke(999999);
                                    sendNotification("Item Value Set via RPC!", false);
                                }
                            } catch (e) { }
                        }
                    },
                    toolTip: "Uses the game's Network RPC to set an item's value to 999,999."
                }),
                
            new ButtonInfo({
                buttonText: "TP ALL Gun",
                method: () => {
                    if (!rightGrab)
                        return;
                    const gunData = renderGun();
                    const ray = gunData.ray;
                    if (!rightTrigger)
                        return;
                    if (!ray || ray.isNull())
                        return;
                    try {
                        const hitPoint = ray.method("get_point").invoke();
                        const playerDict = NetPlayer.field("playerIDToNetPlayer").value;
                        const playerValues = playerDict.method("get_Values").invoke();
                        const enumerator = playerValues.method("GetEnumerator").invoke();
                        while (enumerator.method("MoveNext").invoke()) {
                            const netPlayer = enumerator.method("get_Current").invoke();
                            if (!netPlayer || netPlayer.handle.isNull())
                                continue;
                            if (netPlayer.method("get_IsMine").invoke())
                                continue;
                            netPlayer.method("RPC_Teleport").invoke(hitPoint);
                        }
                    }
                    catch (e) {
                        console.error("TP ALL Gun error:", e);
                    }
                },
                isTogglable: true,
                toolTip: "Teleports all players to your gun pointer."
            }),
            new ButtonInfo({
                buttonText: "Kick Gun",
                method: () => {
                    if (rightGrab) {
                        const gunData = renderGun();
                        const ray = gunData.ray;
                        if (rightTrigger) {
                            const gunTarget = getComponentInParent(ray.method("get_collider").invoke(), NetPlayer);
                            if (gunTarget && !gunTarget.handle.isNull() && time > tagGunDelay) {
                                if (!playerIsLocal(gunTarget)) {
                                    kickPlayer(gunTarget);
                                }
                            }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Kicks whoever your hand desires."
            }),
            new ButtonInfo({
                buttonText: "Stinky Gun",
                method: () => {
                    if (rightGrab) {
                        const gunData = renderGun();
                        const ray = gunData.ray;
                        if (rightTrigger) {
                            const gunTarget = getComponentInParent(ray.method("get_collider").invoke(), NetPlayer);
                            if (gunTarget && !gunTarget.handle.isNull() && time > tagGunDelay) {
                                if (!playerIsLocal(gunTarget)) {
                                    gunTarget.method("RPC_TagAsStinky").invoke();
                                }
                            }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Stinkies whoever your hand desires."
            }),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Misc",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
                buttonText: "Stash Dupe",
                method: () => stashDupeEnabled = true,
                disableMethod: () => stashDupeEnabled = false,
                toolTip: "Lets you eject more times using your stash."
            }),
            new ButtonInfo({
                buttonText: "Change Eject Dupe Amount",
                method: () => {
                    ejectDupeIndex++;
                    ejectDupeIndex %= ejectDupeValues.length;
                    ejectDupeAmount = ejectDupeValues[ejectDupeIndex];
                    sendNotification("[MENU] New eject dupe amount: " + ejectDupeAmount, false);
                },
                isTogglable: false,
                toolTip: "Cycles through preset dupe amounts."
            }),
            new ButtonInfo({
                buttonText: "Infinite Ammo",
                enableMethod: () => { InfAmmo = true; },
                disableMethod: () => { InfAmmo = false; },
                isTogglable: true,
                toolTip: "Infinite ammo"
            }),
            new ButtonInfo({
                buttonText: "Rapid Fire",
                method: () => {
                    try {
                        const player = NetPlayer.method("get_localPlayer").invoke();
                        if (!player)
                            return;
                        const interactor = player.method("GetHandInteractor", 1).invoke(1);
                        if (!interactor)
                            return;
                        const itemAnchor = interactor.field("_itemAnchor").value;
                        if (!itemAnchor)
                            return;
                        const grabbable = itemAnchor.method("get_grabbableObject").invoke();
                        if (!grabbable)
                            return;
                        const interactor2 = player.method("GetHandInteractor", 1).invoke(0);
                        if (!interactor2)
                            return;
                        const itemAnchor2 = interactor2.field("_itemAnchor").value;
                        if (!itemAnchor2)
                            return;
                        const grabbable2 = itemAnchor2.method("get_grabbableObject").invoke();
                        if (!grabbable2)
                            return;
                        if (rightTrigger) {
                            grabbable.method("HandleTriggerUse").invoke();
                        }
                        if (leftTrigger) {
                            grabbable2.method("HandleTriggerUse").invoke();
                        }
                    }
                    catch (e) {
                        console.error(e);
                    }
                },
                isTogglable: true,
                toolTip: "Rapid fire any item used with trigger button."
            }),
            new ButtonInfo({
                buttonText: "Inf Damage",
                enableMethod: () => { infDamage = true; },
                disableMethod: () => { infDamage = false; },
                isTogglable: true,
                toolTip: "Makes all weapons deal 999999 damage per shot/hit."
            }),
            new ButtonInfo({
                buttonText: "No Recoil",
                enableMethod: () => { noRecoil = true; },
                disableMethod: () => { noRecoil = false; },
                isTogglable: true,
                toolTip: "Removes recoil force and spread on all guns (Revolver, Shotgun, Arena guns)."
            }),
            new ButtonInfo({
    buttonText: "VFX gun",
    isTogglable: true,
    method: () => {
        if (!rightGrab) return;
        const gunData = renderGun();
        const ray = gunData.ray;

        if (rightTrigger && ray && !ray.isNull() && time > vfxGunDelay) {
            vfxGunDelay = time + 0.1;
            try {
                const hitPos = ray.method("get_point").invoke();
                playSelectedVFXAt(hitPos);
            } catch (e) { }
        }
    }
}),
new ButtonInfo({
    buttonText: "spawn VFX at hand boi",
    isTogglable: true,
    method: () => {
        if (rightGrab && rightSecondary && time > tagGunDelay) {
            tagGunDelay = time + 0.1;
            try {
                const handPos = rightHandTransform.method("get_position").invoke();
                playSelectedVFXAt(handPos);
            } catch (e) { }
        }
    }
}),
new ButtonInfo({
    buttonText: "sfx",
    isTogglable: true,
    method: () => {
        // TRIGGER: Hold Right Grip + Right Secondary (B)
        if (rightGrab && rightSecondary && time > tagGunDelay) {
            tagGunDelay = time + 0.1; // Delay to prevent audio lag

            try {
                const handPos = rightHandTransform.method("get_position").invoke();
                playSelectedSFXAt(handPos);
            } catch (e) { }
        }
    }
}),
new ButtonInfo({
    buttonText: "SFX gun",
    isTogglable: true,
    method: () => {
        if (!rightGrab) return;
        const gunData = renderGun();
        const ray = gunData.ray;
        if (rightTrigger && ray && !ray.isNull() && time > tagGunDelay) {
            tagGunDelay = time + 0.1;
            try {
                const hitPos = ray.method("get_point").invoke();
                playSelectedSFXAt(hitPos);
            } catch (_) { }
        }
    },
    toolTip: "Aim and pull trigger to play the selected SFX where your laser lands."
}),
new ButtonInfo({
    buttonText: "All Items Grabbable",
    enableMethod: () => {
        try {
            GBOClass.method("get_canGrab").implementation = function () { 
                return true; 
            };
            try { GBOClass.method("get_forceDisableGrab").implementation = function () { return false; }; } catch (_) { }
            try { GBOClass.method("set_forceDisableGrab").implementation = function (_v) { }; } catch (_) { }
            try { GBOClass.method("CanBeGrabbedBy", 1).implementation = function (_who) { return true; }; } catch (_) { }
            try { GBOClass.method("get_canBePickedUp").implementation = function () { return true; }; } catch (_) { }
            try { GBIClass.method("get_canBePickedUp").implementation = function () { return true; }; } catch (_) { }
            try { GBIClass.method("get_canGrab").implementation = function () { return true; }; } catch (_) { }
            try { AssemblyCSharp.class("AnimalCompany.ItemGrabHandle").method("get_canGrab").implementation = function () { return true; }; } catch (_) { }
            
            sendNotification("All Grabbable ON", false);
        }
        catch (e) {
            sendNotification("All Grabbable: " + e, false);
        }
    },
    disableMethod: () => {
        try {
            GBOClass.method("get_canGrab").implementation = null;
            try { GBOClass.method("get_forceDisableGrab").implementation = null; } catch (_) { }
            try { GBOClass.method("set_forceDisableGrab").implementation = null; } catch (_) { }
            try { GBOClass.method("CanBeGrabbedBy", 1).implementation = null; } catch (_) { }
            try { GBOClass.method("get_canBePickedUp").implementation = null; } catch (_) { }
            try { GBIClass.method("get_canBePickedUp").implementation = null; } catch (_) { }
            try { GBIClass.method("get_canGrab").implementation = null; } catch (_) { }
            try { AssemblyCSharp.class("AnimalCompany.ItemGrabHandle").method("get_canGrab").implementation = null; } catch (_) { }
            
            sendNotification("All Grabbable OFF", false);
        }
        catch (e) { }
    },
    isTogglable: true,
    toolTip: "Any item can be grabbed regardless of ownership or state."
}),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Whitelist mods",
                method: () => { currentCategory = 5; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns to Other Players."
            }),
            new ButtonInfo({
                buttonText: "Whitelist gun",
                method: () => {
                    if (!rightGrab) {
                        gunColor = [0.08, 0.08, 0.08, 0.75];
                        return;
                    }
                    const gunData = renderGun();
                    const ray = gunData.ray;
                    let targetrig = null;
                    if (ray && !ray.isNull()) {
                        const gunTarget = getComponentInParent(ray.method("get_collider").invoke(), NetPlayer);
                        if (gunTarget && !gunTarget.handle.isNull() && !playerIsLocal(gunTarget)) {
                            targetrig = gunTarget;
                            let isWhitelisted = false;
                            for (let i = 0; i < whitelist.length; i++) {
                                if (whitelist[i].field("_userID").value.toString() === targetrig.field("_userID").value.toString()) {
                                    isWhitelisted = true;
                                    break;
                                }
                            }
                            gunColor = isWhitelisted ? [0.2, 1.0, 0.3, 1.0] : [1.0, 0.2, 0.2, 1.0];
                        }
                        else {
                            gunColor = [0.08, 0.08, 0.08, 0.75];
                        }
                    }
                    if (rightTrigger && time > idGunDelay) {
                        idGunDelay = time + 0.2;
                        if (targetrig && !targetrig.isNull()) {
                            let nn = [];
                            let v = false;
                            for (let i = 0; i < whitelist.length; i++) {
                                const rig = whitelist[i];
                                if (rig.field("_userID").value.toString() === targetrig.field("_userID").value.toString()) {
                                    v = true;
                                }
                                else
                                    nn.push(rig);
                            }
                            if (v) {
                                whitelist = nn;
                                sendNotification("Removed from whitelist", false);
                                gunColor = [1.0, 0.2, 0.2, 1.0];
                                return;
                            }
                            whitelist.push(targetrig);
                            sendNotification("Added to whitelist", false);
                            gunColor = [0.2, 1.0, 0.3, 1.0];
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Point at player and pull trigger to whitelist/unwhitelist them. Green = whitelisted, Red = not.",
                disableMethod: () => { gunColor = [0.08, 0.08, 0.08, 0.75]; },
            }),
            new ButtonInfo({
                buttonText: "Unwhitelist all",
                method: () => {
                    whitelist = [];
                    sendNotification("Whitelist cleared", false);
                },
                isTogglable: false,
                toolTip: "Clears the entire whitelist."
            }),
            new ButtonInfo({
                buttonText: "Whitelist Fly",
                method: () => {
                    if (time > tagGunDelay) {
                        tagGunDelay = time + 0.02;
                        for (let i = 0; i < whitelist.length; i++) {
                            const rig = whitelist[i];
                            if (!rig)
                                continue;
                            try {
                                const righthandfingas = getRightFingers(rig);
                                if (!righthandfingas || righthandfingas.isNull())
                                    continue;
                                if (righthandfingas.field("_indexValue").value > 0.8 &&
                                    righthandfingas.field("_middleValue").value > 0.8 &&
                                    righthandfingas.field("_thumbValue").value > 0.8) {
                                    const handForward = rig.field("handRight").value.method("get_forward").invoke();
                                    const flyForce = Vector3.method("op_Multiply", 2).invoke(handForward, flySpeed * 0.08);
                                    rig.method("RPC_AddForce", 1).invoke(flyForce);
                                }
                            }
                            catch (e) { }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players can fly by pointing fist (all fingers down)."
            }),
new ButtonInfo({
    buttonText: "Whitelist: Orbit Test",
    isTogglable: true,
    method: () => {
        const selectedName = prefabList[prefabIndex];
        const delta = Time.method("get_deltaTime").invoke();

        // 1. Loop through everyone you have whitelisted
        for (let i = 0; i < whitelist.length; i++) {
            const rig = whitelist[i];
            if (!rig || rig.handle.isNull()) continue;

            const playerID = rig.field("_userID").value.toString();
            
            // 2. Check if this player already has an orbit assigned
            if (!whitelistOrbitData.has(playerID) || lastOrbitPrefab !== selectedName) {
                // If prefab changed, clean up the old one first
                if (whitelistOrbitData.has(playerID)) {
                    whitelistOrbitData.get(playerID).prefabs.forEach(p => despawnNetworkThing(p));
                }

                // Setup new orbit for this player
                let newPrefabs = [];
                let newOrbs = [];
                const count = 16;
                const head = rig.method("get_view").invoke().method("get_head").invoke();
                const centerPos = getTransform(head).method("get_position").invoke();

                for (let j = 0; j < count; j++) {
                    let angle = (Math.PI * 2 / count) * j;
                    // Calculate spawn position
                    const offset = [Math.cos(angle) * 4.5, 0, Math.sin(angle) * 4.5];
                    const spawnPos = Vector3.method("op_Addition", 2).invoke(centerPos, offset);
                    
                    let no = spawnNetworkPrefab(selectedName, spawnPos, identityQuaternion);
                    if (no && !no.isNull()) {
                        newPrefabs.push(no);
                        let tf = no.method("get_gameObject").invoke().method("get_transform").invoke();
                        newOrbs.push({ transform: tf, angle: angle });
                    }
                }
                whitelistOrbitData.set(playerID, { prefabs: newPrefabs, orbData: newOrbs });
                lastOrbitPrefab = selectedName;
                continue; 
            }

            // 3. UPDATE POSITIONS (The actual spinning logic)
            const data = whitelistOrbitData.get(playerID);
            const head = rig.method("get_view").invoke().method("get_head").invoke();
            const currentCenter = getTransform(head).method("get_position").invoke();

            for (let orb of data.orbData) {
                // Spin speed: 1.5
                orb.angle += 1.5 * delta;
                const offset = [Math.cos(orb.angle) * 4.5, 0, Math.sin(orb.angle) * 4.5];
                const newPos = Vector3.method("op_Addition", 2).invoke(currentCenter, offset);
                
                orb.transform.method("set_position").invoke(newPos);
            }
        }
    },
    disableMethod: () => {
        // Clean up every orbit for every whitelisted player
        whitelistOrbitData.forEach((data, id) => {
            data.prefabs.forEach(p => despawnNetworkThing(p));
        });
        whitelistOrbitData.clear();
        lastOrbitPrefab = "";
        sendNotification("Whitelist Orbits Cleared", false);
    },
    toolTip: "Every whitelisted player gets 16 of your selected prefabs orbiting their head."
}),
            new ButtonInfo({
                buttonText: "Whitelist Hand RPG",
                method: () => {
                    if (time > tagGunDelay) {
                        tagGunDelay = time + 0.05;
                        for (let i = 0; i < whitelist.length; i++) {
                            const rig = whitelist[i];
                            if (!rig)
                                continue;
                            try {
                                const righthandfingas = getRightFingers(rig);
                                if (!righthandfingas || righthandfingas.isNull())
                                    continue;
                                if (righthandfingas.field("_indexValue").value > 0.8 &&
                                    righthandfingas.field("_middleValue").value > 0.8 &&
                                    righthandfingas.field("_thumbValue").value > 0.8) {
                                    const hand = rig.field("handRight").value;
                                    spawnNetworkPrefab("RPGRocket", hand.method("get_position").invoke(), hand.method("get_rotation").invoke());
                                }
                            }
                            catch (e) { }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players can shoot rockets by pointing fist (all fingers down)."
            }),
            new ButtonInfo({
                buttonText: "Whitelist Hand Flare",
                method: () => {
                    if (time > tagGunDelay) {
                        tagGunDelay = time + 0.1;
                        for (let i = 0; i < whitelist.length; i++) {
                            const rig = whitelist[i];
                            if (!rig)
                                continue;
                            try {
                                const righthandfingas = getRightFingers(rig);
                                if (!righthandfingas || righthandfingas.isNull())
                                    continue;
                                if (righthandfingas.field("_indexValue").value > 0.8 &&
                                    righthandfingas.field("_middleValue").value > 0.8 &&
                                    righthandfingas.field("_thumbValue").value > 0.8) {
                                    const hand = rig.field("handRight").value;
                                    const no = spawnNetworkPrefab("FlareGunProjectile", hand.method("get_position").invoke(), hand.method("get_rotation").invoke());
                                    if (!no)
                                        continue;
                                    const rb = no.method("GetComponent", 1).inflate(Rigidbody).invoke();
                                    if (rb && !rb.isNull()) {
                                        rb.method("set_isKinematic").invoke(false);
                                        rb.method("WakeUp").invoke();
                                        rb.method("set_linearVelocity").invoke(Vector3.method("op_Multiply", 2).invoke(hand.method("get_forward").invoke(), 20));
                                    }
                                }
                            }
                            catch (e) { }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players shoot flares by pointing fist (all fingers down)."
            }),
            new ButtonInfo({
                buttonText: "Whitelist Hand Car",
                method: () => {
                    if (time > tagGunDelay) {
                        tagGunDelay = time + 0.5;
                        for (let i = 0; i < whitelist.length; i++) {
                            const rig = whitelist[i];
                            if (!rig)
                                continue;
                            try {
                                const righthandfingas = getRightFingers(rig);
                                if (!righthandfingas || righthandfingas.isNull())
                                    continue;
                                if (righthandfingas.field("_indexValue").value > 0.8 &&
                                    righthandfingas.field("_middleValue").value > 0.8 &&
                                    righthandfingas.field("_thumbValue").value > 0.8) {
                                    const hand = rig.field("handRight").value;
                                    const no = spawnNetworkPrefab("Vehicle_Buggy", hand.method("get_position").invoke(), hand.method("get_rotation").invoke());
                                    if (!no)
                                        continue;
                                    const rb = no.method("GetComponent", 1).inflate(Rigidbody).invoke();
                                    if (rb && !rb.isNull()) {
                                        rb.method("set_isKinematic").invoke(false);
                                        rb.method("WakeUp").invoke();
                                        rb.method("set_linearVelocity").invoke(Vector3.method("op_Multiply", 2).invoke(hand.method("get_forward").invoke(), 20));
                                    }
                                }
                            }
                            catch (e) { }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players launch cars by pointing fist (all fingers down)."
            }),
            new ButtonInfo({
                buttonText: "Whitelist Hand Crate",
                method: () => {
                    if (time > tagGunDelay) {
                        tagGunDelay = time + 0.1;
                        for (let i = 0; i < whitelist.length; i++) {
                            const rig = whitelist[i];
                            if (!rig)
                                continue;
                            try {
                                const righthandfingas = getRightFingers(rig);
                                if (!righthandfingas || righthandfingas.isNull())
                                    continue;
                                if (righthandfingas.field("_indexValue").value > 0.8 &&
                                    righthandfingas.field("_middleValue").value > 0.8 &&
                                    righthandfingas.field("_thumbValue").value > 0.8) {
                                    const hand = rig.field("handRight").value;
                                    PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/item_crate"), hand.method("get_position").invoke(), hand.method("get_rotation").invoke(), NULL);
                                }
                            }
                            catch (e) { }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players launch crates by pointing fist (all fingers down)."
            }),
            new ButtonInfo({
                buttonText: "Whitelist Hand Suitcase",
                method: () => {
                    if (time > tagGunDelay) {
                        tagGunDelay = time + 0.1;
                        for (let i = 0; i < whitelist.length; i++) {
                            const rig = whitelist[i];
                            if (!rig)
                                continue;
                            try {
                                const righthandfingas = getRightFingers(rig);
                                if (!righthandfingas || righthandfingas.isNull())
                                    continue;
                                if (righthandfingas.field("_indexValue").value > 0.8 &&
                                    righthandfingas.field("_middleValue").value > 0.8 &&
                                    righthandfingas.field("_thumbValue").value > 0.8) {
                                    const hand = rig.field("handRight").value;
                                    PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/item_pelican_case"), hand.method("get_position").invoke(), hand.method("get_rotation").invoke(), NULL);
                                }
                            }
                            catch (e) { }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players launch suitcases by pointing fist (all fingers down)."
            }),
            new ButtonInfo({
                buttonText: "Whitelist Hand Bomb",
                method: () => {
                    if (time > tagGunDelay) {
                        tagGunDelay = time + 0.5;
                        for (let i = 0; i < whitelist.length; i++) {
                            const rig = whitelist[i];
                            if (!rig)
                                continue;
                            try {
                                const righthandfingas = getRightFingers(rig);
                                if (!righthandfingas || righthandfingas.isNull())
                                    continue;
                                if (righthandfingas.field("_indexValue").value > 0.8 &&
                                    righthandfingas.field("_middleValue").value > 0.8 &&
                                    righthandfingas.field("_thumbValue").value > 0.8) {
                                    const hand = rig.field("handRight").value;
                                    const no = spawnNetworkPrefab("BombController", hand.method("get_position").invoke(), hand.method("get_rotation").invoke());
                                    if (!no)
                                        continue;
                                    const rb = no.method("GetComponent", 1).inflate(Rigidbody).invoke();
                                    if (rb && !rb.isNull()) {
                                        rb.method("set_isKinematic").invoke(false);
                                        rb.method("WakeUp").invoke();
                                        rb.method("set_linearVelocity").invoke(Vector3.method("op_Multiply", 2).invoke(hand.method("get_forward").invoke(), 20));
                                    }
                                }
                            }
                            catch (e) { }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players launch bombs by pointing fist (all fingers down)."
            }),
            new ButtonInfo({
                buttonText: "Whitelist Hand Egg",
                method: () => {
                    if (time > tagGunDelay) {
                        tagGunDelay = time + 0.3;
                        for (let i = 0; i < whitelist.length; i++) {
                            const rig = whitelist[i];
                            if (!rig)
                                continue;
                            try {
                                const righthandfingas = getRightFingers(rig);
                                if (!righthandfingas || righthandfingas.isNull())
                                    continue;
                                if (righthandfingas.field("_indexValue").value > 0.8 &&
                                    righthandfingas.field("_middleValue").value > 0.8 &&
                                    righthandfingas.field("_thumbValue").value > 0.8) {
                                    const hand = rig.field("handRight").value;
                                    const no = spawnNetworkPrefab("ExplosiveEgg", hand.method("get_position").invoke(), hand.method("get_rotation").invoke());
                                    if (!no)
                                        continue;
                                    const rb = no.method("GetComponent", 1).inflate(Rigidbody).invoke();
                                    if (rb && !rb.isNull()) {
                                        rb.method("set_isKinematic").invoke(false);
                                        rb.method("WakeUp").invoke();
                                        rb.method("set_linearVelocity").invoke(Vector3.method("op_Multiply", 2).invoke(hand.method("get_forward").invoke(), 20));
                                    }
                                }
                            }
                            catch (e) { }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players launch explosive eggs by pointing fist (all fingers down)."
            }),
            new ButtonInfo({
                buttonText: "Whitelist Hand Balloon",
                method: () => {
                    if (time > tagGunDelay) {
                        tagGunDelay = time + 0.3;
                        for (let i = 0; i < whitelist.length; i++) {
                            const rig = whitelist[i];
                            if (!rig)
                                continue;
                            try {
                                const righthandfingas = getRightFingers(rig);
                                if (!righthandfingas || righthandfingas.isNull())
                                    continue;
                                if (righthandfingas.field("_indexValue").value > 0.8 &&
                                    righthandfingas.field("_middleValue").value > 0.8 &&
                                    righthandfingas.field("_thumbValue").value > 0.8) {
                                    const hand = rig.field("handRight").value;
                                    spawnNetworkPrefab("InflatedBalloon", hand.method("get_position").invoke(), hand.method("get_rotation").invoke());
                                }
                            }
                            catch (e) { }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players launch balloons by pointing fist (all fingers down)."
            }),
            new ButtonInfo({
                buttonText: "Whitelist Hand Giveaway",
                method: () => {
                    function rand(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }
                    if (time > tagGunDelay) {
                        tagGunDelay = time + 0.3;
                        for (let i = 0; i < whitelist.length; i++) {
                            const rig = whitelist[i];
                            if (!rig)
                                continue;
                            try {
                                const righthandfingas = getRightFingers(rig);
                                if (!righthandfingas || righthandfingas.isNull())
                                    continue;
                                if (righthandfingas.field("_indexValue").value > 0.8 &&
                                    righthandfingas.field("_middleValue").value > 0.8 &&
                                    righthandfingas.field("_thumbValue").value > 0.8) {
                                    const hand = rig.field("handRight").value;
                                    const randItem = itemIDs[rand(0, itemIDs.length - 1)];
                                    PrefabGen.method("SpawnItem", 4).invoke(Il2Cpp.string("item_prefab/" + randItem), hand.method("get_position").invoke(), hand.method("get_rotation").invoke(), NULL);
                                }
                            }
                            catch (e) { }
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players get random items by pointing fist (all fingers down)."
            }),
            new ButtonInfo({
                buttonText: "Whitelist Disintegrate",
                method: () => {
                    for (let i = 0; i < whitelist.length; i++) {
                        const whitelistedPlayer = whitelist[i];
                        if (!whitelistedPlayer || whitelistedPlayer.handle.isNull())
                            continue;
                        const playerID = whitelistedPlayer.field("_userID").value.toString();
                        if (!whitelistDisintegrateDelays[playerID])
                            whitelistDisintegrateDelays[playerID] = 0;
                        try {
                            const righthandfingas = getRightFingers(whitelistedPlayer);
                            if (!righthandfingas || righthandfingas.isNull())
                                continue;
                            if (righthandfingas.field("_thumbValue").value > 0.8 && time > whitelistDisintegrateDelays[playerID]) {
                                whitelistDisintegrateDelays[playerID] = time + 1.0;
                                const whitelistPos = getTransform(whitelistedPlayer).method("get_position").invoke();
                                let lowestDistance = Number.MAX_SAFE_INTEGER;
                                let nearestPlayer = null;
                                const playerDict = NetPlayer.field("playerIDToNetPlayer").value;
                                const playerValues = playerDict.method("get_Values").invoke();
                                const enumerator = playerValues.method("GetEnumerator").invoke();
                                while (enumerator.method("MoveNext").invoke()) {
                                    const player = enumerator.method("get_Current").invoke();
                                    if (!player || player.handle.isNull())
                                        continue;
                                    if (playerIsLocal(player))
                                        continue;
                                    if (player.field("_userID").value.toString() === playerID)
                                        continue;
                                    const dist = Vector3.method("Distance").invoke(whitelistPos, getTransform(player).method("get_position").invoke());
                                    if (dist < lowestDistance) {
                                        lowestDistance = dist;
                                        nearestPlayer = player;
                                    }
                                }
                                if (nearestPlayer && !nearestPlayer.handle.isNull()) {
                                    const targetPos = getTransform(nearestPlayer).method("get_position").invoke();
                                    const NetworkRunnerInst = SFXManager.field("_instance").value.method("get__currentRunner").invoke();
                                    const allVFX = [VFXTypes.MuzzleFlash_Shotgun, VFXTypes.CrateBreak, VFXTypes.Explosion_FlareGun, VFXTypes.Explosion_Coins, VFXTypes.Portal_Teleport, VFXTypes.Autumn_Leaves_Burst, VFXTypes.MeatExplosion_1, VFXTypes.MeatExplosion_2, VFXTypes.MeatExplosion_Headshot, VFXTypes.ConfettiBurst, VFXTypes.Ethereal_Void, VFXTypes.FuelExplosion];
                                    for (const vfxType of allVFX) {
                                        ParticleManagerClass.method("RPC_PlayVFX", 4).invoke(NetworkRunnerInst, vfxType, targetPos, identityQuaternion);
                                    }
                                    nearestPlayer.method("RPC_Teleport").invoke([0, -9999999, 0]);
                                    nearestPlayer.method("RPC_AddForce").invoke(Vector3.method("op_Multiply", 2).invoke(getTransform(nearestPlayer).method("get_forward").invoke(), 1500 * deltaTime));
                                    nearestPlayer.method("RPC_SetColorHSV").invoke(NaN, NaN, NaN, NaN);
                                }
                            }
                        }
                        catch (e) {
                            console.error("Whitelist disintegrate error: " + e);
                        }
                    }
                },
                isTogglable: true,
                toolTip: "Whitelisted players disintegrate nearest player by pressing thumb (1s cooldown)."
            }),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Prefab Stuff",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns to main menu."
            }),
            new ButtonInfo({
                buttonText: "Change Prefab",
                method: () => {
                    if (rightGrab) {
                        prefabIndex--;
                    }
                    else {
                        prefabIndex++;
                    }
                    prefabIndex = ((prefabIndex % prefabList.length) + prefabList.length) % prefabList.length;
                    sendNotification("Prefab: " + prefabList[prefabIndex], false);
                },
                isTogglable: false,
                toolTip: "Changes the prefab to spawn. Hold right grip to go back."
            }),
            new ButtonInfo({
                buttonText: "Spawn Prefab",
                method: () => {
                    try {
                        const position = rightHandTransform.method("get_position").invoke();
                        const rotation = rightHandTransform.method("get_rotation").invoke();
                        const runner = PrefabGen.field("_instance").value.method("get_runner").invoke();
                        if (!runner || runner.isNull()) {
                            console.error("runner null");
                            return;
                        }
                        const sources = runner.field("_config").value.field("PrefabTable").value.field("_sources").value;
                        const count = sources.method("get_Count").invoke();
                        const name = prefabList[prefabIndex];
                        let found = false;
                        for (let i = 0; i < count; i++) {
                            try {
                                const source = sources.method("get_Item").invoke(i);
                                const desc = source.method("get_Description").invoke().toString();
                                if (desc.includes(name)) {
                                    const no = source.method("WaitForResult").invoke();
                                    if (!no || no.isNull()) {
                                        console.error("prefab null");
                                        return;
                                    }
                                    const makeZeroForType = (type) => {
                                        if (type.class.isEnum || type.isPrimitive)
                                            return 0;
                                        if (!type.class.isValueType)
                                            return NULL;
                                        const fields = type.class.fields.filter(f => !f.isStatic);
                                        if (fields.length === 0)
                                            return 0;
                                        return fields.map(f => makeZeroForType(f.type));
                                    };
                                    const buildNullableArg = (nullableType, hasValue, value) => {
                                        const fields = nullableType.class.fields.filter(f => !f.isStatic);
                                        return fields.map(f => {
                                            const lname = f.name.toLowerCase();
                                            if (lname.includes("hasvalue"))
                                                return hasValue ? 1 : 0;
                                            if (lname === "value")
                                                return hasValue ? value : makeZeroForType(f.type);
                                            return makeZeroForType(f.type);
                                        });
                                    };
                                    const normalizeValue = (type, value) => {
                                        if (typeof value === "boolean")
                                            return value ? 1 : 0;
                                        if (value instanceof Il2Cpp.ValueType) {
                                            const fields = type.class.fields.filter(f => !f.isStatic);
                                            if (fields.length === 0)
                                                return 0;
                                            return fields.map(f => normalizeValue(f.type, f.bind(value).value));
                                        }
                                        if (Array.isArray(value))
                                            return value.map(v => normalizeValue(type, v));
                                        return value;
                                    };
                                    const buildNullableFromValueType = (nullableType, valueType) => {
                                        return buildNullableArg(nullableType, true, normalizeValue(valueType.type, valueType));
                                    };
                                    let spawnMethod = null;
                                    for (const m of runner.method("Spawn").overloads()) {
                                        if (m.parameterCount !== 6 || m.isGeneric)
                                            continue;
                                        const p = m.parameters;
                                        if (p[0].type.name.includes("Fusion.NetworkObject") &&
                                            p[1].type.name.startsWith("System.Nullable") && p[1].type.name.includes("Vector3") &&
                                            p[2].type.name.startsWith("System.Nullable") && p[2].type.name.includes("Quaternion") &&
                                            p[3].type.name.startsWith("System.Nullable") && p[3].type.name.includes("PlayerRef") &&
                                            p[4].type.name.includes("OnBeforeSpawned") &&
                                            p[5].type.name.includes("NetworkSpawnFlags")) {
                                            spawnMethod = m;
                                            break;
                                        }
                                    }
                                    if (!spawnMethod) {
                                        console.error("spawn method not found");
                                        return;
                                    }
                                    const posArg = buildNullableFromValueType(spawnMethod.parameters[1].type, position);
                                    const rotArg = buildNullableFromValueType(spawnMethod.parameters[2].type, rotation);
                                    const authArg = buildNullableArg(spawnMethod.parameters[3].type, false, makeZeroForType(spawnMethod.parameters[3].type));
                                    const onBeforeArg = spawnMethod.parameters[4].type.class.isValueType ? makeZeroForType(spawnMethod.parameters[4].type) : NULL;
                                    spawnMethod.bind(runner).invoke(no, posArg, rotArg, authArg, onBeforeArg, 0);
                                    sendNotification("Spawned: " + name, false);
                                    found = true;
                                    break;
                                }
                            }
                            catch (_) { }
                        }
                        if (!found)
                            sendNotification("Prefab not found: " + name, false);
                    }
                    catch (e) {
                        console.error("SpawnPrefab error: " + e);
                    }
                },
                isTogglable: false,
                toolTip: "Spawns selected prefab at right hand."
            }),
            new ButtonInfo({
                buttonText: "Prefab Gun",
                method: () => {
                    if (!rightGrab)
                        return;
                    const gunData = renderGun();
                    const ray = gunData.ray;
                    if (!ray || ray.isNull())
                        return;
                    if (!rightTrigger)
                        return;
                    if (time < mobGunDelay)
                        return;
                    mobGunDelay = time + 0.15;
                    try {
                        const position = ray.method("get_point").invoke();
                        const rotation = identityQuaternion;
                        const runner = PrefabGen.field("_instance").value.method("get_runner").invoke();
                        if (!runner || runner.isNull())
                            return;
                        const sources = runner.field("_config").value.field("PrefabTable").value.field("_sources").value;
                        const count = sources.method("get_Count").invoke();
                        const name = prefabList[prefabIndex];
                        for (let i = 0; i < count; i++) {
                            try {
                                const source = sources.method("get_Item").invoke(i);
                                const desc = source.method("get_Description").invoke().toString();
                                if (desc.includes(name)) {
                                    const no = source.method("WaitForResult").invoke();
                                    if (!no || no.isNull())
                                        return;
                                    const makeZeroForType = (type) => {
                                        if (type.class.isEnum || type.isPrimitive)
                                            return 0;
                                        if (!type.class.isValueType)
                                            return NULL;
                                        const fields = type.class.fields.filter(f => !f.isStatic);
                                        if (fields.length === 0)
                                            return 0;
                                        return fields.map(f => makeZeroForType(f.type));
                                    };
                                    const buildNullableArg = (nullableType, hasValue, value) => {
                                        const fields = nullableType.class.fields.filter(f => !f.isStatic);
                                        return fields.map(f => {
                                            const lname = f.name.toLowerCase();
                                            if (lname.includes("hasvalue"))
                                                return hasValue ? 1 : 0;
                                            if (lname === "value")
                                                return hasValue ? value : makeZeroForType(f.type);
                                            return makeZeroForType(f.type);
                                        });
                                    };
                                    const normalizeValue = (type, value) => {
                                        if (typeof value === "boolean")
                                            return value ? 1 : 0;
                                        if (value instanceof Il2Cpp.ValueType) {
                                            const fields = type.class.fields.filter(f => !f.isStatic);
                                            if (fields.length === 0)
                                                return 0;
                                            return fields.map(f => normalizeValue(f.type, f.bind(value).value));
                                        }
                                        if (Array.isArray(value))
                                            return value.map(v => normalizeValue(type, v));
                                        return value;
                                    };
                                    const buildNullableFromValueType = (nullableType, valueType) => {
                                        return buildNullableArg(nullableType, true, normalizeValue(valueType.type, valueType));
                                    };
                                    let spawnMethod = null;
                                    for (const m of runner.method("Spawn").overloads()) {
                                        if (m.parameterCount !== 6 || m.isGeneric)
                                            continue;
                                        const p = m.parameters;
                                        if (p[0].type.name.includes("Fusion.NetworkObject") &&
                                            p[1].type.name.startsWith("System.Nullable") && p[1].type.name.includes("Vector3") &&
                                            p[2].type.name.startsWith("System.Nullable") && p[2].type.name.includes("Quaternion") &&
                                            p[3].type.name.startsWith("System.Nullable") && p[3].type.name.includes("PlayerRef") &&
                                            p[4].type.name.includes("OnBeforeSpawned") &&
                                            p[5].type.name.includes("NetworkSpawnFlags")) {
                                            spawnMethod = m;
                                            break;
                                        }
                                    }
                                    if (!spawnMethod)
                                        return;
                                    const posArg = buildNullableFromValueType(spawnMethod.parameters[1].type, position);
                                    const rotArg = buildNullableFromValueType(spawnMethod.parameters[2].type, rotation);
                                    const authArg = buildNullableArg(spawnMethod.parameters[3].type, false, makeZeroForType(spawnMethod.parameters[3].type));
                                    const onBeforeArg = spawnMethod.parameters[4].type.class.isValueType ? makeZeroForType(spawnMethod.parameters[4].type) : NULL;
                                    spawnMethod.bind(runner).invoke(no, posArg, rotArg, authArg, onBeforeArg, 0);
                                    break;
                                }
                            }
                            catch (_) { }
                        }
                    }
                    catch (e) {
                        console.error("PrefabGun error: " + e);
                    }
                },
                isTogglable: true,
                toolTip: "Shoots selected prefab where you aim (hold grip + trigger)."
            }),
            new ButtonInfo({
                    buttonText: "ChristmasBox Orbit self",
                    method: () => {
                        const center = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                        function spawnorbitobject() {
                            orbiters = [];
                            for (let i = 0; i < 8; i++) {
                                let angle = Math.PI * 2 / 8 * i;
                                const offset = Vector3.alloc();
                                offset.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(angle) * 6.5, 0, Math.sin(angle) * 6.5);
                                let centerPos2 = center.method("get_position").invoke();
                                let spawnPos = Vector3.method("op_Addition").invoke(centerPos2, [offset.field("x").value, offset.field("y").value, offset.field("z").value]);
                                let no = spawnNetworkPrefab("ChristmasBox", spawnPos, Quaternion.method("get_identity").invoke());
                                if (!no)
                                    continue;
                                orbitprefabs.push(no);
                                let go = no.method("get_gameObject").invoke();
                                if (!go)
                                    continue;
                                let tf = go.method("get_transform").invoke();
                                orbiters.push({ transform: tf, angle });
                            }
                        }
                        if (orbitprefabs.length < 4)
                            spawnorbitobject();
                        let delta = Time.method("get_deltaTime").invoke();
                        let centerPos = center.method("get_position").invoke();
                        for (let orb of orbiters) {
                            orb.angle += 1.5 * delta;
                            const offset = Vector3.alloc();
                            offset.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(orb.angle) * 6.5, 0, Math.sin(orb.angle) * 6.5);
                            let newPos = Vector3.method("op_Addition").invoke(centerPos, [offset.field("x").value, offset.field("y").value, offset.field("z").value]);
                            orb.transform.method("set_position").invoke(newPos);
                        }
                    },
                    disableMethod: () => {
                        for (let i = 0; i < orbitprefabs.length; i++) {
                            const prefab = orbitprefabs[i];
                            if (!prefab)
                                return;
                            const runner = prefab.method("get_Runner").invoke();
                            if (runner && !runner.isNull()) {
                                runner.method("Despawn").invoke(prefab);
                            }
                        }
                        orbitprefabs = [];
                        orbiters = [];
                    },
                    isTogglable: true,
                    toolTip: "Creates 8 selling machines that orbit you."
                }),
                
                new ButtonInfo({
                    buttonText: "Vehicle Buggy Orbit self",
                    method: () => {
                        const center = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                        function spawnorbitobject() {
                            orbiters = [];
                            for (let i = 0; i < 8; i++) {
                                let angle = Math.PI * 2 / 8 * i;
                                const offset = Vector3.alloc();
                                offset.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(angle) * 6.5, 0, Math.sin(angle) * 6.5);
                                let centerPos2 = center.method("get_position").invoke();
                                let spawnPos = Vector3.method("op_Addition").invoke(centerPos2, [offset.field("x").value, offset.field("y").value, offset.field("z").value]);
                                let no = spawnNetworkPrefab("Vehicle_Buggy", spawnPos, Quaternion.method("get_identity").invoke());
                                if (!no)
                                    continue;
                                orbitprefabs.push(no);
                                let go = no.method("get_gameObject").invoke();
                                if (!go)
                                    continue;
                                let tf = go.method("get_transform").invoke();
                                orbiters.push({ transform: tf, angle });
                            }
                        }
                        if (orbitprefabs.length < 4)
                            spawnorbitobject();
                        let delta = Time.method("get_deltaTime").invoke();
                        let centerPos = center.method("get_position").invoke();
                        for (let orb of orbiters) {
                            orb.angle += 1.5 * delta;
                            const offset = Vector3.alloc();
                            offset.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(orb.angle) * 6.5, 0, Math.sin(orb.angle) * 6.5);
                            let newPos = Vector3.method("op_Addition").invoke(centerPos, [offset.field("x").value, offset.field("y").value, offset.field("z").value]);
                            orb.transform.method("set_position").invoke(newPos);
                        }
                    },
                    disableMethod: () => {
                        for (let i = 0; i < orbitprefabs.length; i++) {
                            const prefab = orbitprefabs[i];
                            if (!prefab)
                                return;
                            const runner = prefab.method("get_Runner").invoke();
                            if (runner && !runner.isNull()) {
                                runner.method("Despawn").invoke(prefab);
                            }
                        }
                        orbitprefabs = [];
                        orbiters = [];
                    },
                    isTogglable: true,
                    toolTip: "Creates 8 Vehicle Buggys that orbit you."
                }),
                  new ButtonInfo({
                    buttonText: "SellingMachine Orbit self",
                    method: () => {
                        const center = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                        function spawnorbitobject() {
                            orbiters = [];
                            for (let i = 0; i < 8; i++) {
                                let angle = Math.PI * 2 / 8 * i;
                                const offset = Vector3.alloc();
                                offset.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(angle) * 6.5, 0, Math.sin(angle) * 6.5);
                                let centerPos2 = center.method("get_position").invoke();
                                let spawnPos = Vector3.method("op_Addition").invoke(centerPos2, [offset.field("x").value, offset.field("y").value, offset.field("z").value]);
                                let no = spawnNetworkPrefab("ItemSellingMachineController", spawnPos, Quaternion.method("get_identity").invoke());
                                if (!no)
                                    continue;
                                orbitprefabs.push(no);
                                let go = no.method("get_gameObject").invoke();
                                if (!go)
                                    continue;
                                let tf = go.method("get_transform").invoke();
                                orbiters.push({ transform: tf, angle });
                            }
                        }
                        if (orbitprefabs.length < 4)
                            spawnorbitobject();
                        let delta = Time.method("get_deltaTime").invoke();
                        let centerPos = center.method("get_position").invoke();
                        for (let orb of orbiters) {
                            orb.angle += 1.5 * delta;
                            const offset = Vector3.alloc();
                            offset.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(orb.angle) * 6.5, 0, Math.sin(orb.angle) * 6.5);
                            let newPos = Vector3.method("op_Addition").invoke(centerPos, [offset.field("x").value, offset.field("y").value, offset.field("z").value]);
                            orb.transform.method("set_position").invoke(newPos);
                        }
                    },
                    disableMethod: () => {
                        for (let i = 0; i < orbitprefabs.length; i++) {
                            const prefab = orbitprefabs[i];
                            if (!prefab)
                                return;
                            const runner = prefab.method("get_Runner").invoke();
                            if (runner && !runner.isNull()) {
                                runner.method("Despawn").invoke(prefab);
                            }
                        }
                        orbitprefabs = [];
                        orbiters = [];
                    },
                    isTogglable: true,
                    toolTip: "Creates 8 selling macine that orbit you."
              
                }),
                new ButtonInfo({
                    buttonText: "Spaceship Orbit self",
                    method: () => {
                        const center = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                        function spawnorbitobject() {
                            orbiters = [];
                            for (let i = 0; i < 8; i++) {
                                let angle = Math.PI * 2 / 8 * i;
                                const offset = Vector3.alloc();
                                offset.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(angle) * 6.5, 0, Math.sin(angle) * 6.5);
                                let centerPos2 = center.method("get_position").invoke();
                                let spawnPos = Vector3.method("op_Addition").invoke(centerPos2, [offset.field("x").value, offset.field("y").value, offset.field("z").value]);
                                let no = spawnNetworkPrefab("SpaceshipTeleporter", spawnPos, Quaternion.method("get_identity").invoke());
                                if (!no)
                                    continue;
                                orbitprefabs.push(no);
                                let go = no.method("get_gameObject").invoke();
                                if (!go)
                                    continue;
                                let tf = go.method("get_transform").invoke();
                                orbiters.push({ transform: tf, angle });
                            }
                        }
                        if (orbitprefabs.length < 4)
                            spawnorbitobject();
                        let delta = Time.method("get_deltaTime").invoke();
                        let centerPos = center.method("get_position").invoke();
                        for (let orb of orbiters) {
                            orb.angle += 1.5 * delta;
                            const offset = Vector3.alloc();
                            offset.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(orb.angle) * 6.5, 0, Math.sin(orb.angle) * 6.5);
                            let newPos = Vector3.method("op_Addition").invoke(centerPos, [offset.field("x").value, offset.field("y").value, offset.field("z").value]);
                            orb.transform.method("set_position").invoke(newPos);
                        }
                    },
                    disableMethod: () => {
                        for (let i = 0; i < orbitprefabs.length; i++) {
                            const prefab = orbitprefabs[i];
                            if (!prefab)
                                return;
                            const runner = prefab.method("get_Runner").invoke();
                            if (runner && !runner.isNull()) {
                                runner.method("Despawn").invoke(prefab);
                            }
                        }
                        orbitprefabs = [];
                        orbiters = [];
                    },
                    isTogglable: true,
                    toolTip: "Creates 8 SpaceshipTeleporter that orbit you."
                
                }),
                  new ButtonInfo({
    buttonText: "moving selling machine",
    isTogglable: true,
    enableMethod: () => {
        sendNotification("Point and hold Trigger to guide the machine.", false);
    },
    disableMethod: () => {
        try { despawnNetworkThing(controlledMachine); } catch (_) { }
        controlledMachine = null;
        machineTargetPos = null;
    },
    method: () => {
        if (!rightGrab) return;
        const gunData = renderGun(); // Uses your existing laser logic
        const ray = gunData.ray;

        try {
            // 1. If we haven't spawned our machine yet, spawn it!
            if (controlledMachine == null || controlledMachine.handle.isNull()) {
                const myPos = getTransform(bodyCollider).method("get_position").invoke();
                controlledMachine = spawnNetworkPrefab("ItemSellingMachineController", myPos, identityQuaternion);
                return;
            }

            // 2. Take Authority so we can move it for everyone
            requestStateAuthoritySafe(controlledMachine);

            // 3. Update the Target Position with the laser
            if (rightTrigger && ray && !ray.handle.isNull()) {
                machineTargetPos = ray.method("get_point").invoke();
            }

            // 4. LERP (The Slow Movement)
            if (machineTargetPos != null) {
                const machineTF = controlledMachine.method("get_transform").invoke();
                const currentPos = machineTF.method("get_position").invoke();
                
                const moveStep = deltaTime * 8.0;
                const nextPos = Vector3.method("MoveTowards", 3).invoke(currentPos, machineTargetPos, moveStep);
                
                machineTF.method("set_position").invoke(nextPos);
            }
        } catch (e) { }
    },
    toolTip: "Spawns a machine. Wherever you point your laser, the machine glides toward it."
}),
new ButtonInfo({
    buttonText: "moving currently selected prefab",
    isTogglable: true,
    enableMethod: () => { sendNotification("Guiding Prefab Drone...", false); },
    disableMethod: () => { controlledPrefab = null; prefabTargetPos = null; },
    method: () => {
        if (!rightGrab) return;
        const gunData = renderGun();
        const ray = gunData.ray;

        try {
            // 1. SPAWN if we don't have one yet
            if (controlledPrefab == null || controlledPrefab.handle.isNull()) {
                const name = prefabList[prefabIndex];
                const myPos = getTransform(bodyCollider).method("get_position").invoke();
                controlledPrefab = spawnNetworkPrefab(name, myPos, identityQuaternion);
                return;
            }

            // 2. AUTHORITY
            requestStateAuthoritySafe(controlledPrefab);

            // 3. TARGETING
            if (rightTrigger && ray && !ray.isNull()) {
                prefabTargetPos = ray.method("get_point").invoke();
            }

            // 4. LERP
            if (prefabTargetPos != null) {
                const tf = controlledPrefab.method("get_transform").invoke();
                const currentPos = tf.method("get_position").invoke();
                
                const nextPos = Vector3.method("MoveTowards", 3).invoke(currentPos, prefabTargetPos, deltaTime * 8.0);
                tf.method("set_position").invoke(nextPos);

                // Disable physics while controlling
                const rb = controlledPrefab.method("GetComponent", 1).inflate(Rigidbody).invoke();
                if (rb) rb.method("set_isKinematic").invoke(true);
            }
        } catch (e) { }
    },
    toolTip: "Spawns your selected prefab and lets you guide it smoothly with your laser."
}),
new ButtonInfo({
    buttonText: "Hold All Mobs",
    isTogglable: true,
    enableMethod: () => {
        releaseHeldObjects(heldMobObjects, true);
        heldMobObjects = spawnHeldMobEntries();
        sendNotification(`Holding ${heldMobObjects.length} spawned mobs.`, false);
    },
    method: () => {
        try { updateHeldEntries(heldMobObjects, rightHandTransform, 1.5, 0.5); } catch (e) { console.error("Hold Mobs Error: " + e); }
    },
    disableMethod: () => {
        releaseHeldObjects(heldMobObjects, true);
        sendNotification("Monsters Released.", false);
    }
}),
new ButtonInfo({
    buttonText: "Hold All Prefabs",
    isTogglable: true,
    enableMethod: () => {
        releaseHeldObjects(heldPrefabObjects, true);
        heldPrefabObjects = spawnHeldPrefabEntries();
        sendNotification(`Holding ${heldPrefabObjects.length} spawned prefabs.`, false);
    },
    method: () => {
        try { updateHeldEntries(heldPrefabObjects, rightHandTransform, 0.5, 0.75); } catch (e) { }
    },
    disableMethod: () => {
        releaseHeldObjects(heldPrefabObjects, true);
        sendNotification("Prefabs Dropped.", false);
    }
}),
            new ButtonInfo({
                buttonText: "Selected Prefab Hand Follow",
                method: () => {
                    updateFollowedPrefab();
                },
                disableMethod: () => {
                    clearFollowedPrefab();
                },
                isTogglable: true,
                toolTip: "Keeps exactly one selected prefab synced to your right hand like you are holding it."
            }),
            new ButtonInfo({
                buttonText: "Selected Mob Hand Follow",
                method: () => {
                    updateFollowedMob();
                },
                disableMethod: () => {
                    clearFollowedMob();
                },
                isTogglable: true,
                toolTip: "Keeps one instance of your currently selected mob synced to your right hand."
            }),
            new ButtonInfo({
                buttonText: "Selected Mob Hand Follow Offset",
                method: () => {
                    updateFollowedMobWithOffset(2.00);
                },
                disableMethod: () => {
                    clearFollowedMob();
                },
                isTogglable: true,
                toolTip: "Keeps one instance of your currently selected mob in front of your right hand to reduce collisions."
            }),
            new ButtonInfo({
                buttonText: "ChristmasBox Prefab Hand Follow",
                method: () => {
                    updateFollowedPrefabByName("ChristmasBox", 1.25);
                },
                disableMethod: () => {
                    clearFollowedPrefab();
                },
                isTogglable: true,
                toolTip: "Keeps a ChristmasBox prefab synced to your right hand."
            }),
            new ButtonInfo({
                buttonText: "Vehicle Buggy Prefab Hand Follow",
                method: () => {
                    updateFollowedPrefabByName("Vehicle_Buggy", 1.50);
                },
                disableMethod: () => {
                    clearFollowedPrefab();
                },
                isTogglable: true,
                toolTip: "Keeps a Vehicle_Buggy prefab synced to your right hand."
            }),
            new ButtonInfo({
                buttonText: "Selling Machine Prefab Hand Follow",
                method: () => {
                    updateFollowedPrefabByName("ItemSellingMachineController", 2.00);
                },
                disableMethod: () => {
                    clearFollowedPrefab();
                },
                isTogglable: true,
                toolTip: "Keeps an ItemSellingMachineController prefab synced to your right hand."
            }),
            new ButtonInfo({
                buttonText: "GiantRock Prefab Hand Follow",
                method: () => {
                    updateFollowedPrefabByName("GiantRockObject", 1.50);
                },
                disableMethod: () => {
                    clearFollowedPrefab();
                },
                isTogglable: true,
                toolTip: "Keeps a GiantRockObject prefab synced to your right hand."
            }),
                new ButtonInfo({
                    buttonText: "orbit test",
                    method: () => {
                        const center = AssemblyCSharp.class("AnimalCompany.PlayerController").method("get_instance").invoke().method("get_head").invoke();
                        const name = prefabList[prefabIndex];
                        function spawnorbitobject() {
                            for (let i = 0; i < orbitprefabs.length; i++) {
                                despawnNetworkThing(orbitprefabs[i]);
                            }
                            orbitprefabs = [];
                            orbiters = [];
                            orbitPrefabName = name;
                            const count = 16;

                            for (let i = 0; i < count; i++) {
                                let angle = Math.PI * 2 / count * i;

                                const offset = Vector3.alloc();
                                offset.method(".ctor")
                                    .overload("System.Single", "System.Single", "System.Single")
                                    .invoke(Math.cos(angle) * 6.5, 0, Math.sin(angle) * 6.5);

                                let centerPos2 = center.method("get_position").invoke();

                                let spawnPos = Vector3.method("op_Addition").invoke(centerPos2, [
                                    offset.field("x").value,
                                    offset.field("y").value,
                                    offset.field("z").value
                                ]);

                                let no = spawnNetworkPrefab(name, spawnPos, Quaternion.method("get_identity").invoke());

                                if (!no) continue;

                                orbitprefabs.push(no);

                                let go = no.method("get_gameObject").invoke();
                                if (!go) continue;

                                let tf = go.method("get_transform").invoke();
                                orbiters.push({ transform: tf, angle });
                            }
                        }
                        if (orbitprefabs.length < 4 || orbitPrefabName !== name)
                            spawnorbitobject();
                        let delta = Time.method("get_deltaTime").invoke();
                        let centerPos = center.method("get_position").invoke();
                        for (let orb of orbiters) {
                            orb.angle += 1.5 * delta;
                            const offset = Vector3.alloc();
                            offset.method(".ctor").overload("System.Single", "System.Single", "System.Single").invoke(Math.cos(orb.angle) * 6.5, 0, Math.sin(orb.angle) * 6.5);
                            let newPos = Vector3.method("op_Addition").invoke(centerPos, [offset.field("x").value, offset.field("y").value, offset.field("z").value]);
                            orb.transform.method("set_position").invoke(newPos);
                        }
                    },
                    disableMethod: () => {
                        for (let i = 0; i < orbitprefabs.length; i++) {
                            despawnNetworkThing(orbitprefabs[i]);
                        }
                        orbitPrefabName = "";
                        orbitprefabs = [];
                        orbiters = [];
                    },
                    isTogglable: true,
                    toolTip: "Creates 16 of your selected prefabs that orbit you."
                }),
            new ButtonInfo({
                buttonText: "Rocket Launcher",
                method: () => {
                    if (!rightGrab)
                        return;
                    if (!rightTrigger)
                        return;
                    try {
                        const position = rightHandTransform.method("get_position").invoke();
                        const rotation = rightHandTransform.method("get_rotation").invoke();
                        spawnNetworkPrefab("RPGRocket", position, rotation);
                    }
                    catch (e) {
                        console.error("RocketLauncher error: " + e);
                    }
                },
                isTogglable: true,
                toolTip: "hi"
            }),
            new ButtonInfo({
                buttonText: "boomspear",
                method: () => {
                    if (!rightGrab)
                        return;
                    if (!rightTrigger)
                        return;
                    try {
                        const position = rightHandTransform.method("get_position").invoke();
                        const rotation = rightHandTransform.method("get_rotation").invoke();
                        spawnNetworkPrefab("RPGRocketSpear", position, rotation);
                    }
                    catch (e) {
                        console.error("boomspear error: " + e);
                    }
                },
                isTogglable: true,
                toolTip: "hi"
            }),
            new ButtonInfo({
                buttonText: "egg",
                method: () => {
                    if (!rightGrab)
                        return;
                    if (!rightTrigger)
                        return;
                    try {
                        const position = rightHandTransform.method("get_position").invoke();
                        const rotation = rightHandTransform.method("get_rotation").invoke();
                        spawnNetworkPrefab("RPGRocketEgg", position, rotation);
                    }
                    catch (e) {
                        console.error("boomspear error: " + e);
                    }
                },
                isTogglable: true,
                toolTip: "hi"
            }),
            new ButtonInfo({
    buttonText: "Cycle Flare Delay",
    isTogglable: false,
    method: () => {
        cycleFlareDelayMode();
    },
    toolTip: "Cycles flare launcher delay between zero, half, and normal."
}),
new ButtonInfo({
    buttonText: "flare",
    isTogglable: true,
    method: () => {
        // TRIGGER: Hold Grip + Trigger on Right Hand
        if (rightGrab && rightTrigger && time > tagGunDelay) {
            tagGunDelay = time + getCurrentFlareDelay();

            try {
                const pos = rightHandTransform.method("get_position").invoke();
                const rot = rightHandTransform.method("get_rotation").invoke();
                const forward = rightHandTransform.method("get_forward").invoke();

                // 1. Spawn Flare
                const netObj = spawnNetworkPrefab("FlareGunProjectile", pos, rot);

                if (netObj && !netObj.handle.isNull()) {
                    netObj.method("RequestStateAuthority").invoke();
                    
                    const rb = netObj.method("GetComponent", 1).inflate(Rigidbody).invoke();
                    if (rb && !rb.isNull()) {
                        rb.method("set_isKinematic").invoke(false);
                        rb.method("WakeUp").invoke();
                        
                        // 2. Launch Forward (Speed: 25.0)
                        const velocity = Vector3.method("op_Multiply", 2).invoke(forward, 25.0);
                        rb.method("set_linearVelocity").invoke(velocity);
                    }
                }
            } catch (e) { console.error("Flare Launcher Error: " + e); }
        }
    },
    toolTip: "Make a fist with your right hand to shoot flares forward."
}),
new ButtonInfo({
    buttonText: "Giant Rock Launcher",
    isTogglable: true,
    method: () => {
        if (rightGrab && rightTrigger && time > tagGunDelay) {
            tagGunDelay = time + 0.35;

            try {
                const pos = rightHandTransform.method("get_position").invoke();
                const rot = rightHandTransform.method("get_rotation").invoke();
                const forward = rightHandTransform.method("get_forward").invoke();
                const netObj = spawnNetworkPrefab("GiantRockObject", pos, rot);

                if (netObj && !netObj.handle.isNull()) {
                    requestStateAuthoritySafe(netObj);

                    const rb = getRigidbodySafe(netObj);
                    if (rb && !rb.isNull()) {
                        rb.method("set_isKinematic").invoke(false);
                        rb.method("WakeUp").invoke();
                        const velocity = Vector3.method("op_Multiply", 2).invoke(forward, 22.0);
                        rb.method("set_linearVelocity").invoke(velocity);
                    }
                }
            } catch (e) { console.error("Giant Rock Launcher Error: " + e); }
        }
    },
    toolTip: "Launches GiantRockObject prefabs forward from your right hand."
}),
new ButtonInfo({
    buttonText: "car",
    isTogglable: true,
    method: () => {
        if (rightGrab && rightTrigger && time > tagGunDelay) {
            tagGunDelay = time + 0.6; // Higher cooldown to prevent buggy lag

            try {
                const pos = rightHandTransform.method("get_position").invoke();
                const rot = rightHandTransform.method("get_rotation").invoke();
                const forward = rightHandTransform.method("get_forward").invoke();

                // 1. Spawn Buggy
                const netObj = spawnNetworkPrefab("Vehicle_Buggy", pos, rot);

                if (netObj && !netObj.handle.isNull()) {
                    netObj.method("RequestStateAuthority").invoke();
                    
                    const rb = netObj.method("GetComponent", 1).inflate(Rigidbody).invoke();
                    if (rb && !rb.isNull()) {
                        rb.method("set_isKinematic").invoke(false);
                        rb.method("WakeUp").invoke();
                        
                        // 2. Launch Forward (Speed: 30.0)
                        const velocity = Vector3.method("op_Multiply", 2).invoke(forward, 30.0);
                        rb.method("set_linearVelocity").invoke(velocity);
                    }
                }
            } catch (e) { }
        }
    },
    toolTip: "Make a fist with your right hand to shoot cars forward."
}),
new ButtonInfo({
    buttonText: "crate",
    isTogglable: true,
    method: () => {
        if (rightGrab && rightTrigger && time > tagGunDelay) {
            tagGunDelay = time + 0.2; // 0.2s cooldown

            try {
                const pos = rightHandTransform.method("get_position").invoke();
                const rot = rightHandTransform.method("get_rotation").invoke();
                const forward = rightHandTransform.method("get_forward").invoke();

                // 1. Spawn Crate as an item prefab instead of a network prefab table lookup
                const netObj = spawnItemPrefab("item_crate", pos, rot);

                if (netObj && !netObj.handle.isNull()) {
                    requestStateAuthoritySafe(netObj);
                    
                    // 2. GET PHYSICS (Crates have rigidbodies too!)
                    const rb = getRigidbodySafe(netObj);
                    if (rb && !rb.isNull()) {
                        rb.method("set_isKinematic").invoke(false);
                        rb.method("WakeUp").invoke();
                        
                        // 3. LAUNCH (Speed: 20.0)
                        const velocity = Vector3.method("op_Multiply", 2).invoke(forward, 20.0);
                        rb.method("set_linearVelocity").invoke(velocity);
                    }
                }
            } catch (e) { }
        }
    },
    toolTip: "Make a fist with your right hand to shoot wooden crates forward."
}),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Dumper",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
                    buttonText: "Dump Item Names",
                    method: () => {
                        console.log("\n========== ITEM NAMES DUMP ==========");
                        for (let i = 0; i < itemIDs.length; i++) {
                            console.log("[" + i + "] " + itemIDs[i]);
                        }
                        console.log("========== TOTAL: " + itemIDs.length + " items ==========\n");
                        sendNotification("Dumped " + itemIDs.length + " item names to console", false);
                    },
                    isTogglable: false,
                    toolTip: "Dumps all item names to the Frida console."
                }),
                new ButtonInfo({
                    buttonText: "Dump Prefab Names",
                    method: () => {
                        console.log("\n========== PREFAB NAMES DUMP ==========");
                        for (let i = 0; i < prefabList.length; i++) {
                            console.log("[" + i + "] " + prefabList[i]);
                        }
                        console.log("========== TOTAL: " + prefabList.length + " prefabs ==========\n");
                        sendNotification("Dumped " + prefabList.length + " prefab names to console", false);
                    },
                    isTogglable: false,
                    toolTip: "Dumps all prefab names to the Frida console."
                }),
                new ButtonInfo({
                    buttonText: "Dump Mob Names",
                    method: () => {
                        console.log("\n========== MOB NAMES DUMP ==========");
                        for (let i = 0; i < mobIDs.length; i++) {
                            console.log("[" + i + "] " + mobIDs[i]);
                        }
                        console.log("========== TOTAL: " + mobIDs.length + " mobs ==========\n");
                        sendNotification("Dumped " + mobIDs.length + " mob names to console", false);
                    },
                    isTogglable: false,
                    toolTip: "Dumps all mob names to the Frida console."
                }),
                new ButtonInfo({
                    buttonText: "Dump All Names",
                    method: () => {
                        console.log("\n========================================");
                        console.log("         FULL NAME DUMP");
                        console.log("========================================");
                        console.log("\n--- ITEMS (" + itemIDs.length + ") ---");
                        for (let i = 0; i < itemIDs.length; i++) {
                            console.log("  [" + i + "] " + itemIDs[i]);
                        }
                        console.log("\n--- PREFABS (" + prefabList.length + ") ---");
                        for (let i = 0; i < prefabList.length; i++) {
                            console.log("  [" + i + "] " + prefabList[i]);
                        }
                        console.log("\n--- MOBS (" + mobIDs.length + ") ---");
                        for (let i = 0; i < mobIDs.length; i++) {
                            console.log("  [" + i + "] " + mobIDs[i]);
                        }
                        console.log("\n========================================");
                        console.log("TOTAL: " + (itemIDs.length + prefabList.length + mobIDs.length) + " names");
                        console.log("========================================\n");
                        sendNotification("Dumped ALL names to console", false);
                    },
                    isTogglable: false,
                    toolTip: "Dumps all item, prefab, and mob names to Frida console at once."
                }),
                new ButtonInfo({
                    buttonText: "Dump VFX Names",
                    method: () => {
                        dumpNamedList("VFX NAMES", vfxNames);
                        sendNotification("Dumped " + vfxNames.length + " VFX names", false);
                    },
                    isTogglable: false,
                    toolTip: "Dumps every selectable VFX name from the menu."
                }),
                new ButtonInfo({
                    buttonText: "Dump SFX Names",
                    method: () => {
                        const values = sfxIDList.map(id => id + " = " + (sfxNameMap[id] ?? id));
                        dumpNamedList("SFX NAMES", values);
                        sendNotification("Dumped " + values.length + " SFX names", false);
                    },
                    isTogglable: false,
                    toolTip: "Dumps the known SFX IDs and their names."
                }),
                new ButtonInfo({
                    buttonText: "Dump Container Methods",
                    method: () => {
                        dumpClassMembers("BackpackItem", BackpackItemClass);
                        dumpClassMembers("Quiver", QuiverClass);
                        if (CrossbowClass) dumpClassMembers("Crossbow", CrossbowClass);
                        if (HeartGunClass) dumpClassMembers("HeartGun", HeartGunClass);
                        sendNotification("Dumped container class members", false);
                    },
                    isTogglable: false,
                    toolTip: "Dumps fields and methods for bags, quivers, crossbows, and heart guns."
                }),
                new ButtonInfo({
                    buttonText: "Dump Spawn/FX Methods",
                    method: () => {
                        dumpClassMembers("PrefabGenerator", PrefabGen);
                        dumpClassMembers("ParticleManager", ParticleManagerClass);
                        dumpClassMembers("SFXManager", SFXManager);
                        sendNotification("Dumped spawn and FX class members", false);
                    },
                    isTogglable: false,
                    toolTip: "Dumps fields and methods for the spawn, VFX, and SFX systems."
                }),
                new ButtonInfo({
                    buttonText: "Dump Live Items",
                    method: () => {
                        try {
                            const allItems = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
                            if (!allItems || allItems.length === 0) {
                                sendNotification("No live items found in scene", false);
                                return;
                            }
                            console.log("\n========== LIVE SCENE ITEMS ==========");
                            const names = [];
                            for (let i = 0; i < allItems.length; i++) {
                                const item = allItems.get(i);
                                if (!item || item.handle.isNull()) continue;
                                try {
                                    const nameObj = item.method("get_gameObject").invoke().method("get_name").invoke();
                                    const name = nameObj ? nameObj.toString().replace(/^\x01|\x01$/g, '') : "(unknown)";
                                    names.push(name);
                                    console.log("[" + i + "] " + name);
                                } catch (_) { }
                            }
                            console.log("========== TOTAL: " + names.length + " live items ==========\n");
                            sendNotification("Dumped " + names.length + " live items to console", false);
                        } catch (e) {
                            console.error("Live item dump error:", e);
                            sendNotification("Dump failed: " + e, false);
                        }
                    },
                    isTogglable: false,
                    toolTip: "Scans the scene and dumps names of all active GrabbableObjects."
                }),
                new ButtonInfo({
                    buttonText: "Dump Live Prefabs",
                    method: () => {
                        try {
                            const runner = PrefabGen.field("_instance").value.method("get_runner").invoke();
                            if (!runner || runner.isNull()) {
                                sendNotification("Runner is null", false);
                                return;
                            }
                            const sources = runner.field("_config").value.field("PrefabTable").value.field("_sources").value;
                            const count = sources.method("get_Count").invoke();
                            console.log("\n========== ALL REGISTERED PREFABS ==========");
                            for (let i = 0; i < count; i++) {
                                try {
                                    const source = sources.method("get_Item").invoke(i);
                                    const desc = source.method("get_Description").invoke().toString().replace(/^\x01|\x01$/g, '');
                                    console.log("[" + i + "] " + desc);
                                } catch (_) { }
                            }
                            console.log("========== TOTAL: " + count + " registered prefabs ==========\n");
                            sendNotification("Dumped " + count + " registered prefabs to console", false);
                        } catch (e) {
                            console.error("Prefab table dump error:", e);
                            sendNotification("Dump failed: " + e, false);
                        }
                    },
                    isTogglable: false,
                    toolTip: "Dumps ALL registered prefab names from the network PrefabTable."
                }),
                new ButtonInfo({
                    buttonText: "Dump All Classes",
                    method: () => {
                        console.log("--- STARTING CLASS DUMP ---");
                        Il2Cpp.domain.assemblies.forEach(assembly => {
                            console.log("Assembly: " + assembly.name);
                            assembly.image.classes.forEach(klass => {
                                console.log("  [Class] " + klass.fullName);
                            });
                        });
                        sendNotification("Dumped all Classes to console!", false);
                    },
                    isTogglable: false, toolTip: "Dumps every class name in the game (Heavy lag!)."
                }),
                new ButtonInfo({
                    buttonText: "Dump All Live Images",
                    method: () => {
                        const TextureClass = UnityEngineCore.class("UnityEngine.Texture");
                        const allTextures = Object.method("FindObjectsByType", 1).inflate(TextureClass).invoke(0);
                        console.log("--- LIVE TEXTURE DUMP ---");
                        for (let i = 0; i < allTextures.length; i++) {
                            const tex = allTextures.get(i);
                            console.log(`[Image ${i}] Name: ${tex.method("get_name").invoke()} | Res: ${tex.method("get_width").invoke()}x${tex.method("get_height").invoke()}`);
                        }
                        sendNotification("Dumped all loaded image info to console!", false);
                    },
                    isTogglable: false, toolTip: "Dumps names and resolutions of all loaded textures."
                }),
                new ButtonInfo({
                    buttonText: "Item Inspector Gun",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        if (rightTrigger && gunData.ray && !gunData.ray.isNull()) {
                            try {
                                const hitObj = gunData.ray.method("get_collider").invoke().method("get_gameObject").invoke();
                                const name = hitObj.method("get_name").invoke().toString();

                                console.log("\n--- INSPECTING OBJECT ---");
                                console.log("Name: " + name);

                                // This logs all components attached to what you are looking at
                                const components = hitObj.method("GetComponents", 1).inflate(Component).invoke();
                                console.log("Attached Scripts:");
                                for (let i = 0; i < components.length; i++) {
                                    console.log("  [" + i + "] " + components.get(i).class.fullName);
                                }
                                sendNotification("Inspected: " + name + " (Check Console)", false);
                            } catch (e) { }
                        }
                    },
                    toolTip: "Point at an item to see every script attached to it in the PC console."
                }),
                new ButtonInfo({
                    buttonText: "Global Dump",
                    method: () => {
                        console.log("\n" + "=".repeat(60));
                        console.log("!!! STARTING GLOBAL CLASS DUMP !!!");
                        console.log("Searching every Assembly and Image...");
                        console.log("=".repeat(60));

                        let assemblyCount = 0;
                        let classCount = 0;

                        try {
                            // FIX: Use Il2Cpp.domain (lowercase d)
                            // This is the active instance of the game domain
                            const assemblies = Il2Cpp.domain.assemblies;

                            if (!assemblies) {
                                console.log("Error: Could not find 'assemblies' in Il2Cpp.domain.");
                                return;
                            }

                            assemblies.forEach(assembly => {
                                try {
                                    const img = assembly.image;
                                    if (!img || img.handle.isNull()) return;

                                    console.log(`\n>>> ASSEMBLY: ${assembly.name}`);
                                    assemblyCount++;

                                    // Loop through classes in the image
                                    img.classes.forEach(klass => {
                                        try {
                                            if (klass && klass.fullName) {
                                                console.log(`  [CLASS] ${klass.fullName}`);
                                                classCount++;
                                            }
                                        } catch (classErr) {
                                            // Skip invalid class handles
                                        }
                                    });
                                } catch (assemblyErr) {
                                    // Skip protected or invalid assemblies
                                }
                            });

                        } catch (globalErr) {
                            console.log("CRITICAL DUMP ERROR: " + globalErr);
                        }

                        console.log("\n" + "=".repeat(60));
                        console.log("GLOBAL DUMP COMPLETE");
                        console.log(`Assemblies Scanned: ${assemblyCount}`);
                        console.log(`Total Classes Found: ${classCount}`);
                        console.log("=".repeat(60));

                        sendNotification(`Dumped ${classCount} classes to console!`, false);
                    },
                    isTogglable: false,
                    toolTip: "Dumps every class in the game memory. Check PC console."
                }),
                new ButtonInfo({
                    buttonText: "dump to files folder (Android/data/com.Sunday.Taggers/files/",
                    method: () => {
                        // 1. The specific path you requested
                        const folderPath = "/sdcard/Android/data/com.Sunday.Taggers/files/";
                        const fileName = "dump.cs";
                        const fullPath = folderPath + fileName;

                        try {
                            console.log("Attempting to write dump to: " + fullPath);

                            // 2. Open the file in 'w' (write) mode
                            const file = new File(fullPath, "w");

                            file.write("// ==========================================\n");
                            file.write("// ANIMAL COMPANY: GLOBAL METADATA DUMP\n");
                            file.write(`// Generated for: com.Sunday.Taggers\n`);
                            file.write("// ==========================================\n\n");

                            let classCount = 0;

                            // 3. Iterate through all code
                            Il2Cpp.domain.assemblies.forEach(assembly => {
                                try {
                                    file.write(`\n// --- Assembly: ${assembly.name} ---\n`);

                                    assembly.image.classes.forEach(klass => {
                                        try {
                                            const namespace = klass.namespace || "Global";
                                            // Formatting as C# for better readability in VS Code
                                            file.write(`namespace ${namespace} { public class ${klass.name} { } }\n`);
                                            classCount++;
                                        } catch (_) { }
                                    });
                                    // Save progress to the physical storage after every assembly
                                    file.flush();
                                } catch (_) { }
                            });

                            file.close(); // Important: Close the file to save it!

                            console.log("SUCCESS! Found " + classCount + " classes.");
                            sendNotification("File Saved: Android/data/.../files/dump.cs", false);

                        } catch (e) {
                            console.log("CRITICAL ERROR: " + e);
                            // If it fails, it's usually because the folder 'com.Sunday.Taggers' doesn't exist yet
                            sendNotification("Write Failed! Is the folder created?", false);
                            console.log("TIP: Make sure the folder 'com.Sunday.Taggers/files' exists on your Quest.");
                        }
                    },
                    isTogglable: false,
                    toolTip: "Saves a full .cs dump to the com.Sunday.Taggers data folder."
                }),
                new ButtonInfo({
                    buttonText: "organized dump",
                    method: () => {
                        const folderPath = "/sdcard/Android/data/com.Sunday.Taggers/files/";
                        console.log("Starting Auto-Split Dump...");

                        try {
                            // 1. We first group all classes by their namespace in memory
                            let namespaceMap = {};

                            Il2Cpp.domain.assemblies.forEach(assembly => {
                                try {
                                    assembly.image.classes.forEach(klass => {
                                        try {
                                            let ns = klass.namespace || "Global";
                                            // Clean namespace name for file naming (remove illegal chars)
                                            ns = ns.replace(/[<>:"/\\|?*]/g, "_");

                                            if (!namespaceMap[ns]) namespaceMap[ns] = [];

                                            // Store the class name and which assembly it came from
                                            namespaceMap[ns].push({
                                                name: klass.name,
                                                assembly: assembly.name
                                            });
                                        } catch (_) { }
                                    });
                                } catch (_) { }
                            });

                            // 2. Now we write each namespace to its own file
                            let fileCount = 0;
                            let totalClasses = 0;

                            for (let ns in namespaceMap) {
                                const fullPath = folderPath + ns + ".cs";
                                try {
                                    const file = new File(fullPath, "w");

                                    file.write("// ==========================================\n");
                                    file.write(`// NAMESPACE: ${ns}\n`);
                                    file.write("// Generated by Auto-Split Dumper\n");
                                    file.write("// ==========================================\n\n");

                                    namespaceMap[ns].forEach(item => {
                                        file.write(`// From Assembly: ${item.assembly}\n`);
                                        file.write(`namespace ${ns} { public class ${item.name} { } }\n\n`);
                                        totalClasses++;
                                    });

                                    file.close();
                                    fileCount++;
                                    console.log(`Created: ${ns}.cs`);
                                } catch (e) {
                                    console.log(`Failed to write ${ns}.cs: ${e}`);
                                }
                            }

                            console.log(`--- DUMP COMPLETE ---`);
                            console.log(`Files Created: ${fileCount}`);
                            console.log(`Total Classes: ${totalClasses}`);
                            sendNotification(`Created ${fileCount} .cs files in data folder!`, false);

                        } catch (e) {
                            console.log("Critical Error: " + e);
                            sendNotification("Dump Failed! Check Console.", false);
                        }
                    },
                    isTogglable: false,
                    toolTip: "Creates a separate .cs file for every namespace (AnimalCompany, Global, etc.)"
                }),
                new ButtonInfo({
                    buttonText: "inspector gun (NW)",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        const ray = gunData.ray;

                        if (rightTrigger && ray && !ray.isNull() && time > lagGunDelay) {
                            lagGunDelay = time + 1.2;

                            try {
                                const hitCollider = ray.method("get_collider").invoke();
                                const hitObj = hitCollider.method("get_gameObject").invoke();
                                const objName = hitObj.method("get_name").invoke().toString();

                                console.log("\n" + "==".repeat(30));
                                console.log(`NATIVE DUMP FOR: "${objName}"`);
                                console.log("==".repeat(30));

                                // Get Components list
                                const ComponentClass = UnityEngineCore.class("UnityEngine.Component");
                                const components = hitObj.method("GetComponents", 1).invoke(ComponentClass.type.object);
                                const count = components.length;

                                for (let i = 0; i < count; i++) {
                                    const comp = components.get(i);
                                    if (!comp || comp.handle.isNull()) continue;

                                    // --- THE SAFETY TRICK ---
                                    // We call .ToString() on the object itself.
                                    // This returns something like "AnimalCompany.PlayerController" 
                                    // or "UnityEngine.BoxCollider". It NEVER touches the Il2Cpp Metadata tables.
                                    const nativeName = comp.method("ToString").invoke().toString();

                                    console.log(`\n[COMPONENT ${i}]: ${nativeName}`);

                                    // Only perform a deep peek if it's clearly a game script
                                    if (nativeName.includes("AnimalCompany")) {
                                        try {
                                            // We only access .class inside a local Try-Catch 
                                            // after confirming the name is safe.
                                            const klass = comp.class;

                                            console.log("  >> Variables:");
                                            klass.fields.forEach(f => {
                                                try {
                                                    const val = f.bind(comp).value;
                                                    console.log(`     - ${f.name} = ${val}`);
                                                } catch (_) { }
                                            });

                                            console.log("  >> Functions:");
                                            klass.methods.forEach(m => {
                                                if (m.name.startsWith("get_") || m.name.startsWith("set_")) return;
                                                console.log(`     (Fn) ${m.name}`);
                                            });
                                        } catch (innerErr) {
                                            console.log("  >> [!] Metadata Protected (Even for Game Script)");
                                        }
                                    } else {
                                        console.log("  >> (Unity Engine Internal - Detail Peeking Skipped)");
                                    }
                                }

                                console.log("\n" + "==".repeat(30));
                                sendNotification("Native Dump Complete!", false);

                            } catch (e) {
                                console.error("Inspector Blocked Crash: " + e);
                            }
                        }
                    },
                    toolTip: "Uses Unity's native strings to identify objects. Fixes the Access Violation crash."
                }),
                new ButtonInfo({
                    buttonText: "Dump All RPCs",
                    method: () => {
                        const folderPath = "/sdcard/Android/data/com.Sunday.Taggers/files/";
                        const fileName = "network_rpcs.txt";
                        const fullPath = folderPath + fileName;

                        try {
                            console.log("\n" + "=".repeat(60));
                            console.log("!!! STARTING GLOBAL RPC DUMP !!!");
                            console.log("Searching for Networked Commands (RPC_)...");
                            console.log("=".repeat(60));

                            const file = new File(fullPath, "w");
                            file.write("=== ANIMAL COMPANY: NETWORK RPC DUMP ===\n");
                            file.write("Generated for discovery and remote execution.\n\n");

                            let rpcCount = 0;

                            // 1. We focus on the main game assembly (AnimalCompany)
                            AssemblyCSharp.classes.forEach(klass => {
                                let classHasRPC = false;
                                let classOutput = `\n[CLASS] ${klass.fullName}\n`;

                                try {
                                    klass.methods.forEach(m => {
                                        // 2. Logic: Most Photon Fusion RPCs start with "RPC_" 
                                        // or are inside classes like "NetPlayer" or "NetworkManager"
                                        if (m.name.startsWith("RPC_") || m.name.includes("Rpc")) {
                                            const params = m.parameters.map(p => `${p.type.name} ${p.name}`).join(", ");
                                            classOutput += `   -> (RPC) ${m.name}(${params})\n`;
                                            rpcCount++;
                                            classHasRPC = true;
                                        }
                                    });

                                    // 3. Only write the class to the file if it actually contains an RPC
                                    if (classHasRPC) {
                                        file.write(classOutput);
                                        file.flush(); // Save progress
                                    }
                                } catch (_) { }
                            });

                            file.close();

                            console.log(`SUCCESS! Found ${rpcCount} Networked RPCs.`);
                            console.log(`File saved to: ${fullPath}`);
                            sendNotification(`Dumped ${rpcCount} RPCs to files/rpcs.txt`, false);

                        } catch (e) {
                            console.log("RPC Dump Error: " + e);
                            sendNotification("RPC Dump Failed!", false);
                        }
                    },
                    isTogglable: false,
                    toolTip: "Finds every network command (Teleport, Kill, Spawn) and saves them to a file."
                }),
                new ButtonInfo({
                    buttonText: "inspector gun v2 (NW)",
                    isTogglable: true,
                    method: () => {
                        if (!rightGrab) return;
                        const gunData = renderGun();
                        const ray = gunData.ray;

                        if (rightTrigger && ray && !ray.isNull() && time > lagGunDelay) {
                            lagGunDelay = time + 1.2;

                            try {
                                // 1. Get the GameObject
                                const hitCollider = ray.method("get_collider").invoke();
                                const hitObj = hitCollider.method("get_gameObject").invoke();
                                const objName = hitObj.method("get_name").invoke().toString();

                                console.log("\n" + "=".repeat(60));
                                console.log(`BRUTE FORCE DUMP: "${objName}"`);
                                console.log("=".repeat(60));

                                // 2. Get Components list as RAW Pointers
                                const ComponentClass = UnityEngineCore.class("UnityEngine.Component");
                                const components = hitObj.method("GetComponents", 1).invoke(ComponentClass.type.object);
                                const count = components.length;

                                for (let i = 0; i < count; i++) {
                                    const comp = components.get(i);
                                    if (!comp || comp.handle.isNull()) continue;

                                    try {
                                        // --- THE TRICK: USE System.Object.GetType() ---
                                        // We call GetType() and then GetFullName() on the Type object.
                                        // This stays 100% inside the game's logic and NEVER touches Frida's metadata bridge.
                                        const typeObj = comp.method("GetType").invoke();
                                        const typeName = typeObj.method("get_FullName").invoke().toString();

                                        console.log(`\n[COMPONENT ${i}]: ${typeName}`);

                                        // Only deep peek if it's AnimalCompany, otherwise just stop here.
                                        if (typeName.includes("AnimalCompany")) {
                                            console.log("  >> (Game Script Detected - Peeking Variables...)");

                                            // We use a VERY limited manual field scan
                                            // We don't use .fields loop, we look for specific common names
                                            const commonFields = ["health", "_health", "value", "_value", "itemID", "_itemID", "isMine", "id"];

                                            commonFields.forEach(fName => {
                                                try {
                                                    // Manually check for field by name instead of looping all
                                                    const field = comp.class.field(fName);
                                                    if (field) {
                                                        const val = field.bind(comp).value;
                                                        console.log(`     - ${fName} = ${val}`);
                                                    }
                                                } catch (_) { }
                                            });
                                        } else {
                                            console.log("  >> (Unity Engine Script - Access Restricted)");
                                        }

                                    } catch (e) {
                                        // If it still crashes on a specific component, we catch it here
                                        console.log(`  [!] Component ${i} is too protected to identify.`);
                                    }
                                }

                                console.log("\n" + "=".repeat(60));
                                sendNotification("Inspector Scan Finished!", false);

                            } catch (e) {
                                console.error("CRITICAL SAFETY BLOCK: " + e);
                            }
                        }
                    },
                    toolTip: "The most basic, safest inspector. Identifies names without touching metadata files."
                }),
                new ButtonInfo({
    buttonText: "NUCLEAR: Dump All Items",
    method: () => {
        const fullPath = "/sdcard/Android/data/com.Sunday.Taggers/files/discovered_items.txt";
        try {
            const runner = PrefabGen.field("_instance").value.method("get_runner").invoke();
            if (!runner || runner.isNull()) {
                sendNotification("Join a lobby first!", false);
                return;
            }

            const file = new File(fullPath, "w");
            const sources = runner.field("_config").value.field("PrefabTable").value.field("_sources").value;
            const count = sources.method("get_Count").invoke();

            file.write("=== PREFAB TABLE DISCOVERY ===\n");
            for (let i = 0; i < count; i++) {
                try {
                    const source = sources.method("get_Item").invoke(i);
                    const desc = source.method("get_Description").invoke().toString().replace(/[^\x20-\x7E]/g, '');
                    file.write(`${desc}\n`);
                } catch(_) {}
            }
            file.close();
            sendNotification("Saved all Prefab IDs to discovered_items.txt", false);
        } catch (e) { console.error(e); }
    },
    isTogglable: false
}),
new ButtonInfo({
    buttonText: "NUCLEAR: Dump All RPCs",
    method: () => {
        const fullPath = "/sdcard/Android/data/com.Sunday.Taggers/files/discovered_rpcs.txt";
        try {
            const file = new File(fullPath, "w");
            file.write("=== GLOBAL RPC DISCOVERY ===\n\n");
            
            let total = 0;
            // Iterate through every Assembly/DLL loaded in the game
            Il2Cpp.domain.assemblies.forEach(assembly => {
                assembly.image.classes.forEach(klass => {
                    try {
                        let hasRPC = false;
                        let output = `[${assembly.name}] ${klass.fullName}\n`;
                        
                        klass.methods.forEach(m => {
                            // Find any method starting with RPC (capital or lowercase)
                            if (m.name.toLowerCase().startsWith("rpc_")) {
                                const params = m.parameters.map(p => `${p.type.name}`).join(", ");
                                output += `   -> ${m.name}(${params})\n`;
                                hasRPC = true;
                                total++;
                            }
                        });

                        if (hasRPC) {
                            file.write(output + "\n");
                            file.flush();
                        }
                    } catch(_) {}
                });
            });

            file.close();
            sendNotification(`Found and saved ${total} RPCs!`, false);
        } catch (e) { console.error(e); }
    },
    isTogglable: false
}),
new ButtonInfo({
    buttonText: "NUCLEAR: Dump All Mobs",
    method: () => {
        const fullPath = "/sdcard/Android/data/com.Sunday.Taggers/files/discovered_controllers.txt";
        try {
            const file = new File(fullPath, "w");
            file.write("=== CONTROLLER & MOB DISCOVERY ===\n\n");
            
            let total = 0;
            Il2Cpp.domain.assemblies.forEach(assembly => {
                assembly.image.classes.forEach(klass => {
                    try {
                        // Logic: Any class ending in 'Controller' is usually an AI or Player script
                        if (klass.name.endsWith("Controller")) {
                            file.write(`[CLASS] ${klass.fullName}\n`);
                            file.write(`  Base: ${klass.parent ? klass.parent.name : "None"}\n`);
                            
                            // Log important fields like 'Health' or 'State'
                            klass.fields.forEach(f => {
                                const fn = f.name.toLowerCase();
                                if (fn.includes("health") || fn.includes("state") || fn.includes("target")) {
                                    file.write(`    - Field: ${f.name} (${f.type.name})\n`);
                                }
                            });
                            file.write("\n");
                            total++;
                        }
                    } catch(_) {}
                });
            });

            file.close();
            sendNotification(`Found ${total} Controllers!`, false);
        } catch (e) { console.error(e); }
    },
    isTogglable: false
}),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Test Mods",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
new ButtonInfo({
    buttonText: "spawn all mobs gun",
    isTogglable: true,
    method: () => {
        if (!rightGrab) return;
        const gunData = renderGun();
        const ray = gunData.ray;

        if (rightTrigger && ray && !ray.isNull() && time > tagGunDelay) {
            tagGunDelay = time + 2.0; // 2 second cooldown (this is a very heavy mod)

            try {
                const hitPoint = ray.method("get_point").invoke();
                sendNotification("Spawning the Horde... Lag incoming!", false);

                // Loop through every mob entry in your mobIDs list
                for (let i = 0; i < mobIDs.length; i++) {
                    const mobEntry = mobIDs[i];
                    
                    // Create a tiny offset for each mob so they spread out slightly
                    const offset = [
                        (Math.random() - 0.5) * 2, 
                        0.5, 
                        (Math.random() - 0.5) * 2
                    ];
                    const spawnPos = Vector3.method("op_Addition", 2).invoke(hitPoint, offset);

                    // Use your existing spawnMobAtPos helper
                    spawnMobAtPos(mobEntry, spawnPos, identityQuaternion);
                }
                
                console.log("✓ Spawned " + mobIDs.length + " different monsters.");
            } catch (e) {
                console.error("Horde Spawn Error: " + e);
            }
        }
    },
    toolTip: "Point and pull trigger to spawn every single monster in the game's database at once."
}),
new ButtonInfo({
    buttonText: "kill all mobs",
    method: () => {
        try {
            const MobClass = AssemblyCSharp.class("AnimalCompany.MobController");
            // Find every active monster in the map
            const allMobs = Object.method("FindObjectsByType", 1).inflate(MobClass).invoke(0);
            
            if (!allMobs || allMobs.length === 0) {
                sendNotification("No monsters found to kill!", false);
                return;
            }

            let killCount = 0;
            for (let i = 0; i < allMobs.length; i++) {
                const mob = allMobs.get(i);
                if (!mob || mob.handle.isNull()) continue;

                try {
                    // 1. Request authority so the server accepts the death
                    const netObj = mob.method("get_Object").invoke();
                    if (netObj) netObj.method("RequestStateAuthority").invoke();

                    // 2. Call the death RPCs from your dump
                    // We try multiple methods in case the game version changed
                    const killRPC = tryMethod(mob, "RPC_KillMob") || 
                                   tryMethod(mob, "RPC_Hit", 2) || 
                                   tryMethod(mob, "ForceDie");
                    
                    if (killRPC) {
                        // If using RPC_Hit, we send 999,999 damage
                        if (killRPC.name === "RPC_Hit") {
                            killRPC.invoke(999999, getTransform(mob).method("get_position").invoke());
                        } else {
                            killRPC.invoke();
                        }
                        killCount++;
                    }
                } catch (_) { }
            }
            sendNotification(`GENOCIDE: Eliminated ${killCount} monsters!`, false);
        } catch (e) {
            console.error("Kill All Monsters Error: " + e);
        }
    },
    isTogglable: false,
    toolTip: "Instantly finds and kills every monster currently spawned on the map."
}),
new ButtonInfo({
    buttonText: "Log My Position",
    method: () => {
        try {
            // 1. SAFETY: Ensure the player actually exists
            if (!GTPlayer || GTPlayer.handle.isNull()) {
                sendNotification("Player not found!", false);
                return;
            }

            // 2. Get the Transform and the Position Vector
            const tf = getTransform(GTPlayer);
            if (!tf || tf.isNull()) return;

            const pos = tf.method("get_position").invoke();
            if (!pos) return;

            // 3. Extract and format the coordinates
            // .toFixed(3) rounds it to 3 decimal places for readability
            const x = pos.field("x").value.toFixed(3);
            const y = pos.field("y").value.toFixed(3);
            const z = pos.field("z").value.toFixed(3);

            // 4. PRINT TO PC CONSOLE (Frida Terminal)
            console.log("\n" + "=".repeat(30));
            console.log(`[COORD DUMP]`);
            console.log(`X: ${x}`);
            console.log(`Y: ${y}`);
            console.log(`Z: ${z}`);
            console.log(`JSON: [${x}, ${y}, ${z}]`);
            console.log("=".repeat(30));

            // 5. Visual feedback in VR
            sendNotification(`Pos: ${x}, ${y}, ${z}`, false, 8);
            
        } catch (e) {
            console.error("Log Position Error: " + e);
        }
    },
    isTogglable: false,
    toolTip: "Dumps your current coordinates to the Frida console. Useful for creating TP locations."
}),
new ButtonInfo({
    buttonText: "ChangeExplosion Type",
    method: () => {
        // Hold right grip to cycle backwards
        if (rightGrab) {
            explosionIdx--;
        } else {
            explosionIdx++;
        }
        // Wrap around the list
        explosionIdx = ((explosionIdx % explosionTypes.length) + explosionTypes.length) % explosionTypes.length;
        sendNotification("Explosion: " + explosionTypes[explosionIdx], false, 4);
    },
    isTogglable: false,
    toolTip: "Cycle through 24 explosion types. Hold grip to go back."
}),
new ButtonInfo({
    buttonText: "spawn explosion gun",
    isTogglable: true,
    method: () => {
        if (!rightGrab) return;
        const gunData = renderGun();
        const ray = gunData.ray;

        if (rightTrigger && ray && !ray.isNull() && time > lagGunDelay) {
            lagGunDelay = time + 0.3; // Cooldown to prevent spam crashes

            try {
                const hitPos = ray.method("get_point").invoke();
                const explosionName = explosionTypes[explosionIdx];

                // Use the spawnNetworkPrefab function (since these are 'Static' Network Prefabs)
                const result = spawnNetworkPrefab(explosionName, hitPos, identityQuaternion);

                if (result && !result.isNull()) {
                    // Request authority so it actually detonates
                    result.method("RequestStateAuthority").invoke();
                    
                    sendNotification("BOOM: " + explosionName, false);
                } else {
                    sendNotification("Explosion not found in PrefabTable", false);
                }
            } catch (e) { console.error("Explosion Gun Error: " + e); }
        }
    },
    toolTip: "Aim and pull trigger to spawn the selected explosion type."
}),
new ButtonInfo({
    buttonText: "select current item in right hand",
    method: () => {
        try {
            // 1. Get the Local Player
            const player = NetPlayer.method("get_localPlayer").invoke();
            if (!player || player.handle.isNull()) return;

            // 2. Get the Right Hand Interactor (Index 1)
            const interactor = getHandInteractor(player, 1);
            if (!interactor || interactor.isNull()) return;

            // 3. Access the held object
            const itemAnchor = interactor.field("_itemAnchor").value;
            if (!itemAnchor || itemAnchor.isNull()) {
                sendNotification("Right hand is empty!", false);
                return;
            }

            const grabbable = itemAnchor.method("get_grabbableObject").invoke();
            if (!grabbable || grabbable.isNull()) return;

            // 4. Get the GrabbableItem component to find the ID
            const gbi = grabbable.method("GetComponent", 1).inflate(GBIClass).invoke();
            if (!gbi || gbi.isNull()) return;

            // 5. Extract the ItemID string (e.g., "item_apple")
            const idObj = gbi.method("get_itemID").invoke();
            if (!idObj || idObj.isNull()) return;

            const foundID = idObj.content; // Convert Il2Cpp string to JS string

            // 6. SYNC WITH MENU: Search for this ID in your itemIDs list
            const listIndex = itemIDs.indexOf(foundID);

            if (listIndex !== -1) {
                // Found it! Update your menu selection
                itemIndex = listIndex;
                sendNotification(`Identified: ${foundID}\nUpdated selection!`, false);
                console.log(`[IDENTIFIER] Match found: "${foundID}" is now selected (Index ${listIndex}).`);
                
                // Refresh menu to show the change
                reloadMenu(); 
            } else {
                // If the item isn't in your hardcoded list yet
                sendNotification(`Found: ${foundID}\n(Not in menu list)`, false);
                console.log(`[IDENTIFIER] The item "${foundID}" was found, but it is missing from your itemIDs array.`);
            }

        } catch (e) {
            console.error("Identify Held Item Error: " + e);
        }
    },
    isTogglable: false,
    toolTip: "Identifies the item in your right hand and sets it as the active item in your menu."
}),
new ButtonInfo({
    buttonText: "start zombie",
    method: () => {
        try {
            // 1. Find the Lobby Handler (the brain of the event)
            const HandlerClass = AssemblyCSharp.class("AnimalCompany.HordeMobLobbyHandler");
            const allHandlers = Object.method("FindObjectsByType", 1).inflate(HandlerClass).invoke(0);
            
            if (allHandlers && allHandlers.length > 0) {
                const handler = allHandlers.get(0);
                
                // 2. Request Network Authority
                const netObj = handler.method("get_Object").invoke();
                if (netObj) netObj.method("RequestStateAuthority").invoke();

                // 3. Trigger the official Wave Start RPC from your dump
                handler.method("RPC_WaveStart").invoke();
                
                sendNotification("ZOMBIE EVENT STARTED!", false);
                console.log("✓ HordeMobLobbyHandler: RPC_WaveStart invoked.");
            } else {
                sendNotification("Horde Handler not found in this map.", false);
            }
        } catch (e) {
            console.error("Zombie Event Error: " + e);
        }
    },
    isTogglable: false,
    toolTip: "Uses the HordeMobLobbyHandler to start the official Zombie Wave event."
}),

new ButtonInfo({
    buttonText: "end zombie",
    method: () => {
        try {
            const HandlerClass = AssemblyCSharp.class("AnimalCompany.HordeMobLobbyHandler");
            const handler = Object.method("FindObjectsByType", 1).inflate(HandlerClass).invoke(0).get(0);
            
            if (handler) {
                handler.method("get_Object").invoke().method("RequestStateAuthority").invoke();
                
                // Use the Cancel/Reset RPCs from your dump
                handler.method("RPC_WaveCancel").invoke() || handler.method("RPC_ResetStartButtonPressed").invoke();
                
                sendNotification("Horde Event Cancelled.", false);
            }
        } catch (e) { }
    },
    isTogglable: false
}),
new ButtonInfo({
    buttonText: "break mobs",
    method: () => {
        try {
            const HandlerClass = AssemblyCSharp.class("AnimalCompany.HordeMobLobbyHandler");
            const handler = Object.method("FindObjectsByType", 1).inflate(HandlerClass).invoke(0).get(0);
            
            if (handler) {
                handler.method("get_Object").invoke().method("RequestStateAuthority").invoke();
                
                // Use the Cancel/Reset RPCs from your dump
                handler.method("RPC_WaveCancel").invoke() || handler.method("RPC_ResetStartButtonPressed").invoke();
                
                sendNotification("Horde Event Cancelled.", false);
            }
        } catch (e) { }
    },
    isTogglable: false
}),
new ButtonInfo({
    buttonText: "Orbit Items Around You",
    isTogglable: true,
    method: () => {
        if (frameCount % 5 !== 0) return;
        try {
            const myPos = getTransform(headCollider).method("get_position").invoke();
            const allItems = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
            for (let i = 0; i < allItems.length; i++) {
                const item = allItems.get(i);
                if (!item || item.isNull()) continue;
                const angle = (time * 1.25) + (i * 0.35);
                const offset = [Math.cos(angle) * 2.5, 1.15, Math.sin(angle) * 2.5];
                const orbitPos = Vector3.method("op_Addition", 2).invoke(myPos, offset);
                tryTeleportGrabbable(item, orbitPos);
            }
        } catch (_) { }
    },
    toolTip: "Makes every item on the map float and spin around your head."
}),
new ButtonInfo({
    buttonText: "Orbit Selected Items",
    isTogglable: true,
    method: () => {
        if (frameCount % 5 !== 0) return;
        try {
            const selectedID = itemIDs[itemIndex];
            if (!selectedID) return;
            const myPos = getTransform(headCollider).method("get_position").invoke();
            const allItems = Object.method("FindObjectsByType", 1).inflate(GBOClass).invoke(0);
            let matchedIndex = 0;
            for (let i = 0; i < allItems.length; i++) {
                const item = allItems.get(i);
                if (!item || item.isNull()) continue;
                const itemID = getItemIDFromGrabbable(item);
                if (itemID !== selectedID) continue;
                const angle = (time * 1.6) + (matchedIndex * 0.9);
                const offset = [Math.cos(angle) * 1.8, 0.8 + (matchedIndex % 3) * 0.35, Math.sin(angle) * 1.8];
                const orbitPos = Vector3.method("op_Addition", 2).invoke(myPos, offset);
                tryTeleportGrabbable(item, orbitPos);
                matchedIndex++;
            }
        } catch (_) { }
    },
    toolTip: "Orbits only the currently selected item ID from Settings around you."
}),
new ButtonInfo({
    buttonText: "Orbit Monsters",
    isTogglable: true,
    method: () => {
        if (frameCount % 4 !== 0 || !MobControllerBase) return;
        try {
            const myPos = getTransform(headCollider).method("get_position").invoke();
            const allMobs = Object.method("FindObjectsByType", 1).inflate(MobControllerBase).invoke(0);
            for (let i = 0; i < allMobs.length; i++) {
                const mob = allMobs.get(i);
                if (!mob || mob.handle.isNull()) continue;
                const angle = (time * 1.1) + (i * (Math.PI * 2 / Math.max(1, allMobs.length)));
                const orbitPos = [
                    myPos.field("x").value + Math.cos(angle) * 5.0,
                    myPos.field("y").value + 1.0,
                    myPos.field("z").value + Math.sin(angle) * 5.0
                ];
                tryTeleportMob(mob, orbitPos);
            }
        } catch (_) { }
    },
    toolTip: "Moves active monsters into a ring around you."
}),
new ButtonInfo({
    buttonText: "Orbit Players",
    isTogglable: true,
    method: () => {
        if (frameCount % 2 !== 0) return;
        try {
            const me = getLocalNetPlayer();
            if (!me || me.handle.isNull()) return;
            const myPos = getTransform(me).method("get_position").invoke();
            const allNetPlayers = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
            const others = [];
            for (let i = 0; i < allNetPlayers.length; i++) {
                const p = allNetPlayers.get(i);
                if (p && !p.handle.isNull() && !playerIsLocal(p)) {
                    others.push(p);
                }
            }
            if (others.length === 0) return;
            for (let i = 0; i < others.length; i++) {
                const target = others[i];
                const angle = (time * 1.5) + (i * (Math.PI * 2 / others.length));
                const orbitPos = [
                    myPos.field("x").value + Math.cos(angle) * 4.0,
                    myPos.field("y").value + 0.5,
                    myPos.field("z").value + Math.sin(angle) * 4.0
                ];
                tryTeleportNetPlayer(target, orbitPos);
            }
        } catch (_) { }
    },
    toolTip: "Forces all players to spin around you in a circle via Network RPC."
}),
new ButtonInfo({
    buttonText: "Mass Stun Everyone",
    isTogglable: true,
    method: () => {
        if (frameCount % 60 !== 0) return;
        try {
            const netPlayers = Object.method("FindObjectsByType", 1).inflate(NetPlayer).invoke(0);
            for (let i = 0; i < netPlayers.length; i++) {
                const p = netPlayers.get(i);
                if (p && !p.isNull() && !playerIsLocal(p)) {
                    p.method("RPC_PlayerStun").invoke(getTransform(p).method("get_position").invoke(), 999.0, 5.0, 0);
                }
            }
        } catch (e) { }
    },
    toolTip: "Repeatedly stuns every other player in the lobby."
}),
new ButtonInfo({
    buttonText: "Infinite Money For All",
    isTogglable: false,
    method: () => {
        try {
            const me = getLocalNetPlayer();
            if (me && !me.isNull()) {
                for (let i = 0; i < 50; i++) me.method("RPC_AddPlayerMoney").invoke(9999);
            }
            sendNotification("Raining Money!", false);
        } catch (e) { }
    },
    toolTip: "Spams the money RPC giving everyone insane cash chunks instantly."
}),
new ButtonInfo({
    buttonText: "Explode Shop Machine",
    isTogglable: false,
    method: () => {
        try {
            const MachineClass = AssemblyCSharp.class("AnimalCompany.ItemSellingMachineController");
            const machines = Object.method("FindObjectsByType", 1).inflate(MachineClass).invoke(0);
            if (machines && machines.length > 0) {
                const machine = machines.get(0);
                requestStateAuthoritySafe(machine);
                machine.method("RPC_ExplodeMachine").invoke();
            }
            sendNotification("Blowing up the shop...", false);
        } catch (e) { }
    },
    toolTip: "Detonates the shop violently on all screens."
}),
new ButtonInfo({
    buttonText: "Drop Everyone's Items",
    isTogglable: true,
    method: () => {
        if (frameCount % 30 !== 0) return;
        try {
            const ItemAnchorClass = AssemblyCSharp.class("AnimalCompany.AttachedItemAnchor");
            const anchors = Object.method("FindObjectsByType", 1).inflate(ItemAnchorClass).invoke(0);
            for (let i = 0; i < anchors.length; i++) {
                const anchor = anchors.get(i);
                if (anchor && !anchor.isNull()) {
                    anchor.method("RPC_DropObject").invoke(0, zeroVector, identityQuaternion, zeroVector, zeroVector, true);
                    anchor.method("RPC_UnattachObject").invoke();
                }
            }
        } catch (e) { }
    },
    toolTip: "Continuously forces all players to drop whatever they are holding globally."
}),
new ButtonInfo({
    buttonText: "Zero Weapon Cooldowns (NW)",
    isTogglable: true,
    enableMethod: () => { zeroCooldown = true; },
    disableMethod: () => { zeroCooldown = false; },
    toolTip: "Removes firing delay for Shotguns and AutoReload weapons."
}),

new ButtonInfo({
    buttonText: "select last held items id",
    method: () => {
        updateLastHeldIDFromHands();
        const idx = itemIDs.indexOf(lastHeldID);
        if (idx !== -1) {
            itemIndex = idx; // Update your menu's selection
            sendNotification("ID Selected: " + lastHeldID, false);
            reloadMenu();
        } else {
            sendNotification("ID found but not in list: " + lastHeldID, false);
        }
    },
    isTogglable: false,
    toolTip: "Automatically selects the last item you had in your right hand."
}),
new ButtonInfo({
    buttonText: "delete all prefabs",
    method: () => {
        try {
            // 1. Get the Network Runner (The boss of the server)
            const runner = PrefabGen.field("_instance").value.method("get_runner").invoke();
            if (!runner || runner.isNull()) {
                sendNotification("Join a lobby first!", false);
                return;
            }

            // 2. Find every object in the game with a NetworkObject component
            const NOClass = Il2Cpp.domain.assembly("Fusion.Runtime").image.class("Fusion.NetworkObject");
            const allNetObjs = Object.method("FindObjectsByType", 1).inflate(NOClass).invoke(0);

            if (!allNetObjs || allNetObjs.length === 0) {
                sendNotification("No prefabs found to delete.", false);
                return;
            }

            let count = 0;
            for (let i = 0; i < allNetObjs.length; i++) {
                const netObj = allNetObjs.get(i);
                if (!netObj || netObj.handle.isNull()) continue;

                try {
                    // --- SAFETY CHECK: DO NOT DELETE PLAYERS ---
                    if (isPlayerRelatedObject(netObj)) continue;

                    // 3. THE AUTHORITY THEFT
                    // Force the server to let us control this object
                    requestStateAuthoritySafe(netObj);

                    // 4. THE DELETION
                    // This removes the object for EVERYONE in the lobby
                    runner.method("Despawn").invoke(netObj);
                    count++;
                } catch (innerErr) {
                    // Skip objects that are already being deleted by the game
                }
            }

            sendNotification(`ERASE COMPLETE: Deleted ${count} objects!`, false);
            console.log(`[NUCLEAR] Wiped ${count} Networked Prefabs from the world.`);

        } catch (e) {
            console.error("Nuclear Erase Error: " + e);
        }
    },
    isTogglable: false,
    toolTip: "Takes authority and deletes EVERY spawned prefab (Crates, Machines, Items, Monsters)."
}),
new ButtonInfo({
    buttonText: "Becoming: Selling Machine",
    isTogglable: true,
    enableMethod: () => {
        try {
            const MachineClass = AssemblyCSharp.class("AnimalCompany.ItemSellingMachineController");
            // 1. Find the machine or spawn a new one if none exists
            let machine = findActiveObject(MachineClass);
            
            if (!machine || machine.isNull()) {
                const myPos = getTransform(bodyCollider).method("get_position").invoke();
                machine = spawnNetworkPrefab("ItemSellingMachineController", myPos, identityQuaternion);
            }

            if (machine && !machine.isNull()) {
                machineAvatar = machine;
                requestStateAuthoritySafe(machineAvatar);

                // 2. Parent it to your body
                const machineTF = machine.method("get_transform").invoke();
                const myTF = getTransform(GTPlayer);
                
                // SetParent(target, worldPositionStays)
                machineTF.method("SetParent", 2).invoke(myTF, false);
                
                // Offset it so you are inside the center of the machine
                machineTF.method("set_localPosition").invoke([0, -0.5, 0]);
                machineTF.method("set_localRotation").invoke(identityQuaternion);

                // 3. Disable machine physics so it doesn't bump into things
                const rb = machine.method("GetComponent", 1).inflate(Rigidbody).invoke();
                if (rb) rb.method("set_isKinematic").invoke(true);

                // 4. HIDE YOUR BODY (Optional: makes the effect better)
                const localNet = getLocalNetPlayer();
                if (localNet) localNet.method("RPC_SetHide").invoke(true);

                sendNotification("You have BECOME the Shop!", false);
            }
        } catch (e) { console.error("Machine Avatar Error: " + e); }
    },
    disableMethod: () => {
        if (machineAvatar) {
            try {
                const machineTF = machineAvatar.method("get_transform").invoke();
                // Detach from player
                machineTF.method("SetParent", 2).invoke(NULL, true);
                
                // Restore physics
                const rb = machineAvatar.method("GetComponent", 1).inflate(Rigidbody).invoke();
                if (rb) rb.method("set_isKinematic").invoke(false);

                // Reveal body
                const localNet = getLocalNetPlayer();
                if (localNet) localNet.method("RPC_SetHide").invoke(false);
            } catch(_) {}
        }
        machineAvatar = null;
        sendNotification("Returned to Human form.", false);
    },
    toolTip: "Physically attaches the selling machine to your body and hides your player model."
}),

        ],
        [
            new ButtonInfo({
                buttonText: "Exit Themes",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
                buttonText: "Theme: pizza menu",
                method: () => {
                    applyTheme("Ember", [0.08, 0.08, 0.08, 0.75], [1.0, 0.333, 0.0, 1.0], [0.12, 0.12, 0.12, 0.75], [0.85, 0.35, 0.05, 1.0]);
                },
                isTogglable: false,
                toolTip: "Dark background with orange text."
            }),
            new ButtonInfo({
                buttonText: "Theme: blue kinda good",
                method: () => {
                    applyTheme("Arctic", [0.08, 0.16, 0.22, 0.78], [0.80, 0.96, 1.0, 1.0], [0.10, 0.26, 0.34, 0.82], [0.35, 0.75, 0.95, 1.0]);
                },
                isTogglable: false,
                toolTip: "Cool blue menu theme."
            }),
            new ButtonInfo({
                buttonText: "Theme: green ew",
                method: () => {
                    applyTheme("Toxic", [0.03, 0.08, 0.03, 0.82], [0.60, 1.0, 0.20, 1.0], [0.08, 0.16, 0.08, 0.82], [0.35, 0.95, 0.15, 1.0]);
                },
                isTogglable: false,
                toolTip: "Green acid-style menu theme."
            }),
            new ButtonInfo({
                buttonText: "Theme: pinkk :3",
                method: () => {
                    applyTheme("Cotton Candy", [0.18, 0.10, 0.18, 0.78], [1.0, 0.72, 0.90, 1.0], [0.24, 0.12, 0.24, 0.82], [0.95, 0.50, 0.80, 1.0]);
                },
                isTogglable: false,
                toolTip: "Pink and soft purple menu theme."
            }),
            new ButtonInfo({
                buttonText: "Theme: nice",
                method: () => {
                    applyTheme("Mono", [0.05, 0.05, 0.05, 0.78], [0.95, 0.95, 0.95, 1.0], [0.16, 0.16, 0.16, 0.82], [0.70, 0.70, 0.70, 1.0]);
                },
                isTogglable: false,
                toolTip: "Black, white, and gray menu theme."
            }),
        ],
        [
            new ButtonInfo({
                buttonText: "Exit Soundboard",
                method: () => { currentCategory = 0; currentPage = 0; },
                isTogglable: false,
                toolTip: "Returns you back to the main category."
            }),
            new ButtonInfo({
    buttonText: "PLAY THROUGH MIC",
    method: () => {
        if (soundFiles.length > 0 && photonRecorder) {
            try {
                // THE FIX: Use set_SourceType (1 = AudioSource)
                const setter = photonRecorder.method("set_SourceType");
                setter.invoke(1); 
                
                // Assign our AudioSource to the recorder's internal slot
                photonRecorder.field("AudioSource").value = soundboardSource;

                playCustomWav(soundFiles[currentSoundIdx]);
            } catch (e) { console.error("Play Error: " + e); }
        }
    }
}),
new ButtonInfo({
    buttonText: "STOP & RESTORE MIC",
    method: () => {
        try {
            // THE FIX FOR THE STOP ERROR:
            if (soundboardSource && !soundboardSource.handle.isNull()) {
                // We find the version of Stop that takes ZERO arguments
                let stopMethod = null;
                for (const m of soundboardSource.class.methods) {
                    if (m.name === "Stop" && m.parameterCount === 0) {
                        stopMethod = m;
                        break;
                    }
                }
                
                if (stopMethod) {
                    stopMethod.bind(soundboardSource).invoke();
                } else {
                    // Alternative: Just clear the clip to stop the noise
                    soundboardSource.method("set_clip").invoke(NULL);
                }
            }

            if (photonRecorder && !photonRecorder.handle.isNull()) {
                const setter = photonRecorder.method("set_SourceType");
                if (setter) setter.invoke(0); // 0 = Mic
            }
            sendNotification("Mic Restored.", false);
        } catch (e) { console.error("Restore Error: " + e); }
    }
}),
new ButtonInfo({
    buttonText: "Refresh Sound Folder",
    method: () => {
        refreshSoundList();
        prepareSoundboard();
        sendNotification("Soundboard Ready.", false);
    },
    isTogglable: false
}),

new ButtonInfo({
    buttonText: "Sound: " + (soundFiles[currentSoundIdx] || "None"),
    method: () => {
        currentSoundIdx = (currentSoundIdx + 1) % soundFiles.length;
        reloadMenu();
    },
    isTogglable: false,
    toolTip: "Cycle through your .wav files."
}),
        ]
    ];
    const adminButtons = [
        new ButtonInfo({
            buttonText: "Admin Test",
            method: () => { sendNotification("you are an admin lol", false); },
            isTogglable: false,
            toolTip: "Admin only button."
        }),
    ];
    let buttonMap = new Map();
    buttons.flat().forEach(button => { buttonMap.set(button.buttonText, button); });
    function getIndex(buttonText) { return buttonMap.get(buttonText); }
    const ButtonActivation = GorillaReportButton.method("OnTriggerEnter");
    ButtonActivation.implementation = function (collider) {
        const rawName = this.method("get_name").invoke().toString();
        if (rawName.length > 1 && rawName[1] == "@") {
            if (collider.handle.equals(referenceCollider.handle)) {
                const goName = rawName.substring(2, rawName.length - 1);
                const _time = Time.method("get_time").invoke();
                if (_time > buttonClickDelay) {
                    buttonClickDelay = _time + 0.2;
                    const button = getIndex(goName);
                    if (button) {
                        if (button.isTogglable) {
                            button.enabled = !button.enabled;
                            reloadMenu();
                            if (button.enabled) {
                                if (button.toolTip)
                                    sendNotification("[ENABLE] " + button.toolTip, false);
                                button.enableMethod?.();
                            }
                            else {
                                if (button.toolTip)
                                    sendNotification("[DISABLE] " + button.toolTip, false);
                                button.disableMethod?.();
                            }
                        }
                        else {
                            reloadMenu();
                            if (button.toolTip)
                                sendNotification(button.toolTip, false);
                            button.method?.();
                        }
                    }
                }
            }
            return;
        }
        return this.method("OnTriggerEnter").invoke(collider);
    };
    function updateInput() {
        const leftDevice = InputDevices.method("GetDeviceAtXRNode", 1).invoke(4);
        const rightDevice = InputDevices.method("GetDeviceAtXRNode", 1).invoke(5);
        const outBool = Il2Cpp.alloc(1);
        leftDevice.method("TryGetFeatureValue", 2).invoke(CommonUsages.field("primaryButton").value, outBool);
        leftPrimary = outBool.readU8() !== 0;
        leftDevice.method("TryGetFeatureValue", 2).invoke(CommonUsages.field("secondaryButton").value, outBool);
        leftSecondary = outBool.readU8() !== 0;
        leftDevice.method("TryGetFeatureValue", 2).invoke(CommonUsages.field("gripButton").value, outBool);
        leftGrab = outBool.readU8() !== 0;
        leftDevice.method("TryGetFeatureValue", 2).invoke(CommonUsages.field("triggerButton").value, outBool);
        leftTrigger = outBool.readU8() !== 0;
        leftDevice.method("TryGetFeatureValue", 2).invoke(CommonUsages.field("primary2DAxisClick").value, outBool);
        leftStick = outBool.readU8() !== 0;
        rightDevice.method("TryGetFeatureValue", 2).invoke(CommonUsages.field("primaryButton").value, outBool);
        rightPrimary = outBool.readU8() !== 0;
        rightDevice.method("TryGetFeatureValue", 2).invoke(CommonUsages.field("secondaryButton").value, outBool);
        rightSecondary = outBool.readU8() !== 0;
        rightDevice.method("TryGetFeatureValue", 2).invoke(CommonUsages.field("triggerButton").value, outBool);
        rightTrigger = outBool.readU8() !== 0;
        rightDevice.method("TryGetFeatureValue", 2).invoke(CommonUsages.field("gripButton").value, outBool);
        rightGrab = outBool.readU8() !== 0;
        rightDevice.method("TryGetFeatureValue", 2).invoke(CommonUsages.field("primary2DAxisClick").value, outBool);
        rightStick = outBool.readU8() !== 0;
    }
    const LateUpdate = GTPlayer.method("LateUpdate");
    LateUpdate.implementation = function () {
        deltaTime = Time.method("get_deltaTime").invoke();
        time = Time.method("get_time").invoke();
        updateInput();
        updateLastHeldIDFromHands();
        frameCount++;
        // --- HOTKEY NOCLIP LOGIC ---
if (noclipModMaster) {
    if (rightSecondary && time > noclipHotkeyCooldown) {
        noclipHotkeyCooldown = time + 0.5;
        isCurrentlyNoclipping = !isCurrentlyNoclipping;

        try {
            if (isCurrentlyNoclipping) {
                setPlayerCollidersEnabled(false);
                sendNotification("<color=red>Noclip: ON</color>", false, 1);
            } else {
                setPlayerCollidersEnabled(true);
                sendNotification("<color=green>Noclip: OFF</color>", false, 1);
            }
        } catch (e) { }
    }
}
        if ((righthand && rightSecondary) || (!righthand && leftSecondary)) {
            if (currentNotification != "" && time > notifactionResetTime)
                reloadMenu();
            if (menu == null)
                renderMenu();
            else
                recenterMenu();
        }
        else {
            if (menu != null) {
                Destroy(menu);
                menu = null;
            }
        }
        if (menu == null) {
            if (reference != null) {
                Destroy(reference);
                reference = null;
            }
        }
        else {
            if (reference == null)
                renderReference();
        }
        try {
            if (GunPointer != null) {
                if (!GunPointer.method("get_activeSelf").invoke()) {
                    Destroy(GunPointer);
                    GunPointer = null;
                }
                else
                    GunPointer.method("SetActive").invoke(false);
            }
            let lineObj = GunLine.method("get_gameObject").invoke();
            if (lineObj != null) {
                if (!lineObj.method("get_activeSelf").invoke()) {
                    Destroy(lineObj);
                    GunLine = null;
                }
                else
                    lineObj.method("SetActive").invoke(false);
            }
        }
        catch { }
        buttons.flat().filter(b => b.enabled).forEach(b => {
            if (b.method) {
                try {
                    b.method();
                }
                catch (e) {
                    console.error(`[LateUpdate] Error in '${b.buttonText}':`, e);
                }
            }
        });
        return LateUpdate.invoke();
    };
    function ArenaItemKilla() {
        const MyHook = AssemblyCSharp.class("AnimalCompany.ArenaItemKiller").method("DespawnIfNecessary");
        MyHook.implementation = function () { return; };
    }
    const infflareammo = AssemblyCSharp.class("AnimalCompany.FlareGun").method("get_hasAmmo");
    infflareammo.implementation = function () {
        if (InfAmmo)
            return true;
        return this.method("get_hasAmmo").invoke();
    };
    const infrevolverammo = AssemblyCSharp.class("AnimalCompany.Revolver").method("get_ammoLoaded");
    infrevolverammo.implementation = function () {
        if (InfAmmo)
            return 255;
        return this.method("get_ammoLoaded").invoke();
    };
    const infrevolverammo2 = AssemblyCSharp.class("AnimalCompany.Revolver").method("get_isHammerCocked");
    infrevolverammo2.implementation = function () {
        if (InfAmmo)
            return true;
        return this.method("get_isHammerCocked").invoke();
    };
    const ziplineinfammo = AssemblyCSharp.class("AnimalCompany.ZiplineGun").method("get_isLoaded");
    ziplineinfammo.implementation = function () {
        if (InfAmmo)
            return true;
        return this.method("get_isLoaded").invoke();
    };
    try {
    const shotgunCanShoot = AssemblyCSharp.class("AnimalCompany.Shotgun").method("get_canShoot");
    shotgunCanShoot.implementation = function () {
        if (zeroCooldown) return true;
        return this.method("get_canShoot").invoke();
    };
} catch (e) { }
try {
    const arenaCanShoot = AssemblyCSharp.class("AnimalCompany.AutoReloadGun").method("get_canShoot");
    arenaCanShoot.implementation = function () {
        if (zeroCooldown) return true;
        return this.method("get_canShoot").invoke();
    };
} catch (e) { }
    const shotguninfammo = AssemblyCSharp.class("AnimalCompany.Shotgun").method("get__ammoLeft");
    shotguninfammo.implementation = function () {
        if (InfAmmo)
            return 255;
        return this.method("get__ammoLeft").invoke();
    };
    try {
    const gunRecoil = AssemblyCSharp.class("AnimalCompany.Gun").method("RPC_AnimateShot");
    gunRecoil.implementation = function () {
        if (noRecoil) {
            // We skip the recoil force but still animate the shot if we want visuals
            // Or we can just call it and then zero out the recoil field if it exists
            this.method("RPC_AnimateShot").invoke();
            // If there's a m_Recoil field, we zero it here
        } else {
            this.method("RPC_AnimateShot").invoke();
        }
    };
} catch (e) { }
    const rpginfammo = AssemblyCSharp.class("AnimalCompany.RPG").method("get_loadedState");
    rpginfammo.implementation = function () {
        const state = this.method("get_loadedState").invoke();
        if (InfAmmo) {
            state.field("isLoaded").value = true;
            this.method("set_loadedState").invoke(state);
        }
        return state;
    };
    const arenagunsinfammo = AssemblyCSharp.class("AnimalCompany.AutoReloadGun").method("get__ammoLeft");
    arenagunsinfammo.implementation = function () {
        if (InfAmmo)
            return 255;
        return this.method("get__ammoLeft").invoke();
    };
    const InfJetpack = AssemblyCSharp.class("AnimalCompany.JetpackHandy").method("RPC_UseJetpack");
    InfJetpack.implementation = function () {
        if (InfAmmo) {
            this.method("RPC_UseJetpack").invoke();
            this.field("_isUsed").value = false;
        }
        else {
            this.method("RPC_UseJetpack").invoke();
        }
    };
    const EjectItem = AssemblyCSharp.class("AnimalCompany.StashMachine.StashMachineTrashChuteView").method("EjectItem");
    EjectItem.implementation = function (item) {
        if (stashDupeEnabled) {
            for (let i = 0; i < ejectDupeAmount; i++) {
                this.method("EjectItem").invoke(item);
            }
        }
        else {
            this.method("EjectItem").invoke(item);
        }
    };
    function installMasterShield() {
    const NetPlayerCls = AssemblyCSharp.class("AnimalCompany.NetPlayer");
    
    // THE ULTIMATE BLOCK LIST (From your RPC Dump)
    const hostileRPCs = [
        "RPC_ApplyBuff",            // Speed/Slow/Poison
        "RPC_AddForce",             // Flinging/Pushing
        "RPC_Teleport",            // Forced TP to void/lava
        "RPC_TagAsStinky",         // Green fart cloud
        "RPC_KickPlayer",          // Getting kicked from lobby
        "KickPlayer",              // Secondary kick method
        "RPC_PlayerStun",          // Ragdolling/Freezing
        "RPC_SetColorHSV",         // Flashing colors/Rainbow troll
        "RPC_ShakeScreen",         // Nausea/Earthquake effect
        "RPC_SetRadioActive",      // Green screen overlay
        "RPC_SetJellyEffect",      // Wobbly screen effect
        "RPC_DoPlayerDie",         // Remote killing
        "RPC_AttachToGiantHand",   // Kidnapping by monsters
        "RPC_AttachTo",            // Stuck to objects
        "RPC_AttachToAttachable",  // Stuck to other players
        "RPC_SetMuffledVoiceEnabled", // Voice sabotage
        "RPC_SetSqueakyVoiceEnabled", // Pitch sabotage
        "RPC_AwardKill"            // Spoofing your stats
    ];

    hostileRPCs.forEach(rpcName => {
        try {
            const method = NetPlayerCls.method(rpcName);
            if (method) {
                method.implementation = function (...args) {
                    // CHECK: Is this RPC hitting ME? 
                    // AND: Is it NOT from my own menu (_selfRPCBypass)?
                    if (this.method("get_IsMine").invoke() && !_selfRPCBypass) {
                        console.log(`[SHIELD] Blocked incoming: ${rpcName}`);
                        // Return nothing -> The packet is deleted and never reaches the game
                        return; 
                    }
                    // Allow the RPC if it's for someone else or sent by us
                    return method.invoke(...args);
                };
            }
        } catch (e) {
            // Skip if the game version doesn't have this specific RPC
        }
    });

    // --- SPECIAL CASE: RPC_PlayerHit (Damage) ---
    // This often has 'Overloads' (multiple versions), so we hook all of them.
    try {
        NetPlayerCls.method("RPC_PlayerHit").overloads().forEach(overload => {
            overload.implementation = function (...args) {
                if (this.method("get_IsMine").invoke() && !_selfRPCBypass) {
                    console.log(`[SHIELD] Blocked Damage Packet (RPC_PlayerHit)`);
                    return;
                }
                return overload.invoke(...args);
            };
        });
    } catch (e) { }

    console.log("rpc protection activated you foid");
}
    function quiverHook() {
        let _inAddToBagAck = false;
        try {
            GBIClass.method("get_canAddToBag").implementation = function () { return true; };
        }
        catch (e) { }
        try {
            GBIClass.method("get_allowAddToBag").implementation = function () { return true; };
        }
        catch (e) { }
        try {
            GBIClass.method("get_allowAttachToItem").implementation = function () { return true; };
        }
        catch (e) { }
        try {
            GBIClass.method("get_allowAddToQuiver").implementation = function () { return true; };
        }
        catch (e) { }
        try {
            GBIClass.method("CanBeAddedToContainer").implementation = function (_) { return true; };
        }
        catch (_) { }
        try {
            GBIClass.method("get_isContainable").implementation = function () { return true; };
        }
        catch (_) { }
        try {
            GBIClass.method("get_canBePickedUp").implementation = function () { return true; };
        }
        catch (_) { }
        try {
            const GISClass = AssemblyCSharp.class("AnimalCompany.GameplayItemState");
            function _isBagLike(self) {
                try {
                    const idObj = self.method("get_id").invoke();
                    if (!idObj || idObj.isNull())
                        return false;
                    const id = idObj.content ?? "";
                    return (id.indexOf("stash") !== -1 ||
                        id.indexOf("quiver") !== -1 ||
                        id.indexOf("crossbow") !== -1 ||
                        id.indexOf("heart_gun") !== -1 ||
                        id.indexOf("grenade_launcher") !== -1 ||
                        id.indexOf("salmoncannon") !== -1 ||
                        id.indexOf("salmon_cannon") !== -1);
                }
                catch (_) {
                    return false;
                }
            }
            try {
                const _isBagOrig = GISClass.method("get_isBag");
                GISClass.method("get_isBag").implementation = function () {
                    if (_isBagLike(this))
                        return true;
                    return _isBagOrig.invoke.call(this);
                };
            }
            catch (_) { }
            try {
                const _baseCapOrig = GISClass.method("get_baseCapacity");
                GISClass.method("get_baseCapacity").implementation = function () {
                    if (_isBagLike(this)) {
                        try {
                            const sp = _baseCapOrig.invoke.call(this);
                            if (sp && !sp.isNull()) {
                                try {
                                    sp.field("_value").value = 9999;
                                }
                                catch (_) { }
                                try {
                                    sp.field("value").value = 9999;
                                }
                                catch (_) { }
                            }
                            return sp;
                        }
                        catch (_) { }
                    }
                    return _baseCapOrig.invoke.call(this);
                };
            }
            catch (_) { }
            try {
                const _totCapOrig = GISClass.method("get_totalCurrCapacity");
                GISClass.method("get_totalCurrCapacity").implementation = function () {
                    if (_isBagLike(this))
                        return 9999;
                    return _totCapOrig.invoke.call(this);
                };
            }
            catch (_) { }
            try {
                GISClass.method("get_allowAddToQuiver").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                GISClass.method("get_allowAddToBag").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                GISClass.method("get_allowAttachToItem").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                GISClass.method("get_allowAttachToBack").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                GISClass.method("get_allowAttachToHip").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                GISClass.method("get_isBackpack").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                GISClass.method("get_containerCapacity").implementation = function () { return 9999; };
            }
            catch (_) { }
            try {
                GISClass.method("get_maxContainerCapacity").implementation = function () { return 9999; };
            }
            catch (_) { }
        }
        catch (_) { }
        try {
            BackpackItemClass.method("CheckToAddItem").implementation = function (_) { return true; };
        }
        catch (e) {
            smartError("[quiverHook] BackpackItem.CheckToAddItem:", e);
        }
        try {
            BackpackItemClass.method("get__baseCapacity").implementation = function () { return 9999; };
        }
        catch (e) { }
        try {
            BackpackItemClass.method("get_capacity").implementation = function () { return 9999; };
        }
        catch (e) { }
        try {
            BackpackItemClass.method("set_capacity").implementation = function (_v) { };
        }
        catch (e) { }
        try {
            BackpackItemClass.method("get_isFull").implementation = function () { return false; };
        }
        catch (e) { }
        try {
            BackpackItemClass.method("get_canGrabContainedItems").implementation = function () { return true; };
        }
        catch (e) { }
        try {
            BackpackItemClass.method("get_canUse").implementation = function () { return true; };
        }
        catch (_) { }
        try {
            BackpackItemClass.method("get_canOpen").implementation = function () { return true; };
        }
        catch (_) { }
        try {
            BackpackItemClass.method("HandleAddItemTrigger").implementation = function (collider) {
                try {
                    this.method("set_isOpen").invoke(true);
                }
                catch (_) { }
                this.method("HandleAddItemTrigger").invoke(collider);
            };
        }
        catch (e) { }
        try {
            BackpackItemClass.method("CheckToEmptyItem").implementation = function () { return this.method("CheckToEmptyItem").invoke(); };
        }
        catch (e) { }
        try {
            BackpackItemClass.method("AddToBagAck").implementation = function (obj) {
                if (_inAddToBagAck)
                    return true;
                _inAddToBagAck = true;
                try {
                    try {
                        this.field("_capacity").value = 9999;
                    }
                    catch (_) { }
                    try {
                        this.field("capacity").value = 9999;
                    }
                    catch (_) { }
                    try {
                        this.method("set_capacity").invoke(255);
                    }
                    catch (_) { }
                    try {
                        this.method("set_isOpen").invoke(true);
                    }
                    catch (_) { }
                    return this.method("AddToBagAck").invoke(obj);
                }
                catch (_) {
                    return true;
                }
                finally {
                    _inAddToBagAck = false;
                }
            };
        }
        catch (e) {
            smartError("[quiverHook] BackpackItem.AddToBagAck:", e);
        }
        try {
            QuiverClass.method("CheckToAddItem").implementation = function (item) {
                // Prevent quiver-into-quiver (would cause infinite loops)
                try {
                    const asQuiver = item.method("GetComponent", 1).inflate(QuiverClass).invoke();
                    if (asQuiver && !asQuiver.isNull())
                        return false;
                }
                catch (_) { }
                // Bypass allowAddToQuiver check — force-allow all items by patching the flag then calling original
                try {
                    const ec = item.field("equippingConfig").value;
                    if (ec && !ec.isNull()) {
                        try {
                            ec.field("allowAddToQuiver").value = true;
                        }
                        catch (_) { }
                        try {
                            ec.field("allowAddToBag").value = true;
                        }
                        catch (_) { }
                    }
                }
                catch (_) { }
                return this.method("CheckToAddItem").invoke(item);
            };
        }
        catch (e) {
            smartError("[quiverHook] Quiver.CheckToAddItem:", e);
        }
        try {
            QuiverClass.method("CanAddItem").implementation = function (item) {
                // Prevent quiver-into-quiver
                try {
                    const asQuiver = item.method("GetComponent", 1).inflate(QuiverClass).invoke();
                    if (asQuiver && !asQuiver.isNull())
                        return false;
                }
                catch (_) { }
                // Allow ALL other items unconditionally
                return true;
            };
        }
        catch (_) { }
        // TryAddItem for Quiver — mirrors BackpackItem so shop→quiver works like shop→bag
        try {
            QuiverClass.method("TryAddItem").implementation = function (obj) {
                if (_inAddToBagAck)
                    return;
                _inAddToBagAck = true;
                try {
                    try {
                        this.field("_capacity").value = 9999;
                    }
                    catch (_) { }
                    try {
                        this.method("set_capacity").invoke(255);
                    }
                    catch (_) { }
                    return this.method("TryAddItem").invoke(obj);
                }
                catch (_) { }
                finally {
                    _inAddToBagAck = false;
                }
            };
        }
        catch (_) { }
        try {
            QuiverClass.method("get__baseCapacity").implementation = function () { return 9999; };
        }
        catch (e) { }
        try {
            QuiverClass.method("get_capacity").implementation = function () { return 9999; };
        }
        catch (e) { }
        try {
            QuiverClass.method("get_isFull").implementation = function () { return false; };
        }
        catch (_) { }
        try {
            QuiverClass.method("get_canGrabContainedItems").implementation = function () { return true; };
        }
        catch (e) { }
        try {
            QuiverClass.method("get_canUse").implementation = function () { return true; };
        }
        catch (_) { }
        try {
            QuiverClass.method("get_canOpen").implementation = function () { return true; };
        }
        catch (_) { }
        try {
            QuiverClass.method("get_canGrabContainedItems").implementation = function (_) { return true; };
        }
        catch (e) {
            smartError("[quiverHook] Quiver.canGrabContainedItems:", e);
        }
        if (CrossbowClass) {
            try {
                CrossbowClass.method("CheckToAddItem").implementation = function (_) { return true; };
            }
            catch (_) { }
            try {
                CrossbowClass.method("CanAddItem").implementation = function (_) { return true; };
            }
            catch (_) { }
            try {
                CrossbowClass.method("get_capacity").implementation = function () { return 9999; };
            }
            catch (_) { }
            try {
                CrossbowClass.method("get__baseCapacity").implementation = function () { return 9999; };
            }
            catch (_) { }
            try {
                CrossbowClass.method("get_isFull").implementation = function () { return false; };
            }
            catch (_) { }
            try {
                CrossbowClass.method("get_canGrabContainedItems").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                CrossbowClass.method("get_canUse").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                CrossbowClass.method("get_canOpen").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                CrossbowClass.method("AddToBagAck").implementation = function (obj) {
                    if (_inAddToBagAck)
                        return true;
                    _inAddToBagAck = true;
                    try {
                        try {
                            this.field("_capacity").value = 9999;
                        }
                        catch (_) { }
                        try {
                            this.method("set_capacity").invoke(255);
                        }
                        catch (_) { }
                        return this.method("AddToBagAck").invoke(obj);
                    }
                    catch (_) {
                        return true;
                    }
                    finally {
                        _inAddToBagAck = false;
                    }
                };
            }
            catch (_) { }
        }
        if (HeartGunClass) {
            try {
                HeartGunClass.method("CheckToAddItem").implementation = function (_) { return true; };
            }
            catch (_) { }
            try {
                HeartGunClass.method("CanAddItem").implementation = function (_) { return true; };
            }
            catch (_) { }
            try {
                HeartGunClass.method("get_capacity").implementation = function () { return 9999; };
            }
            catch (_) { }
            try {
                HeartGunClass.method("get__baseCapacity").implementation = function () { return 9999; };
            }
            catch (_) { }
            try {
                HeartGunClass.method("get_isFull").implementation = function () { return false; };
            }
            catch (_) { }
            try {
                HeartGunClass.method("get_canGrabContainedItems").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                HeartGunClass.method("get_canUse").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                HeartGunClass.method("get_canOpen").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                HeartGunClass.method("get_isBackpack").implementation = function () { return true; };
            }
            catch (_) { }
            try {
                HeartGunClass.method("AddToBagAck").implementation = function (obj) {
                    if (_inAddToBagAck)
                        return true;
                    _inAddToBagAck = true;
                    try {
                        try {
                            this.field("_capacity").value = 9999;
                        }
                        catch (_) { }
                        try {
                            this.method("set_capacity").invoke(255);
                        }
                        catch (_) { }
                        return this.method("AddToBagAck").invoke(obj);
                    }
                    catch (_) {
                        return true;
                    }
                    finally {
                        _inAddToBagAck = false;
                    }
                };
            }
            catch (_) { }
        }
        if (GrenadeLauncherClass) {
            try {
                GrenadeLauncherClass.method("CheckToAddItem").implementation = function (_) { return true; };
            }
            catch (_) { }
            try {
                GrenadeLauncherClass.method("get_capacity").implementation = function () { return 9999; };
            }
            catch (_) { }
            try {
                GrenadeLauncherClass.method("set_capacity").implementation = function (_v) { };
            }
            catch (_) { }
            try {
                GrenadeLauncherClass.method("get_cappedCapacity").implementation = function () { return 9999; };
            }
            catch (_) { }
        }
        console.log("[quiverHook] v5 — bags openable, grenade/salmon as bags, no quiver-in-quiver");
    }
    quiverHook();
    try {
        const GunClass = AssemblyCSharp.class("AnimalCompany.Gun");
        const gunShootHook = GunClass.method("Shoot");
        gunShootHook.implementation = function () {
            if (noRecoil || infDamage) {
                try {
                    const cfg = this.method("get_config").invoke();
                    if (cfg && !cfg.isNull()) {
                        // Save originals
                        const origRecoil = cfg.field("recoilForceMag").value;
                        const origHandRecoil = cfg.field("handRecoilForceMag").value;
                        const origSpread = cfg.field("shotSpread").value;
                        let origDmg = null;
                        let origBulletDmg = null;
                        let origDamagePerBullet = null;
                        let origHitDamage = null;
                        // Apply NoRecoil
                        if (noRecoil) {
                            cfg.field("recoilForceMag").value = 0.0;
                            cfg.field("handRecoilForceMag").value = 0.0;
                            cfg.field("shotSpread").value = 0.0;
                        }
                        if (infDamage) {
                            try {
                                origDmg = cfg.field("damage").value;
                                cfg.field("damage").value = 999999.0;
                            }
                            catch (_) { }
                            try {
                                origBulletDmg = cfg.field("bulletDamage").value;
                                cfg.field("bulletDamage").value = 999999.0;
                            }
                            catch (_) { }
                            try {
                                origDamagePerBullet = cfg.field("damagePerBullet").value;
                                cfg.field("damagePerBullet").value = 999999.0;
                            }
                            catch (_) { }
                            try {
                                origHitDamage = cfg.field("hitDamage").value;
                                cfg.field("hitDamage").value = 999999.0;
                            }
                            catch (_) { }
                        }
                        const result = this.method("Shoot").invoke();
                        if (noRecoil) {
                            cfg.field("recoilForceMag").value = origRecoil;
                            cfg.field("handRecoilForceMag").value = origHandRecoil;
                            cfg.field("shotSpread").value = origSpread;
                        }
                        if (infDamage) {
                            try {
                                if (origDmg !== null)
                                    cfg.field("damage").value = origDmg;
                            }
                            catch (_) { }
                            try {
                                if (origBulletDmg !== null)
                                    cfg.field("bulletDamage").value = origBulletDmg;
                            }
                            catch (_) { }
                            try {
                                if (origDamagePerBullet !== null)
                                    cfg.field("damagePerBullet").value = origDamagePerBullet;
                            }
                            catch (_) { }
                            try {
                                if (origHitDamage !== null)
                                    cfg.field("hitDamage").value = origHitDamage;
                            }
                            catch (_) { }
                        }
                        return result;
                    }
                }
                catch (_) { }
            }
            return this.method("Shoot").invoke();
        };
    }
    catch (e) {
        smartError("[Gun.Shoot hook failed]:", e);
    }
    try {
        GBIClass.method("HandleHit").implementation = function (other, velocity) {
            if (infDamage) {
                try {
                    const origDmg = this.field("_hitDamage").value;
                    this.field("_hitDamage").value = 2147483648.0;
                    const result = this.method("HandleHit").invoke(other, velocity);
                    this.field("_hitDamage").value = origDmg;
                    return result;
                }
                catch (_) { }
            }
            return this.method("HandleHit").invoke(other, velocity);
        };
    }
    catch (_) { }
    try {
        const DamageAttributeClass = AssemblyCSharp.class("AnimalCompany.DamageAttribute");
        DamageAttributeClass.method("GetDamage").implementation = function (damageableType) {
            if (infDamage)
                return 2147483647;
            return this.method("GetDamage").invoke(damageableType);
        };
        console.log("[InfDamage] DamageAttribute.GetDamage hooked");
    }
    catch (e) {
        smartError("[InfDamage] DamageAttribute.GetDamage hook failed:", e);
    }
    //const StashQOL = AssemblyCSharp.class("AnimalCompany.StashMachineSecurityKeypad").method("HandleKeyPressed");
    //StashQOL.implementation = function(key) {
    //    for (let i = 0; i < 4; i++) {
    //        this.method("HandleKeyPressed").invoke(key);
    //    }
    //};
    //    function vpnhook() {
    //    const VPNDetector = AssemblyCSharp.class("AnimalCompany.VPNDetector");
    //    const VPNUpdate = VPNDetector.method("Update");
    //    VPNUpdate.implementation = function() {
    //      return;
    //    };
    //    const CheckVPN = VPNDetector.method("CheckVPNIsActive");
    //    CheckVPN.implementation = function() {
    //      return;
    //    };
    //    const AntiCheat = AssemblyCSharp.class("AnimalCompany.AntiCheatSystem");
    //    const AntiCheatUpdate = AntiCheat.method("Update");
    //    AntiCheatUpdate.implementation = function() {
    //      return;
    //    };
    //}
    ArenaItemKilla();
    blockrpc();
    // vpnhook();
    console.log(`Pizza Menu loaded v${version}`);
}, "main" );
}, 5000); // 5000 = 5 seconds
